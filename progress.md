Original prompt: Resolve the complete race-experience backlog, make the authoritative track drive every consumer, and ship the latest production build on 127.0.0.1:8777.

## Eight runtime optimization findings resolved - 2026-09-12

- Replay recording now reads authoritative progress directly; the browser builds only visible menu/results projections with invalidated strategy caches; each field-feedback update constructs classification once and avoids duplicate HUD delivery.
- Disabled pack particles skip all update/burst work. Fully behind-camera rivals keep their scalar animation state advancing while skipping node/limb articulation; a conservative six-metre margin and camera-cut restoration preserve their current pose. No AI, physics, rendering quality, or particle-budget tuning was reduced.
- Replay loading validates once and takes one owned copy. Indexed event lookup retains pause, seek, loop, equal-time, unsorted-import order, and mutation-isolation behavior. Input-label and HUD text caches preserve rebind/device changes, continuous Flow meter updates, and brake-stage colors.
- New runtime regression passes exact child/MultiMesh pose equivalence after 120 hidden updates, particle disable/re-enable, replay boundaries and ownership, menu invalidation, single classification delivery plus session-only compatibility, and HUD value/color updates. Added to the normal quality gate; it fails if a test section exits early.
- Release Web microbenchmarks (seven warmed batches, identical software-rendered 1280x720 scenario) reduced per-call recording-progress cost 532.73 to 0.934 us, closed-menu browser projection 8274.21 to 1178.74 us, binding labels 6.89 to 2.636 us, replay load 174283.8 to 93690 us, and replay advance/event lookup 624.64 to 30.02 us. These are isolated CPU costs, not FPS or real-device GPU claims. Release memory counter was unavailable.
- All 27 focused Godot probes passed, covering replay, caches, input/settings/touch, launch, animation/camera, fairness, feature contracts, menu/history/builds, onboarding, results, presentation, and long-session bounds. All 17 web delivery/recovery tests passed. This is scoped regression validation, not a rerun of the full balance matrix.
- Browser gameplay and Flow-denial screenshots at 2560x1600, 1280x720, and 844x390 were inspected: no new presentation regression, console/page errors, or horizontal overflow. Evidence: `output/optimization-visual/`; final shared-client evidence: `output/optimization-required-client/`. The performance and high-resolution skills drove release-template comparisons and rendered multi-resolution checks instead of quality reductions.
- Reproducible opt-in diagnostic: `features/diagnostics/runtime_work_benchmark.gd` and `tools/benchmark_web_runtime.mjs`. Full measurements, preservation contracts, and reproduction instructions: `docs/RUNTIME_OPTIMIZATION_REPORT.md`.
- Final local package: `index.e76e41bbcf95.pck`, 5,533,488 bytes, SHA-256 `e76e41bbcf95e694ca5f94abaab8922ff01c636d14053580a03d27c681a66987`. Existing uncommitted HUD polish was preserved. No commit, push, or publication requested this turn. Remaining external validation is representative-device frame pacing and player comfort under `docs/PLAYER_VALIDATION_PROTOCOL.md`; no implementation item from this eight-finding list remains.

## Gameplay readability and visual polish - 2026-09-12

- Rebuilt the speed/gear/Flow instrument with separated rows, a contextual cost marker, exact Flow shortfall, ready/input prompts, and active-technique status. Assist text is no longer obscured by the instrument background.
- Start prompts include current control bindings. GO now expires on an independent timer; ordinary technique receipts cannot prolong it or overwrite a still-live warning. Duplicate center/line technique text is suppressed.
- Race status colors use an untinted base, fast-line directions use reliable text glyphs, and the secondary course-decision lane is wider and bounded.
- Captions occupy a dedicated two-line strip below line scoring. New cues supersede queued cues from the same source, preventing old countdown numbers from replaying after GO while retaining unrelated warnings.
- Short landscape browser windows reclaim header space while keeping a 44px fullscreen button in a separate gutter. This follows the local frontend checklist's focus, safe-area, and no-cropping guidance; its optional remote audit tools were unavailable.
- Nine focused Godot probes passed: new HUD readability, presentation, captions, launch, input prompts, racecraft integration, assists, reduced motion, and academy presentation. All 17 web delivery/recovery tests passed; this is scoped validation, not a rerun of the full game/balance suite.
- Browser screenshots at 2560x1600, 1280x720, and 844x390 were inspected with zero reported console/page errors or horizontal overflow. Compact canvas grew from 549x308 to 677x380 pixels (23% per dimension); fullscreen remains in its own gutter. Evidence: `output/hud-polish-visual/` and `output/hud-polish-final-client/`. Software-rendered checks are not an on-device performance certification.
- Final hazard-priority regression also passed: roost warnings survive the bounded decision lane ahead of routine technique receipts. Rebuilt local package: `index.28f8b12cc9d9.pck`, 5,524,744 bytes, SHA-256 `28f8b12cc9d98bf3c69e0e42a22aabe2447e5cf2a38825326fabae7a4dec0bf7`. Existing balance, progression, saves, and opponent tuning are unchanged. No commit, push, or publication requested in this turn.

## Six-finding gameplay audit remediation - 2026-09-11

- Guided first-ride presentation now yields to the full kit/purchase UI when browsing locked or alternate kits, or when repairs are needed. Assist, tune, bike and condition copy reflects the active configuration.
- Rival side separation is expressed per second, and vertical clearance gates following, shoves and NPC contact. The focused fairness probe covers jump-over/landing contacts and 30/60/120Hz side pressure.
- Nearby one-shot Flow warnings preempt incidental race chatter; the existing persistent bike boost pose/effect is retained. The regression drives the pack signal into the controller/HUD/audio/caption path with an occupied cooldown.
- Version-2 recorded plans preserve five named difficulty modes and all five numeric tiers. Comparisons include detailed assists and condition; legacy evidence is explicitly incomplete rather than execution-comparable. Save round-trip coverage includes Expert/tier 4.
- Combined pace telemetry includes tactical boosts and director correction, plus the applicable reserve and per-step cap excess. Values are nominal additive m/s before shared terrain/drive multipliers.
- Balance simulations now use 60Hz and pair every track/mode baseline with traffic. This revealed the strong starter Flow benchmark falling to P12; LITE opponent class scaling was adjusted from 0.94 to 0.92. The unchanged strong benchmark now reaches P8 in the focused rerun.
- Opponent acceptance passed all 37 full-course simulations across three tracks with zero failures, including all five modes both with/without traffic, deterministic replay, starter/upgraded builds, strong Flow (P8), elite Flow (P1) and defense. Original acceptance windows were retained.
- Packaged-browser verification: the guided/locked/denied/restored/actual-riding sequence passed at 2560x1600, 1280x720 and 844x390, with no browser errors or horizontal overflow. Screenshots and state evidence are under `output/gameplay-audit-visual/`; final shared-client startup evidence is under `output/gameplay-audit-final-client/`.
- Current local package: `index.59429b028ed6.pck`, 5,522,440 bytes, SHA-256 `59429b028ed6bb5329a8c807c22044328ed79b1f4dc91cbb6619b6efeb0b28ab`. All 17 delivery tests passed. No commit, push or Pages deployment was requested for this remediation turn.
- Final validation: `QUALITY GATE PASS: probes=105 activities=6 full=True` in `output/gameplay-audit-full-gate.log`, plus 17/17 web delivery tests. The updated production lifecycle and migration tests also passed targeted reruns after the final changes. `git diff --check` passed.
- Remaining external validation only: representative-player comfort/fun and real-device performance per `docs/PLAYER_VALIDATION_PROTOCOL.md`; automation does not certify those qualities. All six code-backed audit findings are addressed; no implementation TODO remains for this list.

## Opponent-challenge polish GitHub Pages publication - 2026-09-08

- **Source publication:** Commit `8496cf1` (`Polish opponent challenge and first ride`) is pushed to `origin/codex/racecraft-v27` with the complete 104-probe, six-smoke, 17-delivery-test candidate and content-addressed Web bundle.
- **Pages deployment:** Commit `6926e59ed02d8cf2e7a913c2b903d410e0a08d08` is pushed to `origin/gh-pages`; GitHub Pages workflow `34196076963` completed successfully at `https://willtran87.github.io/project-riding-dirty/`.
- **Independent public verification:** The public manifest, game HTML, and downloaded `index.2dc376e14a6e.pck` agree on `5,520,568` bytes and SHA-256 `2dc376e14a6eadacd8df8680b7a737bad4c918105d8c88d31a88383a99720c7f`; the superseded `index.fcedba016124.pck` returns 404.
- **Live browser proof:** The prescribed shared-action client opened the public Quarry Trail Garage at Event 01/19, reported schema 13, and produced an inspected clean 1280x720 frame with no console/page-error artifact.

## Professional gameplay polish, fair rival pressure, and progressive first ride - 2026-09-07

- **Five-step challenge curve:** Relaxed, Casual, Standard, Challenging, and Expert now have distinct pace, skill, pressure, Flow-gain, Flow-output, and tactical-budget contracts. The complete three-track matrix covers all five modes plus strong, elite, and max-build benchmarks in 22 deterministic full-course simulations.
- **Rivals use readable Flow without hidden cheating:** Opponents earn Flow from draft/line execution and spend it on bounded boosts. Late pressure, close attacks, and Flow now share one inspectable positive pace cap with raw/applied/clamp telemetry; nearby attacks identify the rival and signature trait through HUD warning, audio, and captions.
- **Cleaner, safer pack racing:** Each NPC owns an explicit visual-sized contact envelope while deterministic symmetric resolution stays authoritative. A 2.5-second start-contact immunity prevents launch pileups, cooldown/separation tuning lowers traffic noise, and full-race starter traffic stays within `3.80` contacts and `0.48` crashes per rider-minute.
- **First ride is now a decision, not a dashboard:** A pristine profile opens on Quarry Trail, Event 01/19 with one Balanced-kit/Sport-assist choice, clear start and optional-Workshop actions, and a short next-goal rail. Advanced history, comparison, repair, and metric detail progressively appears after the first official run; Workshop paging shows the current category with its immediate neighbors instead of all 11 tabs at once.
- **Progression supports experimentation:** Saved complete-bike strategies expand from three to eight slots, official per-event run history expands from five to 12, pairwise pages only exist for occupied builds, and both bounded capacities survive overflow, tamper, and JSON round-trip coverage.
- **Lifecycle cleanup:** Local Duel and Custom Tour probes release detached persistence services cleanly; focused runs complete without orphan-node leak warnings.
- **Complete source proof:** Godot 4.7 parser/editor scan, all `104` discovered probes, and all six Academy/Circuit/Pine/Mesa/Freestyle/Discovery playable smokes pass. The rival feedback probe verifies real opponent Flow activation through controller, HUD, warning audio, and semantic caption; the launch/contact probe verifies exact envelopes and start protection.
- **Fresh browser candidate:** `index.2dc376e14a6e.pck` (`5,520,568` bytes; SHA-256 `2dc376e14a6eadacd8df8680b7a737bad4c918105d8c88d31a88383a99720c7f`) ships beside `index.9778df3b5d0e.wasm`; all `17` delivery/recovery tests pass. Live HTTP on port `8777` reports build `racecraft-v28`, exact length, immutable caching, COOP `same-origin`, and COEP `require-corp`.
- **Rendered browser proof:** The required shared-action client produced clean schema-13 Garage state and no console/page-error artifact. A separate rendered matrix entered live race staging at `2560x1600`, `1280x720`, and `800x900`, with frame-anchored canvases and no document overflow; inspected Garage, countdown, gameplay, and 2560x1600 route images remain readable and unclipped.
- **Acceptance boundary:** All actionable engineering findings are closed, but automation cannot honestly certify subjective fun, audiovisual taste, learning comfort, or physical-device feel. Those claims remain governed by `docs/PLAYER_VALIDATION_PROTOCOL.md` and require recorded representative-player sessions.

## Durable references, sector drilldown, and build experiments - 2026-08-09

- **Comparison target is now a player choice:** Profile schema 12 can pin any retained official Previous/PB result as a durable reference. The sanitized copy survives serialization and remains available after the five-entry recent list advances; invalid result IDs are refused without mutation.
- **Every split is browsable:** Workshop History compares Latest against the pinned result when present (otherwise PB), exposes one read-only page per sector with exact times and signed delta, and retains the shared attribution plus next-run recommendation. Pinning, plan application, and sector acknowledgment have distinct actions and explicit feedback.
- **Saved builds are measurable experiments:** Every occupied build shows exact physical deltas against the active configuration, and A/B, A/C, and B/C comparison pages quantify drive, grip, speed, suspension, and braking without applying either build. This turns saved presets into controlled strategy choices instead of opaque labels.
- **Focused deterministic proof:** Parser/editor import plus setup-history, Garage interaction, saved-build, Web-state, track-evolution, persistence, profile-meta, and run-analysis probes pass. Coverage includes pin durability, fabricated-ID refusal, sector read-only behavior, pairwise comparison read-only behavior, schema-13 browser projection, and the existing 7,000-byte state budget.
- **Complete release proof:** All `103` discovered probes and all six Academy/Circuit/Pine/Mesa/Freestyle/Discovery smokes pass. The fresh content-addressed Web candidate is `index.3a02d08ed18e.pck` (`5,514,136` bytes; SHA-256 `3a02d08ed18ec12e0e176224746fd7855c01ec637b80af167ae9fb0de826725e`) beside `index.9778df3b5d0e.wasm`; all `17` delivery/recovery tests pass.
- **Packaged browser proof:** The prescribed shared-action client produced `output/durable-comparison-final-client/shot-0.png` through `shot-2.png` with matching schema-13 states. Inspected 1280x720 Balanced and Attack frames retain the clean Quarry Event 01 layout, exact three-way runtime bars, and readable setup consequences without clipping; no console/page-error artifact was produced. Live HTTP length, immutable caching, COOP/COEP headers, manifest, disk hash, and inner-page pack reference agree.
- **Acceptance boundary:** Fun, learning, physical control feel, accessibility with representative players, and real-device performance remain governed by `docs/PLAYER_VALIDATION_PROTOCOL.md`; automation cannot honestly substitute for those participants or devices.

## Multi-run race analysis and comprehensive acceptance authority - 2026-08-08

- **Previous versus PB now teaches, not merely archives:** A pure result-analysis authority compares total and sector time, finds the costliest PB gap and latest-run gain, and reports changes in crashes, contacts, resets, and Flow usage. Garage History and browser-readable state share the same bounded result.
- **No false setup causation:** Frozen bike, class, kit, parts, tune, assists, difficulty, controls, crash support, weather, and surface determine whether evidence is execution-comparable, plan-confounded, condition-confounded, or mixed. Guidance asks for matched conditions or a one-variable repeat when attribution is unsafe; technique-focused debrief objectives continue to outrank equipment advice.
- **Every broad finding has an owner:** `docs/ENHANCEMENT_RESOLUTION_LEDGER.md` maps all 115 findings to existing deterministic authority, this comparison closure, or an explicit external gate. `docs/PLAYER_VALIDATION_PROTOCOL.md` defines cohorts, device/input/display coverage, six session scripts, thresholds, evidence records, and stop-ship failures for subjective and physical-hardware claims.
- **Complete release proof:** The parser/editor scan, all `103` discovered probes, and all six Academy/Circuit/Pine/Mesa/Freestyle/Discovery smokes pass. The fresh content-addressed Web candidate is `index.c8ce4d7b116a.pck` (`5,506,824` bytes; SHA-256 `c8ce4d7b116ab0ca5433d9a6086fc64b7d6178ea0e004595cc8f6edcae10fde7`) beside `index.9778df3b5d0e.wasm`; all `17` delivery/recovery tests pass.
- **Packaged browser proof:** The prescribed shared-action client produced `output/run-analysis-final-client/shot-0.png`, `shot-1.png`, and matching schema-12 states. Inspected 1280x720 frames retain the clean Quarry-first Garage and setup paging layout; HTTP length, immutable caching, COOP/COEP headers, manifest, and inner-page pack reference agree, with no console/page-error artifact.
- **Acceptance boundary:** The protocol makes the remaining work executable and auditable; it does not claim that representative people or devices were tested when they were not.

## Exact run-plan history and result-to-Workshop loop - 2026-08-08

- **Official results now explain the exact attempt:** Race staging freezes bike, kit, class, installed parts, physical tune, livery, assists, difficulty, transmission, control response, crash support, weather, surface, condition, and build signature into the authoritative result. The result survives JSON and settlement instead of relying on whichever Garage configuration happens to be active later.
- **Bounded durable comparison history:** Profile schema 12 retains the latest five official plans per event or rotating challenge plus the fastest valid classified personal-best plan and a player-pinned reference. Previous updates on every official result; PB changes only when the effective time improves. The history sanitizer bounds every nested field and persists through verified profile serialization.
- **Safe strategy restoration:** The Workshop adds Previous Run and Personal Best under `HISTORY`, shows position/time/medal/incidents/Flow plus the exact build, and applies an available plan transactionally. Missing bikes, kits, or parts are refused without partial mutation. Applying history changes strategy only—live condition and odometer are deliberately preserved. A new debrief routes the next Workshop visit directly to this comparison, while Results calls the existing retry `REMATCH LAST PLAN`.
- **Browser and accessibility parity:** Schema 13 publishes a compact Previous/PB/pinned projection through the same Garage authority. Representative race JSON remains within the existing 7,000-byte budget; the packaged fresh Garage reports the honest empty-history state until an official result exists.
- **Deterministic proof:** The new setup-history probe covers the five-run cap, latest-versus-fastest identity, exact plan/sector/Flow retention, JSON round trip, wear/odometer preservation, and atomic unavailable-part refusal. Garage coverage proves debrief routing, visible comparison, one-action apply, and preservation language. Full lifecycle coverage requires the staged plan in both the emitted result and JSON round trip.
- **Release candidate:** Parser/editor scan, all `81` curated probes, and all six Academy/Circuit/Pine/Mesa/Freestyle/Discovery smokes pass. All `17` delivery/recovery tests pass for `index.a1ea73be2860.pck` (`5,499,628` bytes; SHA-256 `a1ea73be2860579e442e612866d81aa320fdc14e342b5133910de22a5626a394`) beside `index.9778df3b5d0e.wasm`.
- **Packaged browser proof:** The prescribed shared-action client produced `output/setup-history-final-client/shot-0.png` and `shot-1.png`. Inspected 1280x720 frames retain clean Quarry-first setup comparison layout, schema 11 state matches the visible selection, and no console/page-error artifact was produced.
- **Acceptance boundary:** The code-backed history, comparison, restore, persistence, browser, and release findings are resolved. Representative complete-session players must still judge whether comparing Previous/PB plans improves learning and strategic experimentation; that subjective evidence cannot be manufactured by automation.

## Complete event-plan comparison and reversible application - 2026-08-08

- **Three complete builds, not three decorative ratings:** Each Garage metric row now shows Equipped, Viewing, and Event Plan bars. All three come from the authoritative active bike, installed parts, condition, kit, and tune runtime projection; the Event Plan column applies the recommended preset's real gearing, tire, suspension, preload, and braking values before comparison. The decision snapshot also exposes exact selected-versus-equipped and recommended-versus-equipped deltas plus a bounded explanation of kit, tune, build, and assist ownership.
- **One action with a recovery path:** When the bike does not need repair, the existing contextual repair action becomes `APPLY PLAN`. It equips an already-owned recommended kit and its tune together, refuses locked kits without spending cash, preserves the complete previous kit+tune, and makes the assist action a visible one-step `UNDO PLAN` until restoration. A tune failure rolls the kit back instead of leaving a partial configuration.
- **Clear visual and accessible authority:** The visible three-column bars, decision strip, event strategy snapshot, main Web publisher, and schema-10 browser state share the same data. Heavy comparison state is emitted only when present, keeping ordinary race state under the existing concise JSON ceiling.
- **Focused proof:** Parser/editor, Garage, Web-state, and track-evolution contracts pass. Coverage proves equivalent fresh-plan bars, Pine Trail+Enduro's positive grip/softer-suspension consequence, source ownership, locked-plan refusal, owned-plan kit+tune application, full undo, schema-10 parity, and a bounded `6,743`-byte representative race state.
- **Packaged layout regression closed:** The required shared-action browser loop exposed a reproducible stale Web label rect after rapid Balanced -> Trail -> Attack paging. Setup titles now reassert their fixed card rectangle whenever the shaped glyph run changes; the focused Garage probe covers the rect and the inspected packaged Attack frame stays centered without clipping.
- **Exact local package:** All `17` delivery/recovery tests pass for `index.90a2f0830635.pck` (`5,485,452` bytes; SHA-256 `90a2f0830635dcb6e90ab0d9a45cd0f8d4c51813e140084309e8fd31d0fea254`) beside `index.9778df3b5d0e.wasm`. The required client captures `output/three-way-plan-fixed-client/shot-0.png` through `shot-2.png`; schema 10 and the complete comparison state agree with the visible bars, and there is no console/page-error artifact.
- **Complete release gate:** The clean parser/editor scan, all `80` curated probes, and all six Academy/Circuit/Pine/Mesa/Freestyle/Discovery smokes pass. This revalidates `6,804` bike/track/surface/modifier/setup/build combinations, 47 bindings, save recovery, long-session bounds, three difficulty tiers, tactical opponent Flow pressure, geometry and route ownership, the rider-debrief learning loop, and the full race lifecycle.

## Equipped / viewed / recommended setup decision authority - 2026-08-08

- **One decision strip, three unambiguous roles:** The Garage now identifies the kit that will actually run, the card currently being viewed, and the event's recommended kit+tune in one concise line. A full match collapses to `EQUIPPED + EVENT PLAN`; alternates, tune mismatches, owned previews, and locked previews expose bounded `READY`, `ALTERNATE`, `TUNE_NEEDED`, or `PREVIEW` states plus the next honest action without mutating the profile.
- **Accessible and testable authority:** The same snapshot feeds the visible `SetupDecision` label, event-strategy contract, main Web publisher, and schema-9 browser/assistive projection. Focused Garage coverage proves the fresh Balanced plan and Pine's equipped-Balanced versus recommended-Trail/Enduro distinction; the Web-state probe proves role, state, price shortfall, and label parity.
- **Complete regression and exact package:** The parser/editor scan, all `80` curated probes, and all six Academy/Circuit/Pine/Mesa/Freestyle/Discovery smokes pass. The gate revalidates `6,804` bike/track/surface/modifier/setup/build combinations, three difficulty tiers, tactical AI Flow pressure, 47 bindings, accessibility persistence, long-session bounds, save recovery, and complete race lifecycle. All `17` delivery/recovery tests pass for `index.25a7327773f2.pck` (`5,479,596` bytes; SHA-256 `25a7327773f233f8c15822dda86b029c1bace5d3fb074697187e09ac9f7b0f6f`) beside `index.9778df3b5d0e.wasm`.
- **Packaged browser proof:** The prescribed `develop-web-game` client used the shared `action_payloads.json` and captured `output/setup-decision-final-client/shot-0.png` through `shot-2.png`. The inspected 1280x720 frames show the collapsed fresh-plan state and the distinct equipped-Balanced/viewing-Attack/event-Balanced state with exact runtime deltas, no clipping, schema-9 visual/state parity, and no console/page-error artifact. Port `8777` serves the matching V28 build with exact length, immutable pack caching, COOP `same-origin`, and COEP `require-corp`.
- **Acceptance boundary:** This closes the repository gap behind side-by-side setup choice clarity. Representative riders must still judge whether the wording changes real choices and whether all three kits remain attractive over complete events; that is player evidence, not a condition code can manufacture.

## Authoritative setup comparison and Garage decision hierarchy - 2026-08-08

- **Consequences are explicit before purchase or launch:** Trail, Balanced, and Attack now expose exact drive, grip, speed, suspension, preload, and braking deltas against one stable Balanced reference. The figures come from the same active-bike, condition, build, and tune runtime projection used by the Garage and live bike rather than a disconnected ratings table. The visible card, event-strategy snapshot, main Web publisher, and schema-8 browser/assistive projection share that authority.
- **Honest kit language:** Trail and Attack card descriptions no longer attribute suspension or jump behavior to the kit when those physical changes come from the separately selected Enduro or Rhythm tune. Trail states its grip-for-speed exchange; Attack states its drive/speed-for-grip exchange. The existing event plans still pair those kits with the physical tunes that supply compliance, damping, and preload.
- **Lower Garage information load:** The right rail is now `BIKE & NEXT GOALS`, uses larger copy, and prioritizes the active machine plus the next route/unlock decision. First-run presentation keeps Quarry Event 01, the two-Quarry unlock path, and optional Academy in one hierarchy; progressed profiles retain Tour, Weekend, Academy, and Milestone summaries without four large blank-separated blocks.
- **The coaching loop now closes on the next race:** Every debrief retains a bounded evidence snapshot. The following official race evaluates the previous objective as `CLEARED`, `PROGRESS`, or `ACTIVE` for classification, crash control, recovery, track discipline, clean passing, sector pace, consistency, Flow/Surge use, racecraft, or clean pace. Results explicitly celebrate a safer or more consistent ride even when finishing position is worse, then retain one measurable next objective. The receipt remains session-local and Academy/Test Ride cannot overwrite competitive coaching.
- **Production proof:** The parser/editor scan, all `80` curated probes, and Academy, Circuit, Pine Enduro, Mesa MX, Freestyle, and Discovery smokes pass. Coverage checks exact Trail (`drive -15% / grip +13% / speed -8%`) and Attack (`drive +8% / grip -10% / speed +10%`) projections, stable Balanced identity, visible-label authority, browser parity, unchanged Garage/Workshop actions, zero-crash success despite a worse finish, partial crash progress, and Flow-plus-Surge objective completion. All `17` delivery/recovery tests pass for `index.b350d7d554ea.pck` (`5,476,908` bytes; SHA-256 `b350d7d554ea55233df7581afee8e4e0a325268e0c92c11ac1564d0f15bb1a47`) beside `index.9778df3b5d0e.wasm`.
- **Packaged visual proof:** The prescribed `develop-web-game` client used the shared `action_payloads.json` and captured `output/enhancement-program-final-client/shot-0.png` through `shot-2.png`. The inspected 1280x720 frames open on Quarry Trail `EVENT 01 / 19`, keep the setup card and larger `BIKE & NEXT GOALS` rail readable without clipping, expose the schema-8 setup/debrief contracts, and produce no console/page-error artifact. Port `8777` serves the matching V28 build with exact pack length, immutable caching, COOP `same-origin`, and COEP `require-corp`.

## Authoritative rider debrief and next-run learning loop - 2026-08-08

- **Race evidence now becomes one actionable lesson:** Every official race result includes a deterministic `RiderDebrief` built from classification, rival-relative sector splits, lap spread, penalties, passes, contacts, crashes, resets, course integrity, and player-owned Flow technique counters. It assigns a bounded grade, preserves a concrete strength, selects exactly one highest-value focus, and sets a measurable next-run objective instead of presenting raw statistics without direction.
- **Flow and boost are part of the learning loop:** Results now retain player Flow uses and Surge activations. A trailing rider who banks no Flow receives an explicit `FLOW_USAGE` objective; crash, recovery, track-discipline, contact, sector, consistency, racecraft, and pure-pace needs take fair priority when their evidence is stronger. AI Flow/boost behavior remains independently authoritative and continues to scale across Relaxed, Standard, and Expert.
- **One authority across Results, accessibility, and Garage:** The official Results card presents the same three-line debrief exposed through schema-8 browser/assistive state. Returning to the Garage carries the objective into the setup/event decision band as `RIDER FOCUS`, paired with the event's recommended kit and tune. Academy retains its authored grading card and Test Ride remains progression-neutral.
- **Deterministic coverage:** The new focused probe verifies repeatability, exact best/costliest sector selection, crash priority, unused-Flow coaching, invalid-run classification guidance, score bounds, Results layout fit, and Garage carry-forward. Full race lifecycle/JSON coverage now requires racecraft metrics and debrief serialization. The parser/editor scan, all `80` curated probes, and Academy, Circuit, Pine Enduro, Mesa MX, Freestyle, and Discovery smokes pass.
- **Exact Web candidate:** All `17` delivery/recovery tests pass. The immutable local pack is `index.2730f2e047f1.pck` (`5,469,404` bytes; SHA-256 `2730f2e047f1aba85b2c2aec5bf790e481cfcb3cd9cc1c1e343ddb5bb7daa096`) beside `index.9778df3b5d0e.wasm`. Port `8777` serves the matching V28 build with immutable pack caching, COOP `same-origin`, and COEP `require-corp`.
- **Required packaged browser proof:** The prescribed `develop-web-game` client used the shared `action_payloads.json` and captured `output/rider-debrief-release-client-final/shot-0.png` through `shot-2.png`. The inspected 1280x720 frame is a clean Quarry-first Garage at `EVENT 01 / 19`; schema 8 exposes the bounded empty pre-race debrief contract, and no console/page-error artifact was produced. This candidate is local and uncommitted; representative complete-session play remains the acceptance authority for subjective fun, coaching tone, and objective usefulness.

## Bounded long-session ghost capture - 2026-07-27

- **Long races no longer grow an unbounded ghost:** Personal-best capture keeps its initial 10 Hz fidelity until it reaches `7,200` frames, then deterministically compacts older samples and doubles the future interval. This preserves the complete run instead of truncating a long race. Finished and cancelled attempts immediately release their working buffer; the saved best remains capped.
- **Hostile and legacy saves are handled safely:** Desktop, legacy Variant, and browser ghost loads now share one sanitizer. It rejects non-finite transforms, invalid quaternions, non-monotonic time, and payloads above `36,000` source frames, then endpoint-preserving downsamples valid long inputs to the live `7,200`-frame budget. Valid legacy time-only records remain usable without manufacturing playback frames.
- **Deterministic one-hour proof:** The new quality-gate soak simulates `3,600` seconds at 10 Hz. It finishes with `3,769` frames after three compactions, retains `3,599.3` seconds of coverage, releases finished/cancelled attempt buffers, rejects oversized and malformed imports, and preserves both endpoints when bounding a valid 10,000-frame import.
- **Compact production observability:** Race session and schema-7 browser state expose active/best frame counts, the maximum, effective interval, compaction count, retained spans, and a bounded flag without exposing transform payloads. The required packaged client reports `maximum_frames=7200`, `bounded=true`, Quarry Trail Event 01/19, and no console/page-error artifact.
- **Regression and exact Web candidate:** The parser scan, all `79` curated probes, all six Academy/Circuit/Pine/Mesa/Freestyle/Discovery smokes, save recovery, ghost eligibility, replay, full race, and Web-state contracts pass. The Mesa pace smoke retains its elite `+2.93 m/s` opponent advantage under a `0.05 m/s` fixed-step epsilon; gameplay pace is unchanged. All `17` delivery/recovery tests pass for `index.fcedba016124.pck` (`5,456,392` bytes; SHA-256 `fcedba016124e4682cca0bf7b56867dd84d008109d566db676545759eca5c666`) beside `index.9778df3b5d0e.wasm`.
- **Release status:** This accumulated candidate is authorized for source commit, branch push, and GitHub Pages republication. Representative long-session play should still judge sustained pacing, concentration load, reward cadence, and subjective fun.

## Crash-accessibility and long-session GitHub Pages publication - 2026-08-03

- **Source release:** Commit `375bc09` publishes player-selectable Standard/Assisted crash support, run-locked competitive separation, adaptive bounded ghost capture, hostile ghost-save validation, schema-7 runtime evidence, deterministic probes, and the exact verified Web bundle on `codex/racecraft-v27`.
- **Pages deployment:** GitHub Pages commit `0948dcbe94d98364ac4cd8cb20ac4688a0e60482` completed workflow run `30869927250` successfully from `gh-pages` with HTTPS enforcement at `https://willtran87.github.io/project-riding-dirty/`. Its tree exactly matches the source commit's `web/` tree and excludes ignored Godot export scratch files.
- **Public payload proof:** The public manifest and inner game page reference `index.fcedba016124.pck`. An independent public download is exactly `5,456,392` bytes with SHA-256 `fcedba016124e4682cca0bf7b56867dd84d008109d566db676545759eca5c666`; the former `index.6e651106bec7.pck` returns 404.
- **Public browser proof:** The required shared-action client reaches Quarry Trail Event 01/19 at `output/github-pages-long-session-release-client/shot-0.png`. Schema 7 reports Standard crash support plus `ghost_runtime.maximum_frames=7200` and `bounded=true`; no console or page-error artifact was produced.
- **Release gates:** The published candidate passed the parser scan, all `79` curated probes, all six representative activity smokes, the deterministic one-hour soak, all `17` Web delivery/recovery tests, exact public payload verification, and public production-browser startup.

## Session-local evolving track lines - 2026-07-26

- **Race traffic now changes the line:** The player and every running opponent write deterministic wear into five route-relative lanes across 12-meter bins. A rider can contribute only once per bin, keeping deformation independent of frame rate; surface erodibility and bounded adjacent-lane transfer make repeated lines develop naturally without rewriting authoritative collision.
- **Readable risk and reward:** One batched, translucent `ImmediateMesh` darkens forming grooves without adding collision objects. Dry compacted lines provide a bounded grip/drive benefit, while rain and mud turn established grooves into slick or deep-rut risks. The bike receives the same bounded context through its existing rear-drive and lateral-grip paths, and the Racecraft strip reports `LINE FORMING`, `COMPACTED LINE +GRIP`, `SLICK GROOVE`, `DEEP RUT`, or `DEEP WET RUT`.
- **Fair lifecycle and activity isolation:** Evolution resets on every run, follows the authoritative route and active surface/weather policy, and remains disabled in Academy and Bike Test Ride so tutorials and setup comparisons stay repeatable. Browser text state schema `6` publishes only a compact aggregate and the player's current line.
- **Deterministic production proof:** The new focused probe verifies pass de-duplication, lane isolation, wear caps, dry benefit, wet risk, visual batching, zero collision objects, restart cleanup, activity isolation, source integration, and Web projection. The complete parser/editor scan, all `97` focused probes, and Academy, Circuit, Pine Enduro, Mesa MX, Freestyle, and Discovery smokes pass. Live smokes form `33–40` visible grooves on each race track; Academy remains at zero.
- **Exact local Web candidate:** The deterministic export and all `17` delivery/recovery tests pass. The immutable pack is `index.37a1f08ab31a.pck` (`5,441,164` bytes; SHA-256 `37a1f08ab31a86b2421e00c988a20f9dacb3de15e7cbc29efcbd84c50204c611`) beside the unchanged `index.9778df3b5d0e.wasm`.
- **Packaged browser proof and release status:** The required shared-action client opens the clean Quarry Trail `EVENT 01 / 19` Garage at `output/track-evolution-final-required-client/shot-0.png` with schema `6` and no error artifact. Direct exact-package play reaches the green-flag twelve-rider Circuit with `142` sampled passes, `40` visible grooves, `68.6%` maximum wear, a `FORMING` player line, eight visible opponents, and empty console/page-error arrays; `output/track-evolution-packaged-live-final.png` visibly shows the established lines down the start straight. This candidate remains uncommitted, unpushed, and unpublished because this automatic continuation received no fresh release instruction. Representative riders should still judge groove legibility, strategy value, and long-race balance across weather, surfaces, cameras, and display classes.

## Event-correct trackside sponsor identity - 2026-07-26

- **Sponsor relationships now exist in the event space:** Dustline Works, Wildbrush Outpost, and Sundown Static receive their authored identity, accent, and relationship language on physical trackside landmarks. Ordinary route events place one high, collision-free downcourse arch plus a finish board; Freestyle and Discovery receive two bounded open-area boards near their shared staging zone.
- **Track authority and onboarding stay honest:** The presenter is owned outside the streamed `LevelRoot`, so the route keeps exactly one authoritative streamed track child. Every landmark has zero collision objects and remains clear of the riding envelope. Academy and Bike Test Ride explicitly clear the commercial treatment instead of inheriting stale sponsor state.
- **Readable, observable presentation:** A visual iteration moved the opening identity away from the minimap and raised it into an overhead route treatment. Web text state schema `5` exposes a bounded sponsor projection with activity, relationship identity, accent, landmark positions, and collision count, allowing the packaged browser to prove what it renders without turning state output into gameplay.
- **Regression discovered and closed:** The first complete gate caught that an early presenter placement violated route ownership by increasing `LevelRoot` to two children. Moving the presenter to Main restored `active_children=1` across Quarry, Pine, Mesa, and reverse route checks. The clean rerun passes the parser/editor scan, all `96` focused probes, and Academy, Circuit, Pine Enduro, Mesa MX, Freestyle, and Discovery smokes in `695.2` seconds.
- **Exact local Web candidate:** The deterministic export and all `17` delivery/recovery tests pass. The local immutable pack is `index.c2af9dba512c.pck` (`5,424,948` bytes; SHA-256 `c2af9dba512c00b4f6662f1127addae364234b8b6448b4a3e2266b5d64862b23`) beside the unchanged `index.9778df3b5d0e.wasm`.
- **Packaged browser proof and release status:** The required shared-action client opens the clean Quarry `EVENT 01 / 19` Garage at `output/sponsor-trackside-final-required-client/shot-0.png`. A direct exact-package launch reaches the real twelve-rider race with schema `5`, Dustline Works / Race Precision, and two visible zero-collision landmarks at `output/sponsor-trackside-final-live-client/shot-0.png`; neither run produced a console/page-error artifact. This candidate remains uncommitted, unpushed, and unpublished because this automatic goal continuation received no fresh release instruction. Representative players should still judge sponsor legibility at race speed and memorability across every event.

## Interactive terrain-reading Academy lesson and production release - 2026-07-26

- **Terrain literacy is now taught through play:** The Academy expands from nine to ten lessons with `Surface Reading and Grip` immediately after Control Basics. One authored lap projects four deterministic grip sectors at checkpoints `0 / 2 / 5 / 8`: hardpack, sand, grass, and mud. The lesson previews the next surface, explains the relevant throttle/steering approach, tracks each visited sector, and grades sustained safe-control adaptation instead of mere exposure.
- **The lesson uses the real bike, not a tutorial simulation:** Academy sectors override the live wheel-contact surface, so the existing friction, drag, roughness, roost, equipment-wear, and contact-audio systems all change together. Mud also reaches the wet-weather path. Each sector transition receives a visible grip callout, current/next coaching, live objective counters, and an immediate positive `SURFACE ADAPTED` receipt after the correct control pattern is held for `0.45` seconds. Reset/session cleanup removes the override without contaminating ordinary races.
- **Progression and presentation remain coherent:** Control Basics now advances to Surface Reading before Gate Drop. The payoff authority reports the new lesson among the exact first-pass access delta; Profile persistence, deterministic grading, rematches, Garage projection, Results continuation, and first-run onboarding use the existing lesson ID path. All ten lessons remain actionable and unclipped across keyboard, gamepad, touch, and `175%` text scale.
- **Focused and full production proof:** The dedicated surface probe verifies the exact four-sector schedule, checkpoint projection, current/next forecast, safe and unsafe control patterns, real physical bike override, cleanup, and bronze-through-gold grading. The complete parser/editor scan, all `95` focused probes, and Academy, Circuit, Pine Enduro, Mesa MX, Freestyle, and Discovery smokes pass in `737 seconds`. The opponent matrix independently revalidates `16` simulations across all three tracks and difficulty tiers, including strong/elite Flow pressure, `51` tactical boosts, starter traffic, upgraded builds, defense, and replay identity.
- **Exact Web candidate:** The deterministic export and all `17` delivery/recovery tests pass. The immutable pack is `index.260068df35da.pck` (`5,413,952` bytes; SHA-256 `260068df35da0499e7e8c6477fd31da994eec7c2174ba663bf3c809c141f7272`) beside the unchanged `index.9778df3b5d0e.wasm`; manifest, disk, inner-page reference, and live `127.0.0.1:8777` payload agree exactly.
- **Required packaged browser proof:** The prescribed `develop-web-game` client using the shared `action_payloads.json` captured `output/academy-surface-release-required-client/shot-0.png`. The inspected frame is clean and readable, opens on Quarry Trail `EVENT 01 / 19`, reports schema `4`, a full-condition starter Tyke, and no console/page-error artifact. This exact accumulated release is now authorized for source commit, branch push, and GitHub Pages republication.
- **Production publication:** Source commit `a25d7cf` is pushed on `codex/racecraft-v27`, and Pages commit `b0f6f22` completed GitHub Pages build `1116675868`. The public manifest reports `0.3.8-racecraft-v28`; the downloaded public `index.260068df35da.pck` is exactly `5,413,952` bytes with SHA-256 `260068df35da0499e7e8c6477fd31da994eec7c2174ba663bf3c809c141f7272`, and the public game HTML references that immutable pack.
- **Public browser acceptance:** The required client launched `https://willtran87.github.io/project-riding-dirty/`, captured `output/academy-surface-public-required-client/shot-0.png`, and produced no console/page-error artifact. The inspected live frame is the clean Quarry Trail Garage at `EVENT 01 / 19`; schema `4` reports a valid first-event race, full-condition starter bike, and saved Garage state.

## Integrity warning audio parity - 2026-07-26

- **Authoritative warning transitions now speak:** GameplayAudio listens to the real RaceController integrity snapshot instead of inferring course state. Wrong Way, Off Course, Course Cut, Stuck, and penalized Reset enter through the established Commentary bus with exact corrective language; returning to a clear course produces one positive receipt.
- **Bounded and visually equivalent:** Identical per-frame snapshots are de-duplicated before audio, warning changes still respect the commentary anti-spam window, and only an accepted cue requests the important semantic caption. Start, final-lap, and finish signals retain their existing sounds, avoiding duplicate flag callouts.
- **Deterministic production proof:** The exact source parser/editor scan, all `92` focused probes, and Academy, Circuit, Pine Enduro, Mesa MX, Freestyle, and Discovery activity smokes pass in `747.1 seconds`. Coverage proves one exact Wrong Way cue/caption, duplicate suppression, Course Clear recovery, real signal binding, and unchanged lifecycle flags.
- **Fresh exact Web candidate:** The deterministic `0.3.8-racecraft-v28` export and all `17` delivery/recovery tests pass. The final pack is `index.2f2800832ec5.pck` (`5,342,388` bytes; SHA-256 `2f2800832ec5d099bacc0f05e9673a5c65b2c6c7d5c06226844fab07fb296e5c`) beside the unchanged `index.9778df3b5d0e.wasm`; live HTTP returns the same immutable payload length.
- **Packaged browser proof:** The required client reaches the clean Start wrapper at `output/integrity-audio-final-required-client/shot-0.png` without an error artifact. Direct exact-package play reaches schema `4` and the real 12-rider Quarry staging grid; `output/integrity-audio-final-live.png` shows the default Important caption equivalent for race control with empty console and page-error arrays. The milestone remains uncommitted, unpushed, and unpublished because this automatic continuation received no fresh release instruction; representative listening should still tune correction urgency, fatigue, and clarity while intentionally entering and recovering from every integrity state.

## Semantic audio captions and cue accessibility - 2026-07-26

- **One bounded semantic caption feed:** Accepted commentary, countdown, gate, route-discovery, denied-action, landing-warning, boost-denial, and rider-voice cues can now produce a consistent two-line visual equivalent. Continuous engine, tyre, surface, ambience, and music layers remain deliberately outside the feed so captions communicate actionable meaning rather than becoming an unreadable waveform transcript.
- **Independent accessibility controls:** The Access page now offers `OFF / IMPORTANT / ALL` cue detail, `75%–175%` caption size independent of global text/HUD scale, and a Standard/High Contrast background. Global high contrast and HUD safe-area preferences compose with the feed; Reduced Motion keeps captions readable without fade animation.
- **Safe behavior and observability:** Settings schema `15` migrates existing riders to conservative Important captions, validates every mode and bound, and persists through the existing verified primary/backup path. Accepted cues share audio anti-spam decisions, repeated messages fold into a bounded `xN` receipt, priority preemption uses a three-item cap, and Web text schema `4` exposes only the current semantic caption and presentation state. Routine settings-navigation tones remain uncaptioned because the selected row and save receipt are already exact visual equivalents; a browser inspection caught and closed the resulting control-occlusion risk before the final build.
- **Deterministic production proof:** The exact post-fix parser/editor scan, all `92` focused probes, and Academy, Circuit, Pine Enduro, Mesa MX, Freestyle, and Discovery activity smokes pass in `773.3 seconds`. The dedicated probe covers schema-14 migration, tamper rejection, ordered Access controls, important-only filtering, duplicate folding, all-cue mode, 150% sizing, high contrast, viewport containment, non-occluding routine navigation, real GameplayAudio commentary/countdown/denial emission, persistence, and Off behavior.
- **Fresh exact Web candidate:** The deterministic `0.3.8-racecraft-v28` export and all `17` delivery/recovery tests pass. The final pack is `index.8affa65409bb.pck` (`5,341,444` bytes; SHA-256 `8affa65409bb85b6ac417c3558a7782e65a828b05861fb16e0ebfd294e6de195`) beside the unchanged `index.9778df3b5d0e.wasm`; manifest, disk, and live HTTP hashes/lengths agree, and HTTP retains immutable caching plus `require-corp` / `same-origin` isolation.
- **Packaged browser and persistence proof:** The required client reaches the clean wrapper Start screen at `output/audio-caption-final-required-client/shot-0.png` without an error artifact. Direct exact-package inspection reaches schema `4`; `output/audio-caption-final-access-applied.png` shows the unobscured All / 150% / High Contrast controls, `output/audio-caption-final-live-cue.png` shows the Race Control countdown equivalent, and `output/audio-caption-final-live-race.png` shows important holeshot commentary during the real 12-rider race. A full page reload restores every preference, with empty console and page-error arrays. This milestone remains uncommitted, unpushed, and unpublished because this automatic continuation received no fresh release instruction; representative riders should still judge caption wording, duration, and placement across every event, language-ready text length, compact display, and visual load.

## Runtime graphics controls and weather scaling - 2026-07-26

- **A dedicated Graphics page:** Settings now has a seventh, purpose-built page with a preset plus independent `AUTO` overrides for 3D render resolution (`67%` through `100%`), directional shadows (`OFF`, `SHORT`, or `FULL`), effect density (`LOW`, `MEDIUM`, or `FULL`), and weather effects (`MINIMAL`, `REDUCED`, or `FULL`). Visual Quality no longer hides among camera preferences, while the existing Reduced Particles accessibility option remains discoverable under Access.
- **Real immediate runtime behavior:** Presets remain the safe baseline, but every override applies immediately to the live viewport or streamed scene. Resolution changes only the 3D render scale so HUD text stays native-resolution; shadows retain authored values or use bounded short/Web distances; generic and weather particle budgets resolve independently. Reduced Particles remains a hard cap even if graphics density is Full, so performance customization cannot silently defeat an accessibility preference.
- **Safe migration and truthful observability:** Settings schema `14` migrates every older rider to `AUTO`, preserving the exact prior preset-driven presentation. Invalid graphics values are rejected by the existing sanitization and atomic primary/backup save path. Web state schema `3` exposes the applied preset, effective render scale, shadow/effect/weather selections and ratios, Web cap, and accessibility cap rather than reporting menu intent alone.
- **Deterministic production proof:** The parser/editor scan, all `91` focused probes, and Academy, Circuit, Pine Enduro, Mesa MX, Freestyle, and Discovery activity smokes pass on the final source in `705.6 seconds`. Coverage includes all seven pages, 47 bindings, legacy migration, exact Graphics-page order, mouse/keyboard/gamepad selection, viewport application, authored shadow restoration, independent weather density, Web caps, accessibility dominance, persistence, and compact JSON projection.
- **Browser inspection closed an observability gap:** The first exact-package screenshot showed a visibly open Graphics modal while text state still said Garage. Modal resolution now has explicit precedence over its background, with a permanent contract. The clean rerun and second export prove the correction without making the projection feed gameplay.
- **Fresh exact Web candidate:** The deterministic `0.3.8-racecraft-v28` export and all `17` delivery/recovery tests pass. The final pack is `index.0027d97032a9.pck` (`5,330,704` bytes; SHA-256 `0027d97032a983b4a482785ffdd0da7782186f1fb77c4073a6ce955c2f1e73d1`) beside the unchanged `index.9778df3b5d0e.wasm`; manifest, disk, and HTTP hashes match, and HTTP remains immutable with the expected build and cross-origin-isolation headers.
- **Packaged visual and persistence proof:** The required client reaches the clean first-event Garage at `output/graphics-settings-final-client/shot-0.png`, reports schema `3`, and produces no error artifact. Direct exact-package interaction applies `100% / SHORT / MEDIUM / MINIMAL`; `output/graphics-settings-final-applied.png` shows all five Graphics rows plus the save receipt, and `output/graphics-settings-final-live.png` carries the same effective `1.0 / 0.55 / 0.10` contract into the real 12-rider Quarry countdown. A complete page reload retains every selection, with empty console/page errors. This milestone remains uncommitted, unpushed, and unpublished because the automatic goal continuation received no fresh release instruction; representative riders should still judge each preset/override combination on lower-end devices, compact screens, and demanding weather.

## Team graphics, decals, and sponsor placement - 2026-07-26

- **A real Graphics Workshop:** A dedicated ten-tab Workshop category now offers six bounded team palettes, five authored decal families, four sponsor identities including Privateer, and four sponsor-placement layouts. Every item explains where it appears, confirms immediate application, states `PERFORMANCE EFFECT NONE`, participates in existing keyboard/gamepad/touch-semantic feedback, and keeps the expanded tab strip contained at `1280x720`.
- **Visible bike and rider graphics:** Team palettes recolor the bike body and jersey without replacing the selected number plate. Speed Stripes, Gate Checkers, Topo Lines, and Lightning Cut build distinct procedural meshes on both shrouds. Dustline, Wildbrush, and Sundown identities create authored marks on the two shrouds, two fenders, rider chest, and rider back; Bike Shrouds, Bike Fenders, Rider Kit, and Full Race Kit show exactly the selected surfaces. Graphics receive the same bounded dirt, mud, wetness, and crash-wear treatment as the surrounding kit and remain player-only so the 11-opponent Web render budget is unchanged.
- **Safe, durable identity:** Profile schema `10` strictly bounds `team_palette`, `decal_id`, `sponsor_id`, and `sponsor_placement`. Schema-9 riders migrate conservatively to their prior Style/Clean/Privateer appearance, fabricated IDs are rejected at the profile trust boundary, and atomic cosmetic transactions carry all four fields through primary/backup Web saves and every saved Outfit slot without copying or changing bike tune, parts, condition, performance, scoring, rewards, or competitive eligibility.
- **Results and browser continuity:** The official podium uses the chosen team palette for the player silhouette and exposes the complete graphics identity alongside the saved number/body/kit/livery. Web state schema `2` reports the four fields in live player state and the podium projection, while Garage/Workshop state names the current semantic item and apply receipt.
- **Deterministic validation:** The permanent graphics probe proves the 19 bounded choices, schema defaults, tamper rejection, schema-9 migration, Workshop navigation and feedback, performance isolation, Outfit save/restore, exact palette color, six Lightning meshes, six Full Kit sponsor marks, two Rider Kit marks, Privateer removal, podium identity, and browser projection. The parser/editor scan, all `91` focused probes, and Academy, Circuit, Pine Enduro, Mesa MX, Freestyle, and Discovery activity smokes passed in `737.6 seconds`; the first responsive-gate run caught a redundant third Results line, which was removed before the clean full rerun.
- **Fresh exact Web candidate:** The deterministic `0.3.8-racecraft-v28` export and all `17` delivery/recovery tests pass. The local pack is `index.c6af0e3ecf88.pck` (`5,326,928` bytes; SHA-256 `c6af0e3ecf8852c04b48fcbaecb6f2ad4994df800d146a88fa5207b7e335b285`) beside the unchanged `index.9778df3b5d0e.wasm`.
- **Packaged visual proof and release status:** The required final-package client reaches the clean Quarry Event `01 / 19` Garage at `output/graphics-final-required-client/shot-0.png`, reports schema `2` with all four conservative defaults, and produces no error artifact. Direct final-package input applies `NIGHTSHIFT / LIGHTNING / DUSTLINE / FULL_KIT`; `output/graphics-final-workshop.png` shows the contained semantic receipt and `output/graphics-final-race.png` shows the cyan player bike plus the readable `DL` back mark inside the real 12-rider gate. The Workshop audit reported no console or page errors. This milestone and accumulated work remain uncommitted, unpushed, and unpublished because this automatic goal continuation received no fresh release instruction; representative riders should still judge palette/decal legibility in every camera, weather state, surface treatment, and display class.

## Official podium ceremony and event recap - 2026-07-26

- **Results now celebrate the race:** Every classified competitive race presents an animated, data-driven top-three ceremony before the complete official classification. The podium uses the authoritative P1/P2/P3 order and rider names/numbers, highlights the player when applicable, and gives the winner a restrained spotlight and idle motion without replacing exact timing, penalties, rewards, standings, or replay controls.
- **Player identity reaches the ceremony:** The player podium silhouette and number plate use the saved rider number, accent, body type, jersey, and livery. The ceremony remains cosmetic and never changes result authority, physics, scoring, progression, or competitive eligibility. Off-podium finishes still see the real top three and their own selected classification row.
- **A coherent event recap:** A new two-line recap acknowledges the active sponsor relationship and team rank, the saved rider number and kit, the nearest meaningful race rival and time delta, and one result-derived signature moment such as a lights-to-flag win, podium, clean ride, overtakes, or resilient finish. It reuses authoritative result telemetry rather than inventing story outcomes.
- **Responsive and accessible presentation:** The ceremony has full and compact layouts, uses a progressive frame-rate-independent reveal, and disables animation immediately under Reduced Flashes/Reduced Motion. Touch and short displays select the compact stage. Large text above `135%` yields the redundant local-board block and tightens internal margins while retaining the podium, recap, exact career payoff, complete scrollable classification, and semantic actions; the maximum `175%` payoff card passes keyboard, gamepad, and touch containment.
- **Academy and replay remain correctly scoped:** Academy completions retain their authored objective/medal grading card with no competitive podium language. Starting a replay hides the official card and restores the same result afterward; race reset clears ceremony ownership so replay teardown cannot resurrect stale results.
- **Truthful browser observability:** Web state schema `2` exposes the visible Results title/summary/recap plus podium event, player position, on-podium status, compact/reduced-motion flags, saved number/body/kit/livery, and bounded top-three entries. Hidden Garage and live-race states report an empty, non-visible ceremony.
- **Deterministic and production validation:** The permanent podium probe proves authoritative ordering, player customization, team/rival/moment recap, progressive animation, reduced-motion behavior, compact touch layout, desktop containment, and Academy scoping. The existing Web-state, progression-payoff, and results-navigation probes cover schema projection, `175%` text, payoff density, P12 reveal, scrolling, input prompts, replay lifecycle, and race reset. The final parser/editor scan, all `90` focused probes, and Academy, Circuit, Pine Enduro, Mesa MX, Freestyle, and Discovery activity smokes passed in `730.4 seconds`.
- **Fresh exact Web candidate:** The deterministic `0.3.8-racecraft-v28` export and all `17` delivery/recovery tests pass. The local pack is `index.137aecc59746.pck` (`5,301,636` bytes; SHA-256 `137aecc597465da7d80be089167fa0b94ea8071b854541ca8ce1d90a8f5e82e8`) beside the unchanged `index.9778df3b5d0e.wasm`.
- **Packaged browser proof and release status:** The required client reaches the clean Quarry Event 1 Garage at `output/podium-packaged-client/shot-0.png` with schema `2` and no error artifact. Direct exact-package play starts the first race, drives all `18` gates through real physics/recovery, and reaches `mode=RESULTS`; `output/podium-packaged-late-race.png` visibly shows the official P1-P3 ceremony, Dustline team/rival/moment recap, exact payoff and unlocks, scroll-selected P12 row, and rematch/replay/Garage actions. Browser console and page errors are empty. The milestone and accumulated work remain uncommitted, unpushed, and unpublished because this automatic goal continuation received no fresh release instruction; representative riders should still judge ceremony scale, pacing, and rivalry language across all event types and customization palettes.

## Inclusive rider identity - 2026-07-26

- **Identity beyond equipment:** Rider profiles now include independently selectable body type, skin tone, and nonverbal voice. Compact, Athletic, and Powerful silhouettes; five inclusive skin tones; and Focused, Bright, and Grounded voice registers are all cosmetic-only. They do not change mass, collision, bike setup, grip, condition, scoring, rewards, progression, or competitive signatures.
- **Safe, durable profile ownership:** Profile schema `9` strictly bounds all three identity fields, rejects fabricated values, and migrates schema-8 riders conservatively to Athletic, Medium, and Focused. Identity changes use the existing atomic profile transaction, persist through browser primary/backup storage, and are included in all three rider outfit slots without copying bike performance or career state.
- **A complete Rider Workshop:** The new Rider category sits beside Style and Number and presents eleven semantic choices with explicit descriptions, current body/skin/voice context, `PERFORMANCE EFFECT NONE`, immediate visual application, and an audible voice-preview receipt. Keyboard, gamepad, touch-semantic navigation, save feedback, and browser-readable Workshop state reuse the existing production interaction path.
- **Visible articulated silhouettes and skin:** Body type scales the existing torso rig and its shoulder/hip limb anchors together, preserving hand and foot contact while producing visibly distinct compact and powerful silhouettes. Skin tone now owns a real material on the neck and helmet opening, responds to the same dust, mud, rain, and crash-wear presentation as the rest of the rider, and remains compatible with every helmet, goggle, armor, accessory, trick, and riding pose.
- **Distinct, bounded rider voices:** Focused, Bright, and Grounded use one pooled procedural nonverbal exertion sample with distinct authored pitch registers on the Commentary mixer. Workshop previews are immediate; boosts, hard landings, clean tricks, and finishes use the saved voice with a `1.15-second` anti-spam cooldown, preserving engine and surface-audio priority during busy races.
- **Truthful browser observability:** `render_game_to_text()` exposes `body_type`, `skin_tone`, and `voice` beside the player's full equipment identity. The exact browser package starts at `ATHLETIC / MEDIUM / FOCUSED`, applies `POWERFUL / DEEP / GROUNDED`, reports the semantic voice-preview receipt, retains all three fields after a full page reload, and carries them into live race state.
- **Deterministic and production validation:** The permanent identity probe proves schema defaults, tamper controls, legacy migration, Workshop selection, distinct audio profiles, preview and riding reactions, cooldown behavior, outfit save/restore, performance isolation, visible skin geometry, and a `1.11x` Powerful articulated silhouette. The parser/editor scan, all `68` curated probes, and Academy, Circuit, Pine Enduro, Mesa MX, Freestyle, and Discovery activity smokes passed in `491.9 seconds`; the wear, outfit, audio, Web-state, animation timestep, full-race, and opponent-pressure regressions remain green.
- **Fresh exact Web candidate:** The deterministic `0.3.8-racecraft-v28` export and all `17` delivery/recovery tests pass. The local pack is `index.c3ca83f346aa.pck` (`5,287,660` bytes; SHA-256 `c3ca83f346aa8a90c6aa0d37804ee74234ee41113606f1fedc189ee421e25c22`) beside the unchanged `index.9778df3b5d0e.wasm`.
- **Packaged visual proof and release status:** The required client reaches the clean first-event Garage at `output/rider-identity-final-client/shot-0.png` with complete default identity state and no error artifact. Direct exact-package proof shows the Rider tab at `output/rider-identity-workshop-default-1280x720.png`, the applied Grounded identity and voice receipt at `output/rider-identity-workshop-applied-1280x720.png`, exact `844x390` containment at `output/rider-identity-workshop-applied-844x390.png`, and the Powerful/Deep rider in live Close Chase at `output/rider-identity-live-powerful-deep-close.png`. Browser console and page errors are empty. This milestone and accumulated work remain uncommitted, unpushed, and unpublished because this automatic goal continuation received no fresh release instruction; representative riders should still judge silhouette and skin readability across all outfit palettes, cameras, displays, and weather.

## Environmental rider and bike wear - 2026-07-26

- **The ride now leaves a visible history:** The player rider and bike accumulate deterministic, cosmetic-only dry dust, mud, wetness, and crash wear from the authoritative session weather, active surface, tyre contact, roost, speed, and recovery outcome. The system never alters grip, bike condition, scoring, rewards, progression, or opponent simulation.
- **Coherent surface and weather behavior:** Packed dirt, loose dirt, gravel, rock, loam, mud, and wet ground feed distinct exposure rates. Rain builds a wet sheen, washes loose dust quickly and caked mud gradually, while dry running sheds water over time. A new activity resets to clean gear; respawning within the same activity preserves the story of that ride.
- **Readable real materials:** Jersey, pants, helmet, goggles, gloves, boots, armor, accessories, and bike surfaces blend from their saved clean colors toward tan dust, dark mud, or crash scrape tones. Wetness darkens surfaces and lowers roughness. Six animated splatter meshes and four scratch layers follow the articulated rider/bike pose; the presentation is player-only and material updates are bounded to `12.5 Hz` to protect the existing pack render budget.
- **Crash and recovery semantics:** Automatic tipped and world-fall recoveries apply severity-scaled crash wear before returning the bike to safety. A manual race rejoin remains a neutral recovery rather than falsely inventing a crash. Cosmetics, equipment presets, and earned outfits recapture their own clean material baselines before the current activity wear is reapplied.
- **Truthful browser observability:** `render_game_to_text()` now exposes `equipment_wear.status`, dry dust, mud, wetness, and crash wear beside the player's physical state. The required packaged client starts the first Quarry Event at `CLEAN`; direct production play reaches `DUST_COATED` at `0.644` dry-dust load without debug hooks or browser errors.
- **Deterministic and production validation:** The dedicated wear probe proves exact replay, dry dust, storm washing, mud caking, wet roughness, visible splatter and scratch geometry, production crash recovery, respawn persistence, and exact new-session cleanup. The parser/editor scan, all `67` curated probes, and Academy, Circuit, Pine Enduro, Mesa MX, Freestyle, and Discovery activity smokes passed in `499.9 seconds`; the opponent challenge matrix retained its Flow-spending and boost-pressure coverage.
- **Fresh exact Web candidate:** The deterministic `0.3.8-racecraft-v28` export and all `17` delivery/recovery tests pass. The local pack is `index.3ba3f6750e91.pck` (`5,279,948` bytes; SHA-256 `3ba3f6750e919e3f124bd0ba229fba8d34395f57744bc31adf0e5bc5919ed888`) beside the unchanged `index.9778df3b5d0e.wasm`.
- **Packaged visual proof and release status:** The required client reaches the clean Quarry Event `01 / 19` Garage at `output/rider-wear-final-client/shot-0.png` with structured clean wear state and no error artifact. Live exact-package proof at `output/rider-wear-final-dust-coated-close.png` shows the dust-coated rider and bike in Close Chase with empty console/page errors. This milestone and the accumulated local work remain uncommitted, unpushed, and unpublished because this automatic goal continuation received no fresh release instruction. Representative riders should still judge dust/mud/wet/crash readability across every camera, outfit palette, weather condition, and display class.

## Visible, persistent rider equipment - 2026-07-26

- **Saved gear is now real gear:** Gloves and boots no longer inherit the jersey and pants render materials. Each owns an independent articulated surface that follows the existing trick and riding poses, so the stored Workshop choices visibly reach the rider instead of surviving only as profile labels.
- **Three complete equipment slots:** Rider identity now includes goggles, protection, and an accessory in addition to the established helmet, jersey, pants, gloves, boots, livery, plate, accent, and number. Existing profiles migrate conservatively to Clear goggles, a Roost Guard, and no accessory; JSON sanitization, atomic cosmetic writes, all three Outfit slots, and failed-write rollback preserve the expanded identity.
- **Four coherent earned kits:** Factory, Desert, Night, and Champion styles now specify every equipment slot. Their visible lens, armor, and optional neck roll, hydration pack, or champion sash stay cosmetic-only and are described before equipping. Workshop summaries disclose the complete saved outfit without implying performance benefits.
- **Renderer and observability proof:** The bike owns distinct glove, boot, goggle, armor, and accessory materials plus visibility-controlled equipment meshes. A production presentation snapshot proves the independent surfaces and optional geometry without becoming gameplay authority. Browser-readable player state now exposes pants, boots, gloves, goggles, protection, and accessory alongside the existing identity fields.
- **Deterministic validation:** The outfit probe covers expanded preset identities, legacy defaults, JSON sanitization, complete save/load, atomic rollback, independent glove/boot surfaces, Factory visibility, optional accessory suppression, and a fully visible Night equipment load. The browser-state probe covers every new field. The parser/editor scan, all `66` curated production probes, and Academy, Circuit, Pine Enduro, Mesa MX, Freestyle, and Discovery activity smokes passed in `489.0 seconds`; the opponent challenge matrix retained Flow spending and boost pressure across difficulty tiers.
- **Fresh exact Web candidate:** The deterministic `0.3.8-racecraft-v28` export and all `17` delivery/recovery tests pass. The local pack is `index.bd5eb932ffc1.pck` (`5,269,384` bytes; SHA-256 `bd5eb932ffc102993753ae1ea0cc47a563ecb9a14d92fad163700071592bbeae`) beside the unchanged `index.9778df3b5d0e.wasm`.
- **Packaged visual proof:** The required client reaches the clean Quarry Event `01 / 19` Garage at `output/rider-equipment-final-client/shot-0.png`; its compact structured state includes every equipment field and has no error artifact. Direct packaged inspection shows the complete Style card at `output/rider-equipment-workshop-1280x720.png`, exact no-overflow containment at `844x390` in `output/rider-equipment-workshop-844x390.png`, and the distinct Factory armor/kit silhouette in live Circuit play at `output/rider-equipment-race-1280x720.png`. Browser console and page errors are empty.
- **Release status and next acceptance:** This equipment milestone and all accumulated local work remain uncommitted, unpushed, and unpublished because this automatic goal continuation received no fresh release instruction. Representative riders should still judge equipment readability from every camera, the earned-style silhouettes, and Workshop density across keyboard, gamepad, touch, and compact displays.

## Player-authored rider numbers - 2026-07-26

- **True 001-999 identity editing:** The Race Workshop now includes a Number category between Style and Outfit. Riders edit hundreds, tens, and ones independently with the same semantic navigation and Confirm controls used everywhere else, see a bracketed live digit preview, then explicitly apply the complete number. `000` is refused without mutating the active rider; the number remains cosmetic and clearly states that it has no performance effect.
- **One coherent customization path:** Applying a number uses the atomic profile transaction, updates the Garage bike immediately, persists through browser primary/backup storage, appears on the real gameplay number plates, and is captured by the existing Outfit slots. Loading an outfit resynchronizes both the active number and Number-editor draft, so the editor, preview, saved identity, and rendered bike cannot disagree.
- **Accessible observability and feedback:** The browser-readable player state now exposes rider number, helmet, jersey, and livery. Workshop state exposes current and draft numbers plus the visible item, action, and status receipt. Every digit cycle, successful apply, invalid refusal, outfit save, and outfit load uses explicit semantic text and the existing interface audio/haptic feedback rather than color alone.
- **Deterministic proof:** The rider identity probe starts at `#017`, edits `#128` through all three digit controls, applies it, refuses `#000`, saves `#128` to Outfit A, changes away, restores the saved number, and verifies `#128` on the real 3D plate. The browser-state contract now proves the same identity and Workshop fields. The parser/editor scan, all `66` curated production probes, and Academy, Circuit, Pine Enduro, Mesa MX, Freestyle, and Discovery activity smokes passed in `473.4 seconds`; opponent pressure retained 51 Flow boosts in both strong and elite traffic samples. `git diff --check` is clean.
- **Exact Web candidate:** The fresh deterministic Web export and all `17` delivery/recovery tests pass. The local `racecraft-v28` pack is `index.79a835e3cdac.pck` (`5,265,032` bytes; SHA-256 `79a835e3cdac1458718970b848a6f6abf2ba444fe89e8cbc03ce565559d47ef5`) beside the unchanged `index.9778df3b5d0e.wasm`.
- **Packaged browser proof:** The required client opens the clean Quarry Event `01 / 19` Garage at `output/rider-number-final-client/shot-0.png` with matching starter identity state and no error artifact. Direct exact-package interaction visibly builds `#128` at `output/rider-number-browser-proof-draft-128.png`, applies and durably saves it, stores it in Outfit A, changes to `#129`, then restores `#128`; `output/rider-number-browser-proof-outfit-restored-128.png` shows the final outfit and save receipts. The Number panel remains contained without document overflow at `844x390` in `output/rider-number-browser-proof-844x390.png`. Browser console and page errors are empty.
- **Release status and next acceptance:** This milestone remains local, uncommitted, unpushed, and unpublished because this continuation received no new release instruction. Representative riders should still judge digit-edit speed, plate readability across cameras, and identity discoverability on keyboard, gamepad, and touch.

## Persistent rider outfit identities - 2026-07-26

- **Independent identity slots:** The Workshop now exposes an Outfit category with three bounded load/save slots. Each slot preserves the complete rider identity—helmet, jersey, pants, boots, gloves, bike livery, number plate, accent color, and rider number—without copying tune, parts, durability, odometer, cash, reputation, or other career state. Empty loads are refused explicitly, occupied slots preview every saved dimension, and Save/Overwrite/Load actions use the existing keyboard, gamepad, and semantic Workshop flow.
- **Durable, safe persistence:** Profile schema 8 stores outfits beside existing bike builds. Slot IDs and names are normalized, cosmetics are sanitized, rider numbers are clamped, unknown slots are discarded, and schema-7 profiles migrate without invented data. Outfit save/load and direct style changes now use the atomic profile transaction path, so a failed durable write restores the complete prior identity and active-bike livery rather than leaving a memory-only mutation.
- **Immediate visual coherence:** Equipping or loading an outfit updates the active Garage bike immediately and applies the same identity when gameplay is composed. The bike visual retains a testable presentation snapshot, and its real number-plate labels receive the saved rider number.
- **Deterministic proof:** `rider_outfit_slots_probe.tscn` covers distinct A/B identities, complete restore, performance/progression isolation, livery synchronization, JSON tamper controls, legacy migration, save/load rollback, exact three-slot bounds, Workshop feedback, and the visible `#17` plate. The final parser/editor scan, all `87` focused probes, and Academy, Circuit, Pine Enduro, Mesa MX, Freestyle, and Discovery activity smokes passed in `671.6 seconds`. `git diff --check` is clean.
- **Exact Web candidate:** The deterministic Web build and all `17` delivery/recovery tests pass. The local `racecraft-v28` pack is `index.44e7d2822cf0.pck` (`5,262,184` bytes; SHA-256 `44e7d2822cf0cd275ac52e5fe13eec5b4e0ffa190b225af712e1f883c0e9476f`) beside the unchanged `index.9778df3b5d0e.wasm`.
- **Packaged browser proof:** The required final-package client reaches the clean Quarry Event `01 / 19` Garage at `output/rider-outfit-garage-client/shot-0.png` with structured `mode=GARAGE`, `event=CIRCUIT`, `setup=BALANCED`, and no console-error artifact. Direct packaged interaction reaches the readable Outfit A save card at `output/rider-outfit-browser-proof-tab.png`, then completes durable save and load; `output/rider-outfit-browser-proof-loaded.png` visibly reports `OUTFIT A LOADED // MESA RED // #17` and `[SAVED] // CAREER PROGRESS`. The same Outfit card remains contained at `844x390` in `output/rider-outfit-browser-proof-844x390.png` with exact document/client dimensions and no overflow. Browser console and page errors are empty.
- **Release status and next acceptance:** This outfit milestone and the accumulated local riding, competition, accessibility, feedback, and progression work remain uncommitted, unpushed, and unpublished because this continuation received no new release instruction. Representative riders should still judge outfit naming, identity variety, and tab density across keyboard, gamepad, touch, and compact displays.

## Production Web game-state observability - 2026-07-26

- **A truthful production bridge:** The shipped wrapper and inner Godot runtime now expose `window.render_game_to_text()`. It returns compact JSON for the current mode, activity, coordinate convention, player pose/velocity/controls/Flow/boost/transmission/contact, race progress/classification/integrity, visible opponents, live semantic coaching, camera, Garage/Workshop selection, save lifecycle, and input mode. Engine-only values are projected into ordinary JSON, opponent output is capped at eight non-player riders, and the live snapshot is refreshed at a bounded `10 Hz`.
- **Presentation-aware rather than scene-aware:** Garage and Workshop deliberately suppress hidden race riders and coaching while retaining the selected event, setup, workshop, and menu status. Live play mirrors the actually visible center message, racecraft and control prompts, landing/balance coach, and line moment/score; browser inspection caught and closed an initial omission where `CLEAN LANDING` was visible but absent from the text state.
- **Outer-page forwarding and time fallback:** The public wrapper forwards both `render_game_to_text()` and `advanceTime(ms)` into the same-origin game iframe, with honest Start/Loading state before the engine is ready. The inner runtime owns the current state string and installs a non-blocking `requestAnimationFrame` time fallback only when a test harness has not supplied one. That fallback waits real browser frames; it is not deterministic Godot physics stepping.
- **Deterministic contract coverage:** The permanent Web text-state probe verifies schema and world coordinates, signed controls, physical and transmission state, race timing/gaps/integrity, player exclusion and rider cap, one-wheel and line coaching, Garage suppression, JSON safety/size, platform publication, and bridge ownership. Web delivery coverage increased from `15` to `17` tests for the wrapper and inner-runtime contracts.
- **Production browser agreement:** The required packaged-game client opens on the clean Quarry Event `01 / 18` Garage at `output/web-text-state-accepted-client/shot-0.png`; its `1,643`-byte state reports `mode=GARAGE`, `event=CIRCUIT`, `setup=BALANCED`, zero visible riders, and no hidden coaching. Direct live proof at `output/web-text-state-proof/03-accepted-live-driving.png` agrees with `03-accepted-live-driving-state.json`: `mode=RACE`, `phase=RACING`, a 12-rider field, P10, active throttle, eight bounded visible rivals, `BUILD THE LINE`, `LINE 000000`, the gate instruction, and the `FAST LINE` racecraft cue. `advanceTime(120)` resolved in `138 ms`; browser console and page errors are empty.
- **Complete regression:** The exact source passed the parser/editor scan, all `82` focused/full probes, and Academy, Circuit, Pine, Mesa, Freestyle, and Discovery production activity smokes in `668.0 seconds`. The opponent challenge matrix retained meaningful AI Flow and boost pressure, including `51` boost activations in both strong and elite pressured Circuit samples. `git diff --check` reports no whitespace errors.
- **Exact Web candidate and release status:** All `17` delivery/recovery tests pass. The local `racecraft-v28` pack is `index.64914a6729d4.pck` (`5,202,244` bytes; SHA-256 `64914a6729d4d1efbce7dffecc58c1c01b6205994332517e97517d9531e4c036`) beside `index.9778df3b5d0e.wasm`. Manifest, disk, and HTTP hashes match exactly; port `8777` returns immutable caching, `X-Riding-Dirty-Build: racecraft-v28`, COOP `same-origin`, and COEP `require-corp`. This milestone is local, uncommitted, unpushed, and unpublished pending a fresh release request.

## Deliberate stoppies and readable one-wheel balance - 2026-07-26

- **A real missing control outcome:** Hard braking while leaning forward now shifts a bounded share of braking demand toward the front contact patch, producing physical load transfer and rear-wheel lift through the existing brake and rider-weight controls. Neutral braking keeps the established one-third front bias, while full deliberate intent is capped at `72%`; no hidden stunt button or orientation override was added.
- **Two coherent ground-balance styles:** The existing physical wheelie and new physical stoppie share one pure authority for speed, contact, hold-time, progress, and signed pitch coaching. Wheelies award after `0.80 seconds`; stoppies use a responsive `0.30-second` deliberate hold. Each awards exactly once per continuous attempt, adds a small bounded Flow reward, contributes to the existing line/style system, and fully clears on contact recovery, takeoff, or respawn.
- **Actionable multimodal feedback:** The otherwise-airborne landing meter is reused on the ground, where the two presentations cannot conflict. It first teaches `LOAD FRONT // HOLD BRAKE + LEAN FORWARD`, then shows `WHEELIE` or `STOPPIE`, numeric progress, and a signed `LEAN FORWARD` / `LEAN BACK` correction. Completion has distinct racecraft pitch identity, bounded haptics, the standard race-moment receipt, and semantic text rather than color-only status.
- **Physical and production-chain proof:** The new permanent probe covers neutral suppression, bounded brake bias, setup language, direction-aware wheelie/stoppie coaching, controller signals/counters, Flow, HUD progress, haptic/audio identity, exactly-once awards, respawn clearing, and a real `16 m/s` rigid-body braking run. That run produced `21` rear-lift frames, reached `100%`, awarded one stoppie, and remained upright.
- **Complete regression:** The exact source passed the parser/editor scan, all `81` focused/full probes, and Academy, Circuit, Pine, Mesa, Freestyle, and Discovery production activity smokes in `644.2 seconds`. The directly affected landing, racecraft, presentation, audio-identity, and bike-dynamics probes also passed; the reference bike retained `0.983` jump-face momentum and a `32.00 m` jump.
- **Exact Web candidate:** All `15` delivery/recovery tests pass. The local `racecraft-v28` pack is `index.0d5bd496a2b6.pck` (`5,191,616` bytes; SHA-256 `0d5bd496a2b65604b75a54c5bd3f9722fe3a7306d96eb2c9f381ef6a535da6d1`) beside `index.9778df3b5d0e.wasm`. Manifest, disk, and HTTP hashes match exactly; port `8777` returns immutable caching, `X-Riding-Dirty-Build: racecraft-v28`, COOP `same-origin`, and COEP `require-corp`.
- **Browser proof and release status:** The required final-package client reached the clean Quarry-first Garage at `output/ground-balance-client/shot-0.png`. Direct production-canvas play reaches `BALANCE // STOPPIE // 100% // LEAN BACK`, the full meter, `STOPPIE` race moment, line reward, and Flow gain at `output/ground-balance-proof/01-stoppie.png`; browser console and page errors are empty. This milestone is local, uncommitted, unpushed, and unpublished pending a fresh release request.

## Actionable landing prediction and receiver coaching - 2026-07-26

- **Hidden physics made readable:** The bike already sampled the predicted receiver for bounded landing alignment, but the rider had no access to that state. A new pure `LandingProjection` authority now classifies real airborne observations into Search, Set, Adjust, or Danger without changing orientation, velocity, assistance, scoring, or crash rules.
- **One actionable correction:** Receiver slope, bike-to-slope orientation, travel direction, signed pitch error, roll error, normal impact speed, angular speed, distance, and trick-return state resolve into one semantic instruction: Scan Receiver, Hold, Lean Forward, Lean Back, Level Bike, Stop Rotation, Straighten Bike, Match Slope, or Return to Bike. The HUD explicitly states `LIKELY SAFE`, `ADJUST`, or `HIGH RISK` with a numeric confidence bar, so essential status never depends on color alone.
- **Restrained multimodal feedback:** The live coach appears only after a real airborne window and clears immediately on contact, reset, or HUD Off. A close-range Danger transition produces one bounded haptic warning and a descending SFX; correcting that warned approach to Set produces one distinct rising recovery SFX. Transition latches prevent noisy state flapping.
- **Production-chain coverage:** The new curated probe covers grounded suppression, missing-receiver search, safe confidence, signed pitch advice, roll priority, late trick return, hard-impact risk, real bike transition latching, viewport containment, semantic HUD text, haptic identity, distinct SFX inventory, danger-to-safe recovery, and post-contact clearing.
- **Complete regression:** The exact source passed the parser/editor scan, all `59` curated probes, and Academy, Circuit, Pine, Mesa, Freestyle, and Discovery production activity smokes in `451.1 seconds`. Bike dynamics retained `0.983` jump momentum across a `32.00 m` reference jump, animation remained stable at 30/60/120 Hz, and Freestyle retained its `1128`-point physical scoring smoke.
- **Opponent-pressure evidence:** The dedicated challenge matrix confirms opponents spend Flow rather than merely accumulating it: Circuit boost use scales `24 / 46 / 54` from Relaxed through Expert, Pine scales `55 / 88 / 100`, Mesa scales `29 / 53 / 66`, and a pressured Standard starter field converts Flow into `51` boost activations over `55.60 seconds` at a bounded `2.30 m/s` peak. A P12 smoke correctly permits zero reactive boosts because the field is already ahead rather than wasting Flow as visible rubber-banding.
- **Exact Web candidate:** All `15` delivery/recovery tests pass. The local `racecraft-v28` pack is `index.4ba8553763ac.pck` (`5,185,084` bytes; SHA-256 `4ba8553763acf7f0767d6e37827352d9de012d5aac92d053d424b4dd0f592c5e`) beside `index.9778df3b5d0e.wasm`.
- **Browser proof and release status:** The required final-package client opens on Quarry event `01 / 18` at `output/landing-readability-client/shot-0.png`. Direct production-canvas play shows high-altitude `SCAN RECEIVER` at `output/landing-readability-proof/01-trick-danger.png`, the real close-range `HIGH RISK // 60% // LEVEL BIKE` projection at `03-returned.png`, and clean coach removal before the authoritative touchdown receipt at `04-landed.png`; `browser-errors.json` is empty. The synthetic contract proves the opposite `LIKELY SAFE // HOLD` state. Representative riders should still judge confidence thresholds across bike classes and camera modes. This milestone is local, uncommitted, unpushed, and unpublished pending a fresh release request.

## Deliberate freestyle tricks and transparent scoring - 2026-07-26

- **Physical trick language:** `C` / gamepad Right Shoulder remains the semantic, remappable Racecraft action on the ground and becomes an intentional trick modifier only in flight. Modifier plus neutral, forward lean, rear lean, left steer, or right steer produces No-Hander, Superman, Seat Grab, Left Can-Can, or Right Can-Can. Measured signed pitch/yaw motion supplies Backflip, Frontflip, and 360 identity, and physically valid combinations retain both names.
- **Expressive rider animation:** The procedural rider now blends hands and feet away from grips/pegs for the selected airborne pose, keeps the bike's rigid-body orientation authoritative, and smoothly reconnects every limb after release. Releasing at least `0.14 seconds` before touchdown is a real execution requirement; a late return resets the combo and reduces the score.
- **Fair, varied scoring:** One pure rule authority grades Perfect, Clean, Heavy, Late Return, Hard Landing, or Crashed outcomes and exposes base, airtime, rotation, landing, execution, combo, variety, and repetition factors. New tricks earn a `1.15x` variety bonus, repeated identities fall through `1.00 / 0.72 / 0.50 / 0.35`, and upright heavy impacts are no longer mislabeled as crashes.
- **Exploit removed:** Direct Web play exposed that the elevated Freestyle spawn could award a free `0.3s` Straight Air. Score settlement now requires a real suspension-contact ground-to-air takeoff. The production smoke establishes contact and launches through a physical mass-scaled impulse, while the focused probe explicitly proves a spawn drop leaves score `0` and combo `x1`.
- **Readable onboarding and feedback:** Freestyle start teaches the active device binding and safe return without adding another control. Every scored landing presents trick name, landing grade, awarded total, all four additive components, variety, repetition, and combo. The same authoritative receipt drives pitched SFX plus clean/failure Commentary and Crowd reactions.
- **Deterministic release proof:** The new curated end-to-end probe covers five input poses, combined physical classification, difficulty ordering, repetition loss, variety reward, late-return failure, hard-landing/crash separation, production bike observation/reset, articulated limb travel/return, no-free-spawn scoring, dynamic prompts, HUD receipts, and audio identity. The exact source passed the parser/editor scan, all `58` curated probes, the complete opponent Flow/boost challenge matrix, and all six production activity smokes in `442.8 seconds`; `git diff --check` is clean.
- **Exact Web candidate:** All `15` delivery/recovery tests pass. The final `racecraft-v28` pack is `index.6c65944c16dc.pck` (`5,174,796` bytes; SHA-256 `6c65944c16dc524e3a41721b35442d314f07cfe2e960c644d05a46243e880939`) beside `index.9778df3b5d0e.wasm`.
- **Browser proof and release status:** The required final-package client opens on Quarry event `01 / 18` at `output/freestyle-v28-final-client/shot-0.png`. Direct production-canvas inspection proves zero-score onboarding at `output/freestyle-v28-browser-proof-final/03-superman-pose-a.png`, visible airborne extension/return at `output/freestyle-v28-airborne-proof-final/02-superman-held.png` and `03-safe-return.png`, and the final named `SUPERMAN // HARD LANDING` receipt at `04-score-receipt.png`; `browser-errors.json` is empty. This milestone remains local, uncommitted, unpushed, and unpublished without a fresh explicit release request.

## Visible autosave lifecycle and recoverable ghost data — 2026-07-26

- **One truthful save authority:** The new `SaveLifecycle` autoload owns tokenized Saving, Saved, Recovered, and Failed states across career, settings, and personal-best ghost writes. Nested writes retain the critical-save guard until every critical token finishes, unknown tokens cannot release it, and every state carries a bounded semantic domain, context, error identity, and sequence.
- **Always-readable player feedback:** A top-center status badge survives Garage, riding, paused Settings, and every supported viewport. It states `[SAVING]`, `[SAVED]`, `[RECOVERED]`, or `[SAVE FAILED]` in text as well as color, names the affected data, remains visible for a bounded readable hold, and never relies on a transient animation to communicate the result.
- **Protected application close:** Main disables automatic window-close acceptance and defers an operating-system close request while any critical write is active. Completion of the final successful critical token releases the pending close; a failed write cancels that close so the player can read and act on the error, while programmatic smoke/probe exits remain unaffected.
- **Verified career/settings boundaries:** Career and Settings now publish lifecycle state at their actual verified storage boundary. Browser backup recovery reports whether the primary slot was repaired. Corrupt settings with no valid recovery explicitly report that defaults were restored. The settings-reference setter is idempotent, preventing an applied setting from causing a redundant profile write or replacing honest `SAVED // RIDER SETTINGS` feedback.
- **Recoverable personal-best ghosts:** Desktop ghost writes migrated from direct binary overwrite to the verified temporary-write, readback, primary-replace, and rotating-backup store. Legacy V27 binary ghosts load once and migrate automatically; corrupt primary data recovers and repairs from backup. Browser ghost loads now expose primary/backup source and repair state through the same lifecycle.
- **Deterministic regression contract:** The new curated save-lifecycle probe covers nested critical/noncritical writes, close-guard counts, semantic badge text, viewport containment, failure identity, real settings persistence, the settings-apply no-op regression, two-generation ghost rotation, deliberate primary corruption, backup recovery, and repair.
- **Production validation:** The exact final source passed the parser/editor scan, all 57 curated high-risk probes, the complete opponent Flow/boost challenge matrix, and Academy, Circuit, Pine, Mesa, Freestyle, and Discovery smokes in `477.1 seconds`. The new save contract and all affected persistence, settings, ghost, presentation, accessibility, profile, and Web-budget probes also passed independently; `git diff --check` is clean.
- **Fresh Web candidate:** All 15 delivery/recovery tests pass. The exact local `racecraft-v28` pack is `index.0f02a4629c89.pck` (`5,164,620` bytes; SHA-256 `0f02a4629c895a8b7301fb63d7c4becae2b0f7f6f879a67a507d4c0c00aee610`) beside the unchanged `index.9778df3b5d0e.wasm` runtime on `127.0.0.1:8777`.
- **Browser proof and release status:** The required Web client reached the clean Quarry-first Garage at `output/save-lifecycle-final-client/shot-0.png`, visibly showing Event `01 / 18`. Direct production-canvas inspection caught and fixed the redundant-career-save feedback bug, then verified Commentary changed from `80%` to `75%` at `artifacts/save-lifecycle/settings-saved-75.png` and restored to `80%` with `SAVED // RIDER SETTINGS` at `artifacts/save-lifecycle/settings-restored-80.png`; no browser console or page errors occurred. This milestone remains local, uncommitted, unpushed, and unpublished without a fresh explicit release request.

## Independent commentary and crowd mixing — 2026-07-26

- **Complete seven-row mixer:** Audio Settings now exposes Master, Music, Engine, Effects, Commentary, Crowd, and Interface as independent 5% controls. The two new authored defaults are both `80%`, keeping every visible value aligned to the promised step grid.
- **Semantic race callouts:** The real `RaceController.race_moment` stream maps overtakes, position losses, holeshots, near misses, tricks, warnings, and other scored moments into restrained positive, warning, or milestone Commentary identities. Sponsor-completion motifs also route through Commentary instead of being mixed into world effects.
- **Reactive crowd layer:** Deterministic band-shaped crowd swells provide distinct cheer, roar, and gasp reactions for overtakes, holeshots, tricks, crashes/recoveries, and finishes. Reactions use their own `Crowd` bus and bounded cooldown, so busy races stay readable rather than producing an unbroken wall of noise.
- **Honest route isolation:** Music, SFX, Engine, Commentary, Crowd, and Interface own distinct mixer buses. The shared five-voice pool claims its semantic bus on every playback; live-source coverage proves Commentary and Crowd routing before the next gameplay cue reclaims SFX.
- **Durable setting and migration:** Settings schema V13 adds clamped Commentary/Crowd state, verified primary/backup persistence, exact `0%` mute/unmute, isolation from Effects and each other, corrupt-primary recovery, and conservative migration defaults. Mouse, keyboard, gamepad, and touch-compatible Settings navigation covers all seven rows.
- **Production validation:** The exact corrected source passed the parser/editor scan, all 56 curated high-risk probes, the complete opponent Flow/boost challenge matrix, and Academy, Circuit, Pine, Mesa, Freestyle, and Discovery smokes in `445.1 seconds`.
- **Fresh Web candidate:** All 15 delivery/recovery tests pass. The exact local `racecraft-v28` pack is `index.ab24d58650c5.pck` (`5,155,056` bytes; SHA-256 `ab24d58650c5537bc1bf8ce0dc50dfa476aa8f417989f9e6a39dbeddf06c6835`) beside the unchanged `index.9778df3b5d0e.wasm` runtime on `127.0.0.1:8777`.
- **Browser proof and release status:** The required Web client reached the clean Quarry-first Garage at `artifacts/commentary-crowd-audio/default-garage.png`. Direct production-canvas inspection verified the full seven-row page at `artifacts/commentary-crowd-audio/seven-channel-defaults.png`, changed Crowd from `80%` to `75%` at `artifacts/commentary-crowd-audio/crowd-control-75.png`, restored it to `80%` at `artifacts/commentary-crowd-audio/crowd-restored-80.png`, and produced only the three expected Godot/WebGL startup logs. This milestone remains local, uncommitted, unpushed, and unpublished without a fresh explicit release request.

## Independent interface audio mixing — 2026-07-26

- **Five honest audio channels:** Audio Settings now exposes Master, Music, Engine, Effects, and Interface as independent 5% controls. Semantic menu navigation, confirmation, refusal, reward, and result cues route through a real `Interface` bus, while landings, impacts, Flow, sponsors, opponents, and other world feedback remain on `SFX`.
- **Deterministic shared-voice routing:** The pooled audio player claims its requested bus on every cue before playback, so an Interface cue cannot leak into Effects and the next gameplay cue reliably reclaims `SFX`. The same contract remains testable under Godot's dummy audio driver without creating backend playback objects or leaking resources.
- **Durable setting and migration:** Settings schema V12 adds clamped `interface_volume` state with a conservative `90%` default. Legacy settings migrate without disturbing the four established channels, and Interface persists through verified primary/backup storage, exact `0%` mute and unmute, and corrupt-primary recovery.
- **Isolation and presentation coverage:** Focused probes verify a distinct Interface bus, independent Effects/Interface levels, live UI-to-gameplay bus reclamation, the exact five-row Audio inventory, dormant Voice/Crowd rejection, migration, persistence, and recovery.
- **Production validation:** The parser/editor scan and affected audio, settings, navigation, and presentation probes pass. The exact final source passed all 56 curated high-risk probes, the complete opponent Flow/boost challenge matrix, and Academy, Circuit, Pine, Mesa, Freestyle, and Discovery smokes in `466.3 seconds` with no ObjectDB leak.
- **Fresh Web candidate:** All 15 delivery/recovery tests pass. The exact local `racecraft-v28` pack is `index.4e9516274cda.pck` (`5,153,120` bytes; SHA-256 `4e9516274cdadb5549a9a8712d08822f82aa7b964bee2e5a59189669b39b2f97`) beside the unchanged `index.9778df3b5d0e.wasm` runtime on `127.0.0.1:8777`.
- **Browser proof and release status:** The required Web client reached the garage at `artifacts/interface-audio/default-garage.png`. Direct production-canvas inspection verified the five-row Audio page, changed Interface to `85%` with immediate confirmation at `artifacts/interface-audio/interface-control-85.png`, restored it to `90%` at `artifacts/interface-audio/interface-restored-90.png`, and produced only the three expected Godot/WebGL startup logs. This milestone remains local, uncommitted, unpushed, and unpublished without a fresh explicit release request.

## Persistent HUD safe-area repositioning — 2026-07-26

- **Player-controlled live-HUD placement:** Access now exposes `HUD SAFE AREA` directly after HUD Size. Riders can keep the established edge-aligned layout at `0%` or inset every live race element by `5%` or `10%` of the active viewport. The option composes with Full, Focused, Minimal, Off, 75–100% HUD sizing, text scale, and every activity-specific visibility rule.
- **Safety overlays stay authoritative:** Only the live-race root is repositioned. Settings, pause messaging, and official Results remain full-screen and readable even when the live HUD is hidden or maximally inset. The inset is recomputed from the actual viewport on resize, preserving proportional placement across supported resolutions.
- **Durable setting and migration:** Settings schema V11 adds a clamped `hud_safe_area` value with a conservative `0%` default. Legacy files migrate to the pixel-identical edge layout, while mouse, keyboard, gamepad, and touch-compatible Settings changes persist through verified primary/backup storage and corrupt-primary recovery.
- **Deterministic geometry contract:** Presentation coverage proves the default layout is unchanged, `10%` positions the live root at one tenth of the configured viewport and reduces its unscaled size to 80%, HUD scaling remains independent, activity essentials survive Minimal mode, and pause/Results survive Off mode. The sensory-accessibility probe covers the new eleven-row Access page, migration, live interaction, persistence, and recovery.
- **Production validation:** The parser/editor scan and focused sensory, presentation, production-settings, settings-navigation, and Web-budget probes pass. The complete curated gate passed all 56 high-risk probes, the opponent Flow/boost challenge matrix, and Academy, Circuit, Pine, Mesa, Freestyle, and Discovery smokes in `454.1 seconds`; `git diff --check` is clean.
- **Fresh Web candidate:** All 15 delivery/recovery tests pass. The exact local pack is `index.cd48271be56f.pck` (`5,152,672` bytes; SHA-256 `cd48271be56f62a149e595938f2df53c1f1040efcfe964efe8a936ca87eb80ba`) beside the unchanged `index.9778df3b5d0e.wasm` runtime on `127.0.0.1:8777`.
- **Browser proof and release status:** The required Web client reached the clean Quarry-first Garage at `artifacts/hud-safe-area/default-garage.png`. Direct production-canvas inspection verified all eleven Access rows, set the maximum inset, entered live Mesa play, and retained readable telemetry, standings, map, Flow, and touch controls with only the three expected Godot/WebGL startup logs. The live capture is `artifacts/hud-safe-area/live-10-percent.png`; the saved preference was restored to `0%`. This milestone remains local, uncommitted, unpushed, and unpublished without a fresh explicit release request.

## Persistent sensory-effects accessibility — 2026-07-26

- **Two missing comfort controls are real:** Access now exposes Reduced Flashes and Reduced Particles directly after Reduced Motion. Both are opt-in, apply live through mouse, keyboard, gamepad, and touch-compatible Settings, and remain independent from camera motion, visual quality, HUD detail, text scale, contrast, and color-safe presentation.
- **Readable feedback without flashing:** Reduced Flashes removes the full-screen cyan/red pulse and replaces the oscillating Flow-refusal meter with one stable warning treatment. Text, color polarity, audio, haptics, timing, and gameplay outcomes remain intact, so accepted actions and warnings stay understandable without repeated luminance changes.
- **Meaningful particle reduction:** Reduced Particles caps every current and newly streamed `GPUParticles3D` effect at 25% density, including bike dust, roost, landing debris, opponent effects, weather, and destructible-prop impacts. The cap composes with Performance/Balanced/Quality instead of altering render scale, anti-aliasing, shadows, physics, timing, records, or gameplay.
- **Durable settings:** Settings schema V10 adds verified defaults, migration, sanitization, primary/backup persistence, corrupt-primary recovery, and live scene propagation for both preferences. The focused sensory probe caught and fixed an initial native-build path that reported the reduction but still forced full particle density.
- **Release validation:** The parser/editor scan, sensory, Web-budget, production-settings, settings-navigation, presentation, difficulty/quality, and persistence probes pass. The curated gate passed all 56 high-risk probes, the complete opponent Flow/boost matrix, and Academy, Circuit, Pine, Mesa, Freestyle, and Discovery smokes in `451.1 seconds`.
- **Fresh Web candidate:** All 15 delivery/recovery tests pass. The exact local pack is `index.3614f84b673f.pck` (`5,152,336` bytes; SHA-256 `3614f84b673f10d0fc0b6b85387093a67301b84e81daf7ff4e1eea33f792a001`) beside the unchanged `index.9778df3b5d0e.wasm` runtime on `127.0.0.1:8777`.
- **Browser proof and release status:** The required Web client reached the clean Quarry-first Garage at `artifacts/web-sensory-accessibility/shot-0.png`. Direct production-canvas inspection verified all ten Access rows, enabled both new reductions, entered live Mesa gameplay, and reported zero console errors; the test preferences were then restored. This milestone remains local, uncommitted, unpushed, and unpublished without a fresh explicit release request.

## Persistent HUD customization and release-rebuild hardening — 2026-07-26

- **Four honest live-HUD modes:** Accessibility now exposes Full, Focused, Minimal, and Off. Full preserves the complete established race presentation; Focused removes the title, standings, assist summary, control teaching, sponsor/modifier copy, and other full-detail clutter while retaining timing, race status, navigation, racecraft, Flow, speed, and warnings; Minimal keeps only essential telemetry and activity guidance; Off disables the complete live HUD.
- **Independent HUD sizing:** Live presentation can be resized from 75% to 100% in 5% steps without changing the global text scale, Settings, pause safety overlay, or official Results. Scaling uses a centered full-viewport live root, so smaller HUDs move inward rather than clipping anchored edge controls.
- **Activity-safe presentation layers:** Full-only and Focused presentation controls are authored directly under transparent visibility layers instead of being moved after layout. Circuit, Pine, Mesa, Freestyle, Discovery, and Academy retain their own natural visibility rules; Minimal Academy keeps the teaching card, Minimal Discovery keeps its compass, and HUD Off still permits pause, Settings, and Results.
- **Durable accessibility state:** Settings schema V9 adds `hud_detail` and `hud_scale`, validates their exact ranges, migrates older files to Full at 100%, persists both through the verified primary/backup path, and exposes them through mouse, keyboard, gamepad, and touch-compatible Settings navigation.
- **Locked-runtime rebuild recovery:** The Web release builder now reuses an existing content-addressed asset only after verifying its complete SHA-256 digest. Re-exporting while the unchanged WASM runtime is open in a browser or preview server no longer attempts a needless Windows overwrite; a digest mismatch at the same immutable name remains a hard failure.
- **Validation:** The parser/editor scan and directly affected presentation, reduced-motion, production-settings, settings-navigation, Academy, progression, Results, competition, and Web release contracts pass. The curated production gate passed all 56 high-risk probes plus Academy, Circuit, Pine, Mesa, Freestyle, and Discovery smokes in `430.3 seconds`; the opponent challenge probe retained deterministic Flow/boost pressure across all difficulty tiers. A fresh V28 Web export passes all 15 delivery/recovery tests and serves `index.61f905ffc6d7.pck` (`5,151,648` bytes) on `127.0.0.1:8777`.
- **Browser proof:** A fresh 1920×1080 browser session reached the garage at Quarry event `01 / 18`, entered live Circuit play, and visually verified Full, Focused at 85%, Minimal, Off, and the Off-mode pause overlay. The browser reported zero console errors and zero page errors. No commit, push, or GitHub Pages publication was performed in this continuation.

## Persistent HUD customization GitHub Pages publication — 2026-07-26

- **Source release:** Commit `8245462` publishes persistent Full, Focused, Minimal, and Off HUD modes; independent 75–100% HUD sizing; activity-safe essential guidance; verified settings migration; immutable Web-asset reuse; deterministic presentation coverage; and the verified Web bundle on `codex/racecraft-v27`.
- **Pages deployment:** GitHub Pages commit `d2887278e2ca9f3ece42937c4e015d20c1fffc05` and workflow run `30198576243` completed successfully from `gh-pages` with HTTPS enforcement at `https://willtran87.github.io/project-riding-dirty/`.
- **Public payload proof:** The public manifest and inner page reference `index.61f905ffc6d7.pck`. An independent public download is exactly `5,151,648` bytes with SHA-256 `61f905ffc6d75a9a0437c82ebd4677d337876d64d22b0106bb2818659fd05fbe`, matching the committed release manifest; the former `index.46c826851ada.pck` returns 404.
- **Public browser proof:** The production wrapper starts the isolated Godot game at Quarry Trail event `01 / 18` with no browser console errors, preserving the intended first-event startup.
- **Release gates:** The published source passed the parser/editor scan, all 56 curated high-risk probes, six production activity smokes in `430.3 seconds`, the opponent Flow/boost challenge matrix, all 15 Web delivery/recovery tests, exact public payload verification, and production-wrapper browser startup.

## Racecraft V28 implementation pass — 2026-07-16

The V28 source target is `0.3.8-racecraft-v28`. This file records implemented source and verification contracts; a version string alone does not prove that the browser files currently served on port 8777 have been rebuilt.

### Race and ride experience

- **Track authority and traversability:** Quarry, Pine, Mesa, and reverse-Quarry sessions derive their visible ribbon, collision, checkpoints, minimap, containment, reset poses, rivals, NPC field, and replay line from the same route data. Automated route-authority, topology, physical-body traversal, clipping, terrain-clearance, barrier, and ground-cover probes cover the full courses, including Quarry immediately after gate 8.
- **Bike feel and racecraft:** The player uses two-point contact suspension, load-sensitive grip, high-speed steering authority, preload jumps, aerial pitch, receiver alignment, surface response, recovery, haptics, and camera feedback. Context Flow selects Surge, Rail, Compose, or Brace from the live bike state. Rear-brake slides, scrubs, pumps, clutch pops, foot dabs, rut capture, outside berms, drafting, defensive roost, and bounded skill lines feed physical impulses, grip, stability, momentum, Flow, HUD, audio, camera, Academy metrics, and replay markers.
- **Racecraft parity:** Direction-gated drafting and deterministic skill-line rules are shared by the player and AI. Rivals use rut and outside-berm targets only after launch release; deliberate player roost applies at most a 2.5% trailing drive cost per bounded hit. Pine creek and ridge trails are legal progress-mapped branches that rejoin the unchanged authoritative centerline.
- **Interactive starts:** The final countdown accepts throttle/brake staging while the bike remains physically frozen. Live prompts, engine response, haptics, and post-drop feedback support keyboard and gamepad. The resulting 0.9-second drive modifier is deliberately bounded to `0.94-1.08`, so a perfect gate matters without deciding the race.
- **Opponent field:** Named riders use the player bike presentation, remain in natural launch lanes, follow the authoritative surface, jump, overtake, defend, make recoverable mistakes, and create bounded contact pressure. Player overtakes, contacts, crashes, recoveries, clean-ride awards, statistics, and achievements are isolated from NPC-generated events.
- **Opponent challenge pass:** Ordinary fields inherit `0.65` of the active bike and selected Trail, Balanced, or Attack setup's forward-performance gain, capped at `1.12`. Neutral starter performance remains at the authored baseline, while upgraded bikes keep the unmatched share of their advantage and the field remains relevant. Difficulty modes retain distinct pace at authored-tier limits; skilled riders use longer committed passing and defensive plans, stronger section and late-race pressure, and skill-scaled crash recovery. The deterministic full-course challenge probe accepts releases by fixed benchmark placements: starter `0.888x`-gold pace must finish P5-P8, max-build `0.680x`-gold pace must finish P4-P9, and max-build elite `0.650x`-gold pace must win. Difficulty ordering, full-field completion, bounded incidents, overtakes, and upgraded-field response remain required.
- **Race depth:** The 18-event catalog includes practice, qualifying, heats, LCQ, mains, circuit, enduro, elimination, rival, endurance, hillclimb, wet, rhythm, freestyle, discovery, daily/weekly challenges, and eight Riding Academy lessons. Championship weekends, classifications, flags, penalties, standings, countback, season rollover, ghosts, replay, local boards, and challenge signatures remain integrated.
- **Closed-loop integrity:** Red Mesa's closing spline, surface frames, lap-crossing direction, continuity checks, and challenge route version share the authoritative closed route. Seam and lap-crossing probes prevent a finish-line tangent discontinuity or reverse/duplicate crossing from corrupting race progress.

### Options, accessibility, and progression

- **Race difficulty:** Relaxed, Standard, and Expert persist in the Ride settings page and apply `-1`, `0`, or `+1` to authored AI difficulty for ordinary offline races, with mode-specific pace scaling preserving meaningful separation when that offset reaches an authored-tier boundary. Daily/weekly challenges and Academy grading stay locked to their authored contracts. Effective difficulty is shown in the event briefing and included in each run signature.
- **Visual quality:** Performance (`0.75` render scale, no MSAA), Balanced (`0.90`, 2x MSAA), and Quality (`1.00`, up to 4x MSAA) persist on the Camera page. Web safely caps Quality at 2x MSAA. Rendering presets do not alter physics, simulation, timing, or run signatures.
- **Reduced Motion:** The Accessibility page can cap dynamic FOV, shake, camera bank, impact response, repositioning, and look-ahead, and swaps animated district transitions for a static briefing. The setting defaults off, migrates older settings, persists, and propagates to live consumers.
- **Settings and results navigation:** Numeric and enum rows have mouse minus/plus controls; keyboard/gamepad selection scrolls into view; page changes reset scroll; long result classifications support wheel, keyboard, and gamepad navigation and reveal the selected player row even at the back of a 12-rider field.
- **Adaptive touch controls:** Phones and tablets receive context-aware Ride, Garage, and Results controls with independent multi-touch ownership, analog steering/lean, preload release, handedness, scale, opacity, safe-area layout, portrait guidance, and a settings override. Touch actions use the same semantic input map as keyboard and gamepad and release cleanly across pause, visibility, orientation, and context changes.
- **Progression pacing:** Gold/Silver/Bronze/finisher reputation values are `30/22/16/8`, with bounded placement and personal-best bonuses. First clears, first wins, and new bests retain full value; non-improving repeat finishes award 35% reputation while cash rewards remain unchanged. Freestyle and Discovery use the same bounded medal values and repeat rule.
- **Pine unlock:** A current profile unlocks Pine after two distinct Quarry activity completions, Rook's `03:10.000` Quarry Trail target, or 80 combined reputation. Two starts alone no longer qualify. The former two-run unlock is preserved only when migrating a qualifying older save.
- **Milestones:** Eight named achievements expose titles, criteria, progress, HUD unlock notifications, workshop completion count, and the nearest unfinished goal.

### Persistence, lifecycle, and performance

- **Profile recovery:** Desktop profile writes use a verified temporary file, a rotating valid backup, final-read verification, and automatic restoration from backup when the primary cannot be read. Browser profiles and ghosts use SHA-256-verified JSON envelopes in primary/backup `localStorage` slots and repair a damaged primary after recovery.
- **Settings recovery:** Settings schema V4 adds honest independent engine/effects mixing on top of touch presentation, difficulty, and graphics while migrating legacy files. Writes are checksum-verified before and after temporary replacement, rotate a verified `.bak`, recover from backup, and repair the primary without losing accessibility or control values.
- **Browser lifecycle:** Hiding the page pauses and mutes the game, then restores only the pause/mute state owned by the visibility transition; a manual pause or pre-existing mute is preserved.
- **Pine loading:** Pine uses a route/config/schema-signature-validated `assets/generated/pine_course_dressing.scn`, with an authoritative live-build fallback when the bake is missing or stale. Exact spatial indexes accelerate terrain-profile and dressing-clearance queries without changing accepted geometry. The focused audit reduced dressing construction from roughly 3.4-4.1 seconds to 8-10 milliseconds and measured covered activity preparation at 4.793 seconds with a 137.3 FPS sample while retaining 3,644 visual instances, 830 ground-cover instances, 768 barrier panels, and 16 opening posts.
- **Web payload:** The release preset excludes development probes and the runtime smoke harness. Threaded WebAssembly delivery uses gzip negotiation, immutable caching only for content-addressed assets, no-store HTML, COOP/COEP/CORP isolation, and a host handshake that distinguishes loading progress, engine readiness, timeout, and failure. The content-addressed executable now carries matching audio and position worklet sidecars, preventing the browser audio fallback caused by a mismatched runtime basename.

## Verification and release workflow

- `python .\tools\run_quality_gate.py` performs the parser/editor scan, the curated high-risk probe suite, and six representative playable smokes (`ACADEMY`, `CIRCUIT`, `PINE_ENDURO`, `MESA_MX`, `FREESTYLE`, and `DISCOVERY`). It stops on the first non-zero result.
- `python .\tools\run_quality_gate.py --full` discovers and runs every focused `*_probe.tscn` before the same activity smokes. Coverage includes dynamics, gate launch, route authority and traversal, clipping, Pine baked assets, persistence recovery, settings/accessibility/navigation, player-owned metrics, reputation pacing, deterministic opponent challenge, competition, career, and the full race lifecycle.
- `python .\tools\build_web_release.py` performs the Godot release export, content-addresses the PCK and the complete WASM/audio-worklet runtime family with 12-character SHA-256 fingerprints, keeps both Godot audio worklets beside the shared executable basename, updates the inner Godot configuration, reinstalls progress/ready/error messaging, stamps the wrapper and `web/build-manifest.json`, rejects leaked test markers, and runs the Web delivery regression tests.
- A browser release is accepted as current only after the quality gate and release script pass, port 8777 reports the expected V28 build header and hashed assets, and live high-resolution plus compact-viewport checks reach real gameplay without console errors or document overflow.

## Racecraft V28 mobile and lap-integrity release — 2026-07-16

- The exhaustive release gate passed all 57 focused probes and five playable activity smokes. This includes deterministic multi-touch ownership and layout coverage, exact Red Mesa loop closure, finish-line tangent continuity, forward-only lap crossing, route authority, bike dynamics, opponent challenge, persistence, career, and the complete activity set.
- `0.3.8-racecraft-v28` exported successfully. All 12 Web delivery/readiness tests passed, including the root-scoped isolation-worker and touch-safe-area contracts. The four content-addressed runtime assets total 41.73 MiB raw; the current release PCK is `index.e9b67e23abdc.pck`.
- The post-export settings containment repair keeps all five tabs, Close, and long Input bindings inside the authored 1080-pixel panel at maximum text scale and with touch-sized targets. Its parser scan, settings-navigation and production-settings probes pass; the rebuilt browser release was visually checked at 2560x1600, 1600x1000, 844x390, and 390x844 with no document overflow.

## Racecraft choice and Academy integrity source pass — 2026-07-16

- **Opt-in fast lines:** RUT, BERM, and PUMP opportunities are now telegraphed before their live window with direction, distance, and alignment feedback. RUT and BERM require deliberate steering toward the live target; PUMP requires the contextual pump technique. Ignoring a line is exactly neutral, while a committed line retains its bounded momentum, stability, and Flow risk/reward.
- **Loop-safe decisions:** Fast-line identity uses a seam-stable track/route/zone key, so closed-spline projection cannot clear or restart a committed choice at the start/finish alias. Both the pre-seam and one-tick post-wrap forms of a beyond-finish preview are suppressed only after the final checkpoint arms the finish; grid staging, early final-lap riding, intermediate laps, and later recurrences remain valid. Competitive signatures advance to racecraft revision 2 so incompatible deterministic ghosts are not compared.
- **Academy authority:** Failed or invalid lessons, DNFs, and DSQs no longer complete onboarding. Academy grading is the sole source of Academy medals and rewards, preventing the generic race settlement from stacking a second payout; a valid non-Academy classified finish and the explicit Freestyle skip remain supported onboarding paths.
- **Presentation verification:** The deterministic rendered Circuit capture now opts into the live integrity tracker. Its staging frame holds the bike at `000 MPH` with no premature fast-line prompt, and its chase frame visibly presents `FAST LINE ◀ RUT · 53m` without HUD clipping.
- **Final validation:** `python .\tools\run_quality_gate.py --full` passed the parser/editor scan, all 57 focused probes, and all five playable activity smokes against the final source state. No Web export, commit, push, or deployment was performed in this source-only continuation; the previously exported V28 browser payload remains unchanged.

## Seamless loop crossing and competitive lifecycle pass — 2026-07-16

- **No start/finish reset:** Closed-loop progress now treats the two spline aliases at the start/finish as the same physical seam. Crossing into a new lap preserves the bike's velocity and legal rejoin state instead of producing a false route-length jump, cut recovery, stop, or respawn.
- **Continuous standings:** Race Pack reconciles sampled chainage with the authoritative lap only inside the seam window, keeps player progress monotonic through projection flicker, bounds every rider to the configured race distance, and pins a finisher to the exact completed distance. The live two-lap Mesa probe preserves `14.0 m/s`, records zero respawns/recoveries/penalties, and finishes at `1670.205842 m` rather than adding a phantom lap.
- **Competition isolation:** Rotating challenge identity is separated from exact build/signature identity so records, boards, ghosts, and replay artifacts cannot bleed between different competition configurations. Invalid CLEAN_RIDE/contact runs cannot overwrite an eligible personal-best ghost.
- **Replay lifecycle:** Abandoned recordings are cancelled on staging/waiting resets, one-sample recordings are rejected, photo mode unwinds safely, and replay results cannot resurrect a dismissed Results panel. Replay moved to contextual gamepad A while preserving custom bindings and migrating only the exact legacy V binding.
- **Final validation:** The curated gate passed all 35 high-risk probes in 245.5 seconds. `python .\tools\run_quality_gate.py --full` then passed the parser/editor scan, all 60 focused probes, and all five playable activity smokes in 582.5 seconds. The activity smokes also reported `prestart=0.000m` on Circuit, Pine, and Mesa. No Web export, commit, push, or deployment was performed in this source-only continuation.

## Last Rider Out adjudication and settlement pass — 2026-07-16

- **Leader-driven elimination:** The first active racer to complete each non-final lap now queues one field-wide elimination round. Resolution happens after the complete NPC field integrates, includes the player and every active opponent, excludes prior terminal riders, and breaks exact progress ties with the same deterministic ordering as classification. Later crossings cannot repeat an already resolved lap.
- **Fair close cutoffs:** A pending production round refreshes the real player's route projection immediately before adjudication. NPC and player positions are therefore equally current at the cutoff without adding another projection query to ordinary physics frames.
- **Real player loss:** The player can now be the last rider out. Elimination cancels personal-best capture, locks the bike, enters Results once, records the elimination lap, leaves time at `-1`, awards `NO_AWARD`, zero rewards, and zero championship points, and classifies surviving opponents instead of incorrectly marking the whole field DNF.
- **Loss presentation and services:** Results explicitly show `LAST RIDER OUT // ELIMINATED` and the elimination lap. Eliminated attempts are valid recorded starts but are rejected from leaderboards, display `LOCAL BOARD // NOT ELIGIBLE`, cannot retain a stale pending/PB board, and cleanly retire run-scoped Ride Director presentation without awarding a finish bonus.
- **Regression contract:** `elimination_race_probe.tscn` covers a stationary player eliminated by an NPC lap crossing, the correct NPC eliminated when the player is ahead, same-lap duplicate suppression, input-order-independent ties, the real Bike/Ghost/RaceController/HUD settlement path, zero awards, survivor classification, locked controls, explicit HUD messaging, and board ineligibility. It is part of the curated quality gate.
- **Validation:** The parser/editor scan, signal audit, focused elimination, competition-visibility, results-navigation, normal full-race lifecycle, race feature, opponent challenge, ghost eligibility, pack launch, and related probes pass. The curated gate passed all 36 high-risk probes and five playable activity smokes in 359.6 seconds; after the final close-cutoff and board-label review refinements, the parser plus all four directly affected probes passed again. No Web export, commit, push, or deployment was performed in this source-only continuation.

## Live input teaching and rebind integrity pass — 2026-07-16

- **One binding authority:** InputRouter now formats keyboard, mouse, gamepad button, trigger, stick, and directional-pair bindings directly from the live InputMap. Stick pairs collapse to LS/RS, D-pad pairs collapse to DPAD, unbound device families remain explicit, and the Settings binding summary is the only surface that intentionally lists every configured device family.
- **Immediate prompt invalidation:** Successful persisted binding application, runtime capture, per-action restore, and restore-all operations publish a centralized binding revision and changed-action notification. HUD and Garage instances that already exist refresh without requiring a device switch or scene reload, including startup after saved controls are applied.
- **Active-device teaching:** Riding hints, contextual Flow guidance, pause help, finish and activity-complete cards, Academy actions, replay/rematch prompts, Garage setup/event/workshop/repair/assist controls, and replay/photo moments now resolve the active device's usable controls. Keyboard-plus-mouse alternatives remain visible together; wrong-device controls are no longer advertised.
- **Stable device intent:** Key and gamepad releases no longer change prompt mode, ordinary stick drift is ignored, and only meaningful stick/trigger motion selects gamepad teaching. Indefinite completion copy is stored semantically and recomposed after a same-mode rebind instead of retaining obsolete keys.
- **Regression contract:** `input_prompt_integrity_probe.tscn` changes throttle, Flow, confirm, and workshop bindings while remaining in keyboard/mouse mode, verifies one batched invalidation updates pre-existing HUD and Garage surfaces, switches to gamepad and rejects keyboard leakage, checks readable mouse/button/axis labels and LS/DPAD collapse, rejects controller drift/release handoff, preserves the other device family, and restores the exact InputMap state. Production settings separately verifies that a successful real capture emits one exact invalidation.
- **Validation:** The parser/editor scan and focused prompt, production-settings, competition, presentation, replay-lifecycle, and settings-navigation probes pass. The curated quality gate passed all 37 high-risk probes and five playable activity smokes in 351.5 seconds. No Web export, commit, push, or deployment was performed in this source-only continuation; the existing browser payload remains unchanged.

## Complete semantic controls and modal ownership pass — 2026-07-16

- **Every production command is configurable:** The remapping inventory now contains 44 exact actions spanning riding, Garage, Workshop, Settings, Results, replay, spectator, and full Photo-mode movement/look. Results row/page/first/last navigation, Settings selection/adjust/page/reset commands, and Photo free-camera translation/rotation no longer bypass InputMap with hardcoded keys.
- **Context-safe reuse:** Binding conflicts use an explicit Ride, Garage, Workshop, Results, Settings, Replay, Photo, and Global context model. Controls such as W/S, A, X, RB, Tab, and L3 can be reused on mutually exclusive screens, while duplicates that can execute together are rejected atomically. Unknown actions and Global actions remain conservatively conflict-safe, and legacy callers retain global conflict behavior.
- **Safe migration and live teaching:** Existing maps receive every missing semantic action without overwriting custom bindings. Exact legacy Replay V and Photo P mappings gain their contextual gamepad defaults only when that addition is conflict-free. Active-device Settings, Results, replay, and Photo prompts resolve the live bindings immediately; Settings summaries retain both keyboard/mouse and gamepad families.
- **Single modal input owner:** Settings suspends Garage, Results, Main, touch, and HUD input beneath it. Replay and Photo suspend Results navigation; Photo also blocks Main riding commands and hides riding touch controls. Escape/B remain immutable Settings/capture safety exits. Safe navigation and adjustment repeat while confirm, reset, close, and first/last jumps stay edge-triggered.
- **Photo/replay integrity:** Photo mode supports keyboard and both controller sticks/triggers, preserves prior bike/camera state, and restores it on every teardown. Paused races, Settings, Photo, and paused replays no longer advance replay recording or playback behind the modal presentation.
- **Regression authority:** Production Settings now asserts the exact ordered 44-action inventory, contexts, defaults, full pairwise conflict matrix, persisted reloads, cross-context acceptance, same-context rejection, legacy migration, and complete InputMap restoration. Settings, Results, replay, and prompt probes exercise real rebound controls, removed-default rejection, prompt invalidation, modal ownership, semantic camera motion, and exact cleanup.
- **Validation:** The parser/editor scan, five directly affected focused probes, closed-loop seam probe, and two-lap continuity probe pass. Mesa crosses the intermediate seam at `14.0 m/s` with zero respawns, automatic recoveries, or penalties and finishes exactly once at `1670.205842 m`. The curated gate passed all 37 high-risk probes and five playable activity smokes in 380.4 seconds. No Web export, commit, push, or deployment was performed in this source-only continuation; the existing browser payload remains unchanged.

## Honest Context Flow refusal feedback pass — 2026-07-16

- **Rejected input is explicit:** An unaffordable Context Flow press now preserves Flow and activation state while presenting the recommended technique, exact cost, and available resource (for example, `NEED 35 FLOW FOR SURGE // 0 AVAILABLE`). The warning remains legible across live racecraft snapshots and briefly pulses the Flow meter.
- **Distinct sensory language:** Failed Flow uses a restrained low-motor haptic tick and a dedicated descending pooled SFX cue. A 280 ms audio limiter prevents repeated presses from stacking the same sound, while successful techniques retain their existing rising cue and stronger haptic identity.
- **Honest camera response:** `FLOW_DENIED` no longer triggers the generic racecraft camera kick, so a resource refusal cannot feel like a successful physical technique.
- **Regression contract:** The real bike/input probe verifies exactly one semantic denial at zero Flow, the `SURGE / 35 / 0` payload, zero resource spend, zero activation, and the refusal haptic. Presentation and audio probes verify warning polarity, meter emphasis, camera suppression, pooled SFX routing, descending cue identity, and anti-spam behavior. These probes are part of the curated gate.
- **Validation:** The parser/editor scan, three directly affected probes, all 39 curated high-risk probes, and Circuit, Pine, Mesa, Freestyle, and Discovery activity smokes pass. No Web export, commit, push, or deployment was performed; the existing browser payload remains unchanged.

## Honest audio mixing and controls pass — 2026-07-16

- **Independent engine mix:** Player, surface-contact, and opponent engine layers now route through a dedicated `Engine` bus. Gameplay cues remain on `SFX` and adaptive stems remain on `Music`, so changing engine level no longer changes impacts, Flow feedback, or other effects.
- **Truthful settings:** Audio exposes four exact categories—Master, Music, Engine, and Effects—and applies each persisted level to its live bus. Zero percent now mutes the selected bus exactly; raising it above zero unmutes it without disturbing another category.
- **Safe schema migration:** Settings schema V4 retains existing Master, Music, Engine, and Effects levels while removing dormant Voice and Crowd preferences that had no corresponding players or buses. Version-3 saves are normalized and rewritten through the verified persistence path.
- **Regression contract:** The dedicated audio-settings probe covers bus creation, distinct Engine/SFX routing, live decibel application, exact mute/unmute, independent category behavior, persistence, rejected dormant keys, and V3 migration. Production Settings asserts the exact ordered labels and independent adjustment rows; event audio verifies real EngineAudio binding and retained pooled-SFX feedback.
- **Validation:** The parser/editor scan, focused settings/audio/migration/accessibility/navigation probes, all 40 curated high-risk probes, and Circuit, Pine, Mesa, Freestyle, and Discovery activity smokes pass. No Web export, commit, push, or deployment was performed; the existing browser payload remains unchanged.

## Live Riding Academy coaching pass — 2026-07-16

- **Mechanics-first instruction:** All eight Academy lessons now carry persistent coaching recipes derived from their real gameplay thresholds, including staged holeshots, rut and slide timing, preload/receiver control, pumps, scrubs, Compose, dabs, clean rejoins, drafting, and slingshots.
- **Live semantic controls:** Coaching resolves the player's current keyboard, gamepad, or touch bindings and refreshes immediately after a rebind or device change. Touch uses the controls actually shown on screen, including one shared `STEER / LEAN` surface instead of duplicated or generic touch directions.
- **Container-driven readability:** The Academy panel sizes from its content container, wraps every instructional line, remains inside the left HUD lane at 175% text scale, and shrinks again when accessibility text is reduced. The existing race view, minimap, timing, speed, Flow, and control lanes remain unobstructed.
- **Regression authority:** The Academy progression probe now validates the exact lesson token/action schema, all eight lessons across three device modes, live binding invalidation, exact touch copy, unresolved-control rejection, viewport containment, line visibility, and scale-down recovery.
- **Validation:** The focused Academy probe passes, 2560×1600 start-grid and chase captures show clean in-race composition, and the curated gate passes all 41 high-risk probes plus Circuit, Pine, Mesa, Freestyle, and Discovery activity smokes in 379.6 seconds. No Web export, commit, push, or deployment was performed; the existing browser payload remains unchanged.

## Durable career payoff and real event unlocks — 2026-07-16

- **Authoritative run payoff:** Each activity snapshots career state at run start and again at the settlement boundary. A DNF therefore retains a sponsor contract already earned on course without admitting rejected result mutations; duplicate, invalid, unclassified, and disqualified outcomes cannot invent payouts or access. Invalid performance telemetry remains in incident/DSQ audit fields but cannot advance holeshot, passing, lap, best-lap, medal, or achievement authority.
- **One event-access rule:** Event availability now flows through one combined Profile-and-catalog predicate. Pine Enduro's intended `80 reputation OR two Quarry clears OR beat Rook` route is real in the Garage and launch flow instead of being accidentally reduced to an AND gate.
- **Results with direction:** The result card adds a container-driven Payout / Career / Next Goal strip, names the highest-value new unlocks and milestones, recommends a measurable next event target, and can focus that event when returning to the Garage. At 175% text scale the classification remains scrollable and selected; touch mode reserves its action band and uses semantic swipe/button guidance.
- **Exact regression:** The progression-payoff probe covers first-win threshold clusters, duplicate and malicious-invalid suppression, retained in-run sponsor credit, Academy first-completion/rematch behavior, racer-versus-total-reputation isolation, Pine's two-clear route, stable ordering, next-event-specific targets, elimination guidance, and keyboard/gamepad/touch Results containment. Reward-integrity and player-metric probes independently defend the same authority boundary.
- **Visual review:** Actual 2560×1440 and 1280×720 keyboard and touch captures remain contained, readable, and action-safe at 175% text scale.
- **Validation:** The parser/editor scan, focused payoff, reward-integrity, results-navigation, full-meta, full-race-lifecycle, and player-metric probes pass. The curated quality gate passes all 42 high-risk probes plus Circuit, Pine, Mesa, Freestyle, and Discovery activity smokes in 386.4 seconds. No Web export, commit, push, or deployment was performed; the existing browser payload remains unchanged.

## Web runtime performance pass — 2026-07-15

- Baseline rendered `CIRCUIT` profile at 1280×720: 1,941 average draw calls, 467 shadow-casting instances, 38 particle systems, 33.48 ms process time, 11.82 ms physics time, and 55.36 average FPS on the local RTX 3090.
- The existing streamed-district architecture and shared engine audio were preserved.
- Startup engine/surface loops and the default 143 BPM Quarry music stems are now baked under `assets/generated/audio`; runtime synthesis remains only as a build tool and for worker-thread event variations.
- Web quality contracts now scale only the 3D scene to 0.67/0.80/0.90, cap Quality at 2× MSAA, disable MSAA for Balanced, shorten directional shadows to 150–220 m, disable them in Performance, force two shadow splits, and budget particles to 35%/62%/84% while leaving the HUD native-resolution.
- Opponent bikes use one coarse shadows-only proxy instead of submitting every articulated part into every cascade. Only the four nearest opponents run roost particles, sharing the existing nearest-four engine-audio decision.
- Clean parser/editor scan and the web-runtime-budget, difficulty/quality, event-audio-identity, and race-pack-launch probes pass.
- The clean rendered `CIRCUIT` after-profile at 1280x720 averaged 1,011 draw calls, 236 shadow-casting instances, 584K primitives, 14.94 ms process time, 13.79 ms physics time, and 59.66 FPS. Compared with the baseline, draw calls fell 48%, shadow casters 49%, rendered primitives 57%, process time 55%, and startup time 69% (7.86 s to 2.41 s). Cleanup reported zero orphaned nodes.
- Release verification complete: the stable quick gate passed 28 focused probes and five activity smokes, and the full gate passed all 54 focused probes plus the same five activity smokes. `0.3.7-racecraft-v27` exported with all 10 Web delivery tests passing; port 8777 serves the V27 header, isolated threaded runtime, and matching immutable audio worklets. The in-app browser reached the garage and live Mesa gameplay at 2560x1600 and 1920x1080, stayed overflow-free at 768x900 and 390x844, and completed a clean fresh launch with zero console warnings or errors. Content-addressed WASM/PCK payloads total 43.7 MB raw and 14.0 MB over gzip.

## Durable settlement authority and final loop gate - 2026-07-16

- **Seamless intermediate laps:** Closed-loop projection reconciles the duplicated start/finish aliases without treating a legal lap crossing as a route-length cut. The focused seam probe checked 825 samples with exact `0.000000 m` closure and a `1.0007 degree` tangent seam. The live two-lap continuity probe carried `14.0 m/s` and angular velocity across the intermediate line with zero respawns, recoveries, penalties, cuts, or motion locks, then produced exactly one result after lap two.
- **Run-bound settlement authority:** Profile-issued one-time race and activity identities now bind the exact event, competitive signature, Academy lesson, rotating challenge, championship round, and managed-weekend context. Exact settled replays are idempotent; fabricated, empty, oversized, stale, cross-mode, cross-lesson, rotated-challenge, wrong-round, and standalone-to-managed mutations are rejected without changing progression.
- **Crash-safe rewards:** Race, Academy, Freestyle, Discovery, sponsor-contract, and feat settlement use save-first transactions with rollback. A `SAVE_FAILED` outcome retains the immutable original payload and retries it before any physical new run, while successful duplicates cannot pay twice. Legacy run fingerprints and V4 Academy migration remain compatible without polluting competitive wins or achievements.
- **Exact retry regressions:** Dedicated settlement probes cover interruption, failed-save rollback, reload dedupe, bounded-ledger eviction, reentrant signal issuance, activity abandonment, generic race retry, Academy lesson identity, and Freestyle-to-Circuit interception. The Academy retry keeps `CONTROL_BASICS` credit attached to its original result even when the live selection has changed to `GATE_DROP`.
- **Terrain query performance:** Pine terrain-profile queries precompute invariant radii, use squared-distance culling, and avoid bounds work for rejected segments. The symmetric benchmark reports zero correctness mismatches, approximately `4.4x` segment-query speedup and `1.2x` context-query speedup; terrain ribbon clearance also passes.
- **Final validation:** `python .\tools\run_quality_gate.py` passed the parser/editor scan, all 44 curated probes, and Circuit, Pine, Mesa, Freestyle, and Discovery activity smokes in 517.5 seconds. The live race smokes report `prestart=0.000m`. The Circuit pace assertion now accounts only for the calculated leader drag caused by its deliberate six-second post-GO idle; production opponent tuning is unchanged. `git diff --check` is clean. No Web export, commit, push, or deployment was performed in this source-only pass.

## Flow-aware opponent challenge rebalance - 2026-07-17

- **Much faster competitive fields:** Standard leader times improved from `121.6 s` to `99.6 s` at Circuit, `203.8 s` to `169.2 s` at Pine, and `148.8 s` to `122.2 s` at Mesa. Competitive pace now scales more sharply by difficulty, accelerates harder off the gate, and uses stronger close attack, draft, late-race pressure, and bounded comeback authority.
- **Flow requires race-winning execution:** The deterministic Circuit pressure benchmark puts a strong `0.710x`-gold Flow run in P9 while an elite `0.670x`-gold run earns P1. The chasing field retains `0.88 m/s` of reserve against the simulated player instead of surrendering once Flow creates a gap.
- **Fair pressure, not finish-line theft:** Reduced leader drag lets the field sustain its earned pace, while the existing final-lap catch-up fade still removes director assistance before the finish. Academy keeps its teaching pace and previous pressure caps. The racecraft signature advanced so records and ghosts from the earlier field model are not silently compared with the new balance.
- **Live course proof:** Circuit opponents reached `15.99 m/s` against the smoke player's `15.14 m/s`, Pine reached `16.41 m/s` against `15.32 m/s`, and Mesa reached `17.75 m/s` against `15.06 m/s`. All live smokes retained `prestart=0.000m`, valid routes, checkpoints, dirt, replay, and reset behavior.
- **Regression coverage:** The opponent probe now runs 16 deterministic simulations, including actual player-progress Flow pressure and replay identity. The elimination scenario no longer relies on a fixed pace-dependent lead margin. The parser/editor scan, all 44 curated probes, and Circuit, Pine, Mesa, Freestyle, and Discovery activity smokes pass in `453.1 seconds`. No Web export, commit, push, or deployment was performed in this source-only pass.

## V28 production Web release and interruption-safe packaging - 2026-07-17

- **Fresh source authority:** `python .\tools\run_quality_gate.py` passed the parser/editor scan, all 44 curated high-risk probes, and Circuit, Pine, Mesa, Freestyle, and Discovery activity smokes in `406.9 seconds`. The live race smokes retained exact `prestart=0.000m`, authoritative route/checkpoint/replay/reset coverage, and the rebalanced opponent pace.
- **Release-pipeline recovery:** The first V28 export exposed a transient Windows handle failure after the old builder had already moved all four unversioned runtime assets. `build_web_release.py` now writes the inner shell, outer wrapper, and manifest through retrying atomic sibling-file replacements; copies content-addressed assets before publishing metadata; recognizes one complete hashed bundle after an interrupted stamp; and removes stale/unversioned assets only after the release metadata is valid.
- **Exact regression:** `WebReleaseBuilderTests.test_interrupted_stamp_recovers_one_complete_hashed_bundle` recreates the half-stamped state and proves `--skip-export` can recover the exact WASM, PCK, and paired audio-worklet bundle. The complete Web delivery suite now passes all 13 tests.
- **Fresh end-to-end export:** A subsequent unsuppressed `python .\tools\build_web_release.py` completed in `9.1 seconds`, proving the normal Godot export, content addressing, host handshake, leak check, manifest stamp, and delivery tests work from a fresh export. The release is `0.3.8-racecraft-v28`; payloads are `index.9778df3b5d0e.wasm` (`38,814,086` bytes), `index.391c80de61fb.pck` (`5,054,168` bytes), and matching `index.9778df3b5d0e` audio worklets.
- **Live browser proof:** The production server on `127.0.0.1:8777` returns `X-Riding-Dirty-Build: racecraft-v28`, same-origin COOP, require-corp COEP, CORP, and update-safe no-store headers. Playwright captured the actual 1280x720 garage and live Academy start gate with readable setup/event/workshop/milestone information, coaching, sponsor objective, minimap, countdown, speed/Flow instruments, and semantic control prompts. No console errors were captured. Evidence is under `artifacts/web-game-baseline/shot-0.png` and `artifacts/web-game-live/shot-0.png`.
- **Exhaustive gate cleanup:** The first `--full` discovery run found two legacy fixtures that the curated set had not kept aligned with V28. The retention audit now explicitly enforces restrained leader drag below the old `0.65 m/s` cap, comeback pressure more than `2.5x` stronger, neutral practice/qualifying behavior, bounded corrections, and exact pre-finish fade. The reward-integrity fixture now binds its managed Red Mesa weekend identity at run start, matching the production settlement authority used by newer probes, while retaining duplicate, DSQ audit, no-invalid-reward, championship, cap, and achievement checks.
- **Final production evidence:** `python .\tools\run_quality_gate.py --full` passed the parser/editor scan, every one of the 66 discovered focused probes, and all five activity smokes in `576.4 seconds`. The final Web suite passes 13/13 tests, `git diff --check` is clean, and the V28 production server remains live on `127.0.0.1:8777`.

## Physics-query allocation hardening - 2026-07-17

- **Confirmed hot-path fix:** Chase-camera obstruction checks and airborne landing-alignment checks now reuse one `PhysicsRayQueryParameters3D` object each instead of constructing a query object and exclusion array on every applicable physics tick. The camera refreshes its cached exclusion only when the tracked collision RID changes; the bike installs its stable self-exclusion once during `_ready()`.
- **Behavioral proof:** The presentation contract retains the exact `78.0-92.0` camera range and passes its framing, pause, hint, and feedback checks. The complete bike-dynamics simulation retains planted turning, steering response, landing behavior, receiver assist (`15.4deg` baseline versus `12.7deg` assisted touchdown), camera speed feedback, and physical tuning contracts. That real simulation now also records the original pooled-query instance IDs and proves both remain identical after the complete airborne, obstruction, landing, recovery, and tuning run (`stable=true`).
- **Audit proof:** The deterministic signal audit reports no risky anonymous signal lambdas. Security review found no process execution, expression evaluation, unsafe variant/object decoding, or player-controlled dynamic resource loading. Browser JavaScript interpolation is JSON-encoded, and release profiling prints remain explicitly environment-gated.
- **Release verification:** `python .\tools\run_quality_gate.py` passed the parser/editor scan, all 44 curated probes, and all five playable activity smokes in `497.4 seconds`. A fresh `python .\tools\build_web_release.py` completed in `9.9 seconds`; all 13 Web delivery/recovery tests pass. The unchanged WASM runtime remains `index.9778df3b5d0e.wasm`, while the current source pack is `index.a7bb66d00b0b.pck` (`5,054,472` bytes).
- **Live browser proof:** The production server continues to return the V28 build and cross-origin-isolation headers. The required Playwright client loaded the rebuilt package to the 1280x720 Garage without console errors; the inspected capture at `artifacts/web-game-post-query-pool/shot-0.png` is contained, readable, and stylistically consistent.

## Frame-consistent animation and pause-safe activity clocks - 2026-07-17

- **Frame-rate-independent rider motion:** Rider torso response now converts its authored response frequency into a delta-based exponential weight, and wobble uses a simulation-driven phase instead of wall-clock ticks. The deterministic animation probe agrees at 30, 60, and 120 Hz for rider pitch, torso pitch, ride phase, and wobble phase, with bounded roll tolerance.
- **Fair activity timing:** Race, Freestyle, and Discovery now share `SimulationClock`, a deterministic microsecond accumulator with fractional carry that advances only while gameplay physics runs. Pausing no longer charges wall time to an official result. Same-frame synthetic race finishes retain one authoritative physics tick so valid zero-duration fixture results cannot bypass profile settlement rules.
- **Lifecycle regressions:** The pause-clock probe accumulates exact ten-second runs at 30/60/120 Hz and proves a real 180 ms wall wait does not advance a detached Freestyle controller. The replay lifecycle probe verifies official race elapsed time is unchanged through a twelve-frame pause and resumes by simulation time only. Full-race lifecycle, reputation pacing, bike dynamics, Freestyle, and Discovery checks also pass.
- **Exhaustive validation:** `python .\tools\run_quality_gate.py --full` passed the parser/editor scan, all 68 discovered focused probes, and all five playable activity smokes in `645.9 seconds`. The deterministic signal audit and `git diff --check` are clean.
- **Fresh Web release:** The rebuilt `0.3.8-racecraft-v28` package uses `index.92a633b2679e.pck` (`5,056,868` bytes; SHA-256 `92a633b2679e74de59bbf8227f0ef7d57fd0afd1df7b2700b915c12fe2197826`) with the unchanged `index.9778df3b5d0e.wasm` runtime. All 13 delivery/recovery tests pass; the live server returns the expected V28 and cross-origin-isolation headers. The required browser client reached the real 1280x720 Garage without console errors, and `artifacts/web-game-post-timestep/shot-0.png` was visually inspected for containment and readability.
- **Remaining acceptance work:** Automation establishes technical correctness and representative coverage, not subjective taste. Representative external player sessions remain the acceptance gate for consistent fun, memorability, audiovisual feel, onboarding comfort, and long-session difficulty/reward pacing.

## Semantic interface audio feedback - 2026-07-17

- **Confirmed feedback gap:** Core riding already had engine, surface, racecraft, camera, HUD, and haptic response, but accepted Garage, Workshop, Settings, Results, replay/photo, pause, reset/restart, and spectator actions were mostly silent. Their visible state changes now share one cross-scene interface-feedback channel instead of each screen constructing its own player or sound.
- **Four honest meanings:** Navigation is a restrained short upward tick, confirmation is a clearer rising cue, cancellation descends, and denial uses a lower descending refusal. All four are procedurally authored, use the existing pooled `SFX`/Effects route, respect exact Effects mute, and suppress identical repeats inside 32 ms without merging different meanings. Rejected replay/photo, unavailable weekend, locked purchase, and ineffective repair actions cannot sound like success.
- **Accepted-action wiring:** Garage setup/event movement, assist, Workshop open/close/category/item/apply, repairs, purchases and ride launch; Settings selection/page/value/reset/rebind/open/close; Results selection and mouse scrolling; pause/resume, return to Garage, reset/restart, replay start/stop, photo entry/exit, and spectator changes all emit feedback after their action path accepts or refuses the request. Manual race reset now returns acceptance so finished/results input can request denial rather than confirmation.
- **Deterministic proof:** Event audio verifies four distinct frequency/identity contracts, EventBus-to-pooled-audio routing, source context retention, and anti-spam (`4` accepted / `1` suppressed). The real Garage probe observes 11 feedback events and one accepted Academy launch; Settings observes 17 mouse/keyboard/gamepad/touch events; Results observes seven keyboard/gamepad/mouse events. Existing audio routing continues to prove Engine/Effects isolation and exact mute.
- **Release gates:** The curated gate passed the parser/editor scan, all 47 high-risk probes, and five activity smokes in `427.2 seconds`. The exhaustive gate passed all 69 discovered probes and the same five smokes in `639.4 seconds`.
- **Fresh Web release:** `python .\tools\build_web_release.py` completed in `9.8 seconds`; all 13 delivery/recovery tests pass. The new source pack is `index.0637b2c21330.pck` (`5,061,076` bytes; SHA-256 `0637b2c213308969484a9a68640d9f1db31a008acb38c5982817cb66138d7054`) beside the unchanged `index.9778df3b5d0e.wasm` runtime. The live server returns the V28/isolation headers.
- **Browser proof:** The required Web client waited for engine readiness, then accepted real Arrow Left Garage navigation from Balanced to Trail Kit and updated stats, price, install action, and prompts without clipping or console errors (`artifacts/web-game-interface-nav-ready/shot-1.png`). A separate accepted Enter launch reached live Academy gameplay with the bike, course, coaching, sponsor target, minimap, timer, racecraft, speed, Flow, and reset feedback visible and contained (`artifacts/web-game-interface-ride/shot-1.png`).
- **Remaining acceptance work:** The semantic and routing contracts prove consistency, not subjective mix taste. Representative listening/play sessions should still tune loudness, timbre, fatigue, and perceived responsiveness against real speakers, headphones, keyboards, and controllers.

## Lesson-scoped Academy onboarding - 2026-07-17

- **Progressive disclosure:** Each of the eight Academy lessons now owns a normalized presentation contract. The first two foundation lessons suppress generic racecraft; later lessons expose only their authored focus (`CORNERING`, `JUMPING`, `FAST_LINE`, `AIR_FLOW`, `RECOVERY`, or `PASSING`). Only Air Control displays the Flow meter.
- **No ordinary-event contamination:** Academy no longer starts or displays daily modifiers, sponsor contracts, or line-chain scoring, and it no longer duplicates its focused coach with the generic all-controls wall. Ride Director applies `STANDARD` instead of Tailwind/Flow Surge/Loose Dirt during lessons, so the change is mechanical as well as cosmetic. Circuit, Pine, Mesa, Freestyle, and Discovery retain their full event layers.
- **Deterministic proof:** The Academy presentation probe verifies all eight normalized scopes, exact event/session projection, three device modes, 175% text containment, focused HUD behavior, and restoration of line, sponsor, modifier, racecraft, and Flow feedback in an ordinary Circuit event. The solo Academy runtime smoke now validates route, surface, checkpoint, field, replay, and reset behavior without demanding a nonexistent opponent, and Academy is part of both recurring release gates.
- **Release gates:** The curated gate passed the parser/editor scan, all 47 high-risk probes, and six activity smokes in `452.2 seconds`. After one non-reproducing process exit from a persistence probe that passed both immediately before and in isolation, the clean exhaustive rerun passed all 69 discovered probes and the same six smokes in `729.5 seconds`.
- **Fresh Web release:** `python .\tools\build_web_release.py` completed in `15.2 seconds`; all 13 delivery/recovery tests pass. The current source pack is `index.1cefcb864e36.pck` (`5,066,660` bytes; SHA-256 `1cefcb864e367f6f14f670bc2defe35cb3f0fcaf14f4298cebaabcbafdb251c7`) beside the unchanged `index.9778df3b5d0e.wasm` runtime.
- **Browser proof:** The required Web client launched the real first Academy lesson from the production wrapper. The inspected 1280x720 frame at `artifacts/web-game-academy-focus/shot-1.png` contains the lesson title, focused device-aware coach, two objectives, timer/gate progress, minimap, and speed, with no sponsor contract, daily modifier, line score, generic racecraft cue, Flow meter, or all-controls wall.
- **Remaining acceptance work:** The instructional hierarchy is technically and visually verified, but representative new-player sessions should still judge learning comfort, wording, objective pacing, and whether each lesson feels rewarding rather than merely clear.

## Forgiving Academy recovery and idle-safe coaching - 2026-07-17

- **Reading time is safe:** The integrity tracker now arms stuck detection only after the bike has moved at least `2.5 m` from its spawn or reached meaningful horizontal speed. A new deterministic fixture holds the bike motionless beyond the stuck grace period and proves no stuck incident, reset, penalty, or accumulated stuck time; the live Academy smoke independently waits six seconds after GO and verifies `0` resets, `0` penalty, and an unarmed detector.
- **Recovery teaches instead of punishing:** Academy resets still count against the authored lesson objective, but carry zero elapsed-time penalty. Automatic and manual recoveries name the safe rejoin, show the live objective budget (for example, `RESET 1 / 2`), and encourage the player to keep going. Wrong-way, off-course, cut, and stuck feedback uses contextual coaching instead of the generic red accumulated-penalty banner.
- **Competitive integrity is unchanged:** Circuit and other ordinary events retain the existing `+2.0 s` reset penalty and negative penalty presentation. The automatic-recovery probe exercises real tipped and world-fall competitive resets alongside a real Academy recovery, proving the mode-specific split rather than weakening the shared integrity system.
- **Release gates:** The curated gate passed the parser/editor scan, all 47 high-risk probes, and six activity smokes in `473.4 seconds`. The exhaustive gate passed all 69 discovered probes and the same six smokes in `707 seconds`.
- **Fresh Web release:** `python .\tools\build_web_release.py` completed successfully and all 13 delivery/recovery tests pass. The current source pack is `index.8add4f08b02e.pck` (`5,672,352` bytes; SHA-256 `8add4f08b02ec235bfac7d425b1b5973fc8ee78c42d9c17a2385717877cf52ed`) beside the unchanged `index.9778df3b5d0e.wasm` runtime.
- **Browser proof:** The required Web client launched the production wrapper and held the first Academy lesson beyond the former idle-reset window. The inspected 1280x720 frame at `artifacts/web-game-academy-forgiving/shot-1.png` shows the stationary rider, two focused objectives, one contextual control cue, and no red penalty or recovery banner; no console-error report was produced.
- **Remaining acceptance work:** The automated and browser contracts establish fairness and clarity, while representative new-player sessions should still judge whether recovery wording feels supportive and whether the two-reset lesson budget is comfortable in practice.

## Saved bike strategies and clean-profile starter balance - 2026-07-18

- **Reusable strategy builds:** The Workshop now has a sixth `BUILD` category with three named slots. Each slot saves and restores the selected bike, Trail/Balanced/Attack kit, eligible class, compatible installed parts, physical tune, and livery. Loading deliberately preserves the live bike's condition and odometer, so strategy convenience cannot become a durability exploit.
- **Safe persistent authority:** Profile schema 6 bounds storage to Build A/B/C, normalizes names and tunes, removes unavailable bikes and incompatible or unowned parts, migrates schema-5 profiles without inventing data, and includes saved builds in verified desktop/browser persistence. Save and load use the existing save-first transaction boundary; injected write failures prove full rollback and no partial slot creation.
- **Clear, configurable feedback:** The Garage Workshop summary accepts mouse clicks with visible hover focus. Existing configurable category, item, confirm, and close actions operate all six build commands; explicit saved, loaded, empty, overwrite, and condition-preserved copy is paired with the shared navigate/confirm/denied Effects feedback. The real interface probe now covers save, mutation, load, and denied empty-slot behavior.
- **Honest starter-bike testing:** Every activity smoke now resets the profile after disabling persistence, eliminating accidental dependence on a developer's upgraded local bike. That exposed a stock-bike opening-pace gap. Balanced drive authority moved from `1200 N` to `1300 N` in both physics and Garage projection: Circuit now measures `14.549 m/s` against a `15.992 m/s` leader, and Mesa measures `14.882 m/s` against `17.749 m/s`, inside their authored fair-start margins while Trail and Attack retain their tradeoffs.
- **Release gates:** The curated gate passed the parser/editor scan, all 48 high-risk probes, and six clean-profile activity smokes in `572.2 seconds`. The exhaustive gate passed all 70 discovered probes and the same six smokes in `741.8 seconds`. Saved-build coverage includes JSON round trip, migration, tamper normalization, condition preservation, bounded slots, transactional rollback, UI action semantics, and production input mapping.
- **Fresh Web release:** `python .\tools\build_web_release.py` completed successfully and all 13 delivery/recovery tests pass. The current source pack is `index.8c553ce92729.pck` (`5,681,520` bytes; SHA-256 `8c553ce92729c9981113836d514b4042513f525fcc876303056255820ab1e4fd`) beside `index.9778df3b5d0e.wasm`.
- **Browser proof:** The required Web client used actual mouse and keyboard input in one live browser context to save Balanced Build A, switch to Holeshot, and reload Build A. The inspected 1280x720 frame at `artifacts/web-game-saved-build-roundtrip/shot-0.png` shows `BUILD A LOADED`, complete configuration details, the condition-preservation promise, readable six-category navigation, and no clipping. The production wrapper also settled into full-width live Academy gameplay at `artifacts/web-game-saved-build-wrapper-settled/shot-1.png`.
- **Remaining acceptance work:** Automation proves integrity and interaction, not whether players naturally name and revisit builds or understand when each strategy pays off. Representative sessions should evaluate naming, comparison clarity, strategic variety, starter acceleration feel, and long-career motivation.

## GitHub Pages production publication - 2026-07-20

- **Public release:** GitHub Pages deployment `aee9b8d5e6149bffaedd044ce7268a517554f14a` published the verified V28 Web bundle from `gh-pages` at `https://willtran87.github.io/project-riding-dirty/`. GitHub reported the exact deployment commit as built with HTTPS enforcement.
- **Payload proof:** The public manifest reports `0.3.8-racecraft-v28` and `index.8c553ce92729.pck`. The downloaded public pack is exactly `5,681,520` bytes and its SHA-256 is `8c553ce92729c9981113836d514b4042513f525fcc876303056255820ab1e4fd`, matching the release manifest; the deployed inner page references that exact content-addressed pack.
- **Live browser proof:** The required Playwright client loaded the public wrapper, accepted Start and Enter, and reached full-width Academy gameplay without a console-error report. The inspected 1280x720 capture at `artifacts/github-pages-release/shot-1.png` contains the lesson HUD, two objectives, stage coaching, minimap, countdown, rider, speed display, and semantic controls without clipping.

## First-event startup selection - 2026-07-21

- **Level 1 opens first:** A fresh profile no longer forces the Garage cursor to Academy near the end of the event list. Startup now selects Quarry Trail (`CIRCUIT`, event 1/18); Academy remains visible as optional recommended onboarding instead of replacing the initial level selection. Active race weekends still focus their correct continuation event.
- **Regression proof:** The real Garage interface probe asserts a fresh `show_garage()` selects `CIRCUIT` before continuing through Workshop mouse access, saved-build save/load/denial, configurable event navigation, and an explicit Academy launch. The Godot parser/editor scan and focused probe pass.
- **Fresh Web release:** The rebuilt V28 pack is `index.8ed00f9e633a.pck` (`5,681,584` bytes; SHA-256 `8ed00f9e633a2f36439f07f314ca13bddf2c81807e8eb6552dd5e36e3a3071b0`), and all 13 Web delivery/recovery tests pass.
- **Browser proof:** The inspected startup capture at `artifacts/web-game-first-event-start/shot-0.png` visibly reads `QUARRY TRAIL // EVENT 01 / 18 // SPRINT`. Confirm launches that exact activity; the settled frame at `artifacts/web-game-first-event-ride-settled/shot-1.png` shows the Quarry Trail title, 12-rider field, event minimap, gate 1/18, and start sequence without clipping.
- **GitHub Pages update:** Deployment `971d69cdacc4b8840d9a901cc98234fa03fa45dd` built successfully from `gh-pages`. The public manifest, downloaded pack size/hash, and inner-page reference all match `index.8ed00f9e633a.pck`; the inspected public capture at `artifacts/github-pages-first-event-start/shot-0.png` visibly opens on Quarry Trail event 1/18.

## Honest first-run weekend shortcut - 2026-07-21

- **Clear first action:** A pristine first-run profile now selects Quarry Trail event 1/18 without advertising an unavailable Red Mesa Practice continuation action. Red Mesa remains visible as future Tour context, and only the shortcut is suppressed; active Main and LCQ weekends retain their production Continue action and exact session dispatch.
- **Deterministic proof:** The real Garage interface probe seeds the production default weekend and asserts its first-run snapshot is unavailable, has empty action copy, and hides the action label while preserving the event-1 cursor. The managed-weekend progression probe separately proves real Main and LCQ continuation remain visible and launch correctly. The parser/editor scan, signal audit, both focused probes, and the curated gate of 48 high-risk probes plus six activity smokes pass; the curated run completed in `446.7 seconds`.
- **Fresh Web release:** `python .\tools\build_web_release.py` completed successfully and all 13 delivery/recovery tests pass. The source pack is `index.675ce34300d1.pck` (`5,681,872` bytes; SHA-256 `675ce34300d1337bb4671110d276f8a74a177bc0f4ac945ee7dbb139597b3125`) beside `index.9778df3b5d0e.wasm`.
- **Browser proof:** The inspected startup frame at `artifacts/web-game-first-event-clean-start/shot-0.png` shows Quarry Trail event 1/18, the normal Enter Ride prompt, and no premature red Continue shortcut. The settled frame at `artifacts/web-game-first-event-clean-ride/shot-1.png` shows the full 12-rider Quarry Trail start sequence, minimap, gate 1/18, and readable HUD without clipping.
- **GitHub Pages update:** Deployment `c7c8b2b259460f1888833bcb0dcbe06c20e04700` built successfully from `gh-pages`. The public manifest reports `index.675ce34300d1.pck`; its downloaded `5,681,872` bytes and SHA-256 match the release manifest, and the deployed inner page references that exact pack. The inspected public capture at `artifacts/github-pages-first-event-clean-start/shot-0.png` confirms the clean event-1 startup on the production URL.
- **Remaining acceptance work:** This removes a concrete onboarding contradiction. Representative first-time players should still validate whether the future-tour summary and optional Academy recommendation establish a clear, comfortable choice.

## Coherent first-route Garage progression - 2026-07-21

- **One coherent first screen:** A pristine profile now leads with `QUARRY TRAIL // FIRST EVENT READY` instead of a Red Mesa eyebrow. The competition briefing reinforces `FIRST ROUTE // FINISH TO SET A PB`, and the Workshop summary identifies Quarry event 1, the immediate finish/rep/PB goal, and the next actionable path: clear two Quarry events to reach Pine, then Red Mesa. Browsing another activity changes the eyebrow to its actual Quarry, Pine, Red Mesa, or Academy context.
- **Preserved career authority:** The first-run presentation predicate requires active onboarding, zero cleared events, and either no weekend or a pristine Practice weekend with no results. A genuine Main or LCQ therefore retains its championship summary, visible Continue action, exact activity mapping, and dispatch even if legacy profile flags are stale.
- **Deterministic proof:** The real Garage interface probe asserts the fresh event, context, first-route objective, two-clear path, absence of later-district briefing copy, hidden pristine shortcut, saved-build round trip, input feedback, and accepted launch. The managed-weekend progression probe separately verifies real Main/LCQ presentation and dispatch. The exact final source passes the parser/editor scan, signal audit, all 48 curated high-risk probes, and six clean-profile activity smokes in `453.3 seconds`.
- **Fresh Web release:** `python .\tools\build_web_release.py` passes all 13 delivery/recovery tests. The local release pack is `index.0668a4d905f9.pck` (`5,682,800` bytes; SHA-256 `0668a4d905f932eabae2561808c9452bb6ceef5fb6fb1932194c266d885f18ab`) beside `index.9778df3b5d0e.wasm`.
- **Visual proof:** The inspected 1280×720 capture at `artifacts/web-game-first-route-progression-final/shot-0.png` contains the complete Quarry-first hierarchy and progression summary without clipping. `artifacts/web-game-first-route-progression-ride/shot-1.png` proves Enter reaches the full Quarry start gate. Additional 2560×1600 and 844×390 browser passes keep the stage anchored, avoid horizontal overflow, and report no console errors; the high-resolution persisted profile also confirms progressed Red Mesa context remains event-aware.
- **Release status and next acceptance:** This continuation is rebuilt and locally verified but intentionally not committed, pushed, or published without a new explicit publication request. Representative first-time sessions should still judge whether the Quarry/Pine/Red Mesa path is motivating and whether the optional Academy choice is discoverable without feeling mandatory.

## Event-aware setup strategy guidance - 2026-07-21

- **Existing depth is now discoverable:** Every authored activity has an advisory kit, tune, event focus, and concise rationale drawn from its actual terrain and format. Quarry starts with the readable Balanced baseline; rough Pine stages surface Trail/Enduro control; gate races surface Attack/Holeshot drive; rhythm and freestyle formats surface Attack/Rhythm jump support. The Garage compares the browsed kit and active tune against that plan and clearly labels a match, partial match, or alternate build.
- **Choice remains player-owned:** Guidance never purchases, equips, tunes, repairs, or changes a saved build. Rotating challenges derive their kit from the authoritative competitive setup rule and their tune from the active surface/format, so the presentation cannot recommend an invalid run signature. The longer rationale remains data-driven for future comparison UI while the first screen uses one compact focus line instead of overwhelming a new player.
- **Deterministic proof:** The Garage interface probe verifies the fresh Balanced/Balanced Quarry match, Trail/Enduro Pine tradeoff, Attack/Rhythm Mesa tradeoff, visible plan copy, and unchanged active-build signature after browsing. The exact final source passes the parser/editor scan, deterministic signal audit, all 48 curated high-risk probes, and six clean-profile activity smokes in `517.3 seconds`.
- **Fresh Web release:** `python .\tools\build_web_release.py` passes all 13 delivery/recovery tests. The local release pack is `index.ddc8a1e9fdcf.pck` (`5,686,944` bytes; SHA-256 `ddc8a1e9fdcf4b9073861d06ff67a0a9fb0821ff7bbbcb3981d71d57fc4cb681`) beside the unchanged `index.9778df3b5d0e.wasm` runtime.
- **Visual proof:** The inspected 1280x720 production frame at `artifacts/web-game-event-strategy-final/shot-0.png` shows the full first-event hierarchy, exact live values, and the compact `KIT BALANCED + TUNE BALANCED // LINE CONTROL // MATCH` plan without overlap, clipping, truncation, or a console-error report. The required browser path also entered the current Quarry session; the deterministic activity smoke provides the complete route, field, checkpoint, replay, reset, and prestart assertions.
- **Release status and next acceptance:** This strategy pass is rebuilt and locally verified but intentionally not committed, pushed, or published without a new explicit publication request. Representative players should still judge whether the suggestions encourage useful experimentation, whether alternate builds remain viable, and whether the recommendations become more meaningful over a long career.

## Event-linked saved-build memory - 2026-07-21

- **Strategies are memorable after saving:** A current build that fully matches the selected event plan now receives a short event/focus identity such as `QUARRY TRAIL // LINE CONTROL`. Non-matching configurations retain their honest bike/tune name, so the game never relabels an experimental build as recommended.
- **Comparison before commitment:** Both Save and Load views show how the actual configuration fits the currently selected event. Full, kit-only, tune-only, and alternate states name the missing kit or tune directly; changing the browsed event recomputes the comparison without equipping, purchasing, tuning, or mutating the saved slot.
- **Persistence remains authoritative:** No profile schema or serialized field changed. The existing three-slot, save-first transaction still owns all mutations, preserves live condition and odometer, filters tampered bikes/parts/slots, clamps tunes, and rolls back failed saves and loads. The focused Garage probe verifies Quarry naming, full-match review, Pine alternate-plan review, and signature immutability; the saved-build persistence probe independently retains all schema, tamper, rollback, and wear contracts.
- **Exact-source validation:** The parser/editor scan, deterministic signal audit, all 48 curated high-risk probes, and six clean-profile activity smokes pass in `472.4 seconds`. `git diff --check` remains clean.
- **Fresh Web release:** All 13 delivery/recovery tests pass. The local release pack is `index.d376ecf35b91.pck` (`5,688,048` bytes; SHA-256 `d376ecf35b91fca58aa4f5b3ebca7141dcdbe21cccfa8a139457a16e0e644a57`) beside the unchanged `index.9778df3b5d0e.wasm` runtime.
- **Browser proof:** The required Web client exercised the real inner Godot canvas through Workshop category navigation, Save Build A, selection change, and Load Build A. The inspected 1280x720 frame at `artifacts/web-game-event-build-roundtrip/shot-0.png` shows `QUARRY TRAIL // LINE CONTROL`, `FULL PLAN MATCH`, the complete saved configuration, explicit condition preservation, and successful load feedback without overlap or a console-error report.
- **Release status and next acceptance:** This improvement is rebuilt and locally verified but intentionally not committed, pushed, or published without a fresh explicit request. Representative players should still judge whether three event-focused slots are enough, whether the naming is memorable after time away, and whether comparisons encourage experimentation rather than formulaic optimization.

## Minimum-path setup affordability - 2026-07-21

- **A real progression gap is closed:** Two distinct first-time Quarry FINISHER activities grant exactly `$375` each and unlock Pine, but the recommended Trail kit formerly cost `$800`. Trail now costs `$750`, so the lowest valid no-medal route funds the plan without assuming a sponsor contract, replay grind, or better-than-finisher result.
- **Attack remains honestly balanced:** Race cash components now have public pure policy helpers shared by live settlement and tests. The minimum post-Trail classified route through Pine, four-rider Mesa Practice, and solo Qualifying yields `$1,550` before the first Attack-planned Heat. The existing `$1,500` Attack price therefore already has a `$50` buffer and was deliberately preserved.
- **Spending remains a choice:** Repairs, parts, and bikes still consume cash and can delay a kit purchase; nothing is auto-granted or auto-equipped. The new read-only setup purchase snapshot exposes ownership, price, affordability, and exact shortfall, and the Garage plan line presents `MATCH`, `KIT MATCH`, `KIT READY $...`, or `KIT $... AWAY` without changing profile or build state.
- **Coherent unlock language:** Pine's locked-event copy now names the authoritative two-Quarry-event gate instead of the stale Red Mesa wording. The focused progression, Garage, race-reputation, and atomic activity-settlement probes all pass. The exact final source also passes the parser/editor scan, all 48 curated high-risk probes, and six clean-profile activity smokes in `484 seconds`; the deterministic signal audit and `git diff --check` are clean.
- **Fresh Web release and visual proof:** All 13 delivery/recovery tests pass. The local release pack is `index.6608ed0cc02b.pck` (`5,689,280` bytes; SHA-256 `6608ed0cc02ba0e96ef80cb4ce5250ff218651f5cb8d370cbdcfc8bcbaaa0082`) beside the unchanged `index.9778df3b5d0e.wasm` runtime. The required browser client reached Pine from the fresh Quarry-first Garage; the inspected 1280x720 frame at `artifacts/web-game-progression-affordability-copy-final/shot-1.png` shows `KIT $750 AWAY`, the exact Quarry unlock condition, and no clipping or console-error report.
- **Release status and next acceptance:** This economy correction is rebuilt and locally verified but intentionally not committed, pushed, or published without a fresh explicit request. Representative progression sessions should still test whether optional repair and Workshop spending create interesting tradeoffs without making the next recommended kit feel grindy.

## Production export-boundary hardening - 2026-07-21

- **Confirmed release leak:** Binary inspection found `output/web-game/shot-0.png`, `shot-1.png`, `shot-2.png`, and their import metadata inside the production PCK. The local `output/` tree contains six files totaling `849,700` bytes; these browser captures are development evidence, not game content.
- **Explicit package boundary:** The Web export preset now excludes both `output/*` and the untracked local `Godot/*` tooling tree alongside artifacts, tools, Web outputs, recovered projects, and test resources. Neither local directory was modified. The release verifier independently rejects `output/web-game` or `Godot/` markers in a generated PCK, so removing the preset filter cannot silently ship them later.
- **Deterministic regression:** A fourteenth Web delivery test parses the real export preset and requires every local-only directory boundary. The complete release build passes all `14/14` delivery, recovery, accessibility, isolation, and packaging tests.
- **Measured improvement:** The production PCK fell from `5,689,280` to `5,085,828` bytes, saving `603,452` bytes (`10.6%`). Binary scanning finds no `output/`, `Godot/`, `artifacts/`, `tools/`, or `features/testing/` paths in the final pack. The current release is `index.b01734451e36.pck`, SHA-256 `b01734451e36f5aced81ccb3c7423efc5f984cc9fca73915cbb004eadd9f22c5`, beside the unchanged `index.9778df3b5d0e.wasm` runtime.
- **Live browser proof:** The production server returns the exact new PCK with a `5,085,828` content length, immutable caching, same-origin COOP, require-corp COEP, and same-origin CORP. The required browser client traversed the real Garage and Quarry transition into a live twelve-rider race; the inspected 1280x720 gameplay frame at `artifacts/web-game-export-hygiene-gameplay/shot-2.png` shows the active gate, field, HUD, sponsor goal, minimap, racecraft feedback, speed/Flow instruments, and controls without a console-error report.
- **Release status and next acceptance:** Publication was deferred at this checkpoint until the explicit request recorded below. Subjective player acceptance remains necessary for fun, audiovisual taste, and long-session pacing.

## First-route strategy GitHub Pages publication - 2026-07-21

- **Source release:** Commit `b34817b8ba64b8ad42cc0eb1516f2d7e045b2f9a` publishes the coherent first-route presentation, event-aware setup and saved-build guidance, minimum-path kit affordability, and guarded production export on `codex/racecraft-v27`.
- **Pages deployment:** GitHub Pages deployment commit `475bed7f7810652ebc1b23fcd30153c8f47d219b` built successfully from `gh-pages` with HTTPS enforcement at `https://willtran87.github.io/project-riding-dirty/`.
- **Public payload proof:** The public manifest reports `index.b01734451e36.pck`; its downloaded `5,085,828` bytes and SHA-256 `b01734451e36f5aced81ccb3c7423efc5f984cc9fca73915cbb004eadd9f22c5` match the release manifest, and the public inner page references that exact pack. The former pack returns 404.
- **Public browser proof:** The required browser client launched the production wrapper without a console-error artifact. The inspected 1280x720 frame at `artifacts/github-pages-first-route-strategy-live/shot-1.png` shows `QUARRY TRAIL // FIRST EVENT READY`, `EVENT 01 / 18`, the first-route objective and Tour path, and the Balanced/Balanced Quarry strategy match.
- **Remaining acceptance:** Representative players should still judge long-session fun, strategy variety, audiovisual taste, learning comfort, and progression pacing; those subjective release qualities cannot be closed by automated verification alone.

## Authored sponsor relationship arcs - 2026-07-21

- **Three coherent programs:** Dustline Works owns race precision, Wildbrush Outpost owns terrain reading and legal secret routes, and Sundown Static owns expressive chains. Each program derives an independent Prospect, Signed, Factory, or Icon relationship from durable completed-contract identities, so existing profiles gain the feature without a schema migration or parallel save authority.
- **Fair mastery and rewards:** Prospect preserves the published `$350 / 35 REP / 1 Style Token` baseline. Signed, Factory, and Icon pay `$425/40`, `$500/50`, and `$600/60` while clean-jump and chain targets rise only after completed work. Route objectives remain capped at the two authored Discovery/Pine gates. One activity still pays at most once per day, and the existing save-first Profile transaction, bounded identity ledger, rollback, and retry path remain authoritative.
- **Clear, themed feedback:** The Garage previews the selected event's sponsor and relationship rank. First-run copy now consistently says `QUARRY TRAIL READY // ACADEMY OPTIONAL`; it no longer gives optional coaching stronger priority than event one. The race HUD names the sponsor, rank, exact objective, progress, cash, reputation, and token reward, while a threshold-crossing completion announces the new relationship rank and uses the existing contract SFX.
- **Deterministic proof:** The new sponsor probe verifies three independent histories, Prospect/Signed/Factory pacing, exact economy totals, the two-route cap, live Ride Director settlement, Signed promotion messaging, Style Tokens, and duplicate rejection. The Garage and Academy/HUD probes verify pre-event identity and exact live reward copy. Circuit, Freestyle, and Discovery smokes cover all three sponsor families; the complete curated gate passes all 49 high-risk probes and six clean-profile activity smokes in `465.1 seconds`. After the final first-run wording refinement, the parser/editor scan and the three directly affected probes pass again.
- **Fresh Web candidate:** All 14 delivery/recovery tests pass. The exact release pack is `index.8894ebdc86a2.pck` (`5,092,484` bytes; SHA-256 `8894ebdc86a26e7bf2027682dd27ea25ff564f9ae125f8899211865cecde316c`) beside the unchanged `index.9778df3b5d0e.wasm`; local-only marker scanning remains clean.
- **Browser proof:** The required browser client exercised the exact production pack from Garage through transition into live Quarry gameplay. The inspected 1280x720 frames at `artifacts/web-game-sponsor-relationships-final/shot-0.png` and `shot-2.png` show the coherent first-route hierarchy, `DUSTLINE PROSPECT` preview, optional Academy copy, and unclipped in-race objective/reward line without a console-error artifact.
- **Release status and next acceptance:** This sponsor pass is rebuilt and locally verified but not committed, pushed, or published without a new explicit publication request. Representative long-session play should judge rank pacing, sponsor memorability, objective variety, and whether the rising rewards feel motivating without becoming grind-driven.

## Sponsor identity transition and audio polish - 2026-07-21

- **Identity before the gate:** Every ordinary event transition now presents the selected program's name, current relationship rank, exact rides to the next rank, and authored tagline. Dustline uses amber race precision, Wildbrush uses trail green terrain reading, and Sundown uses a rose expressive-line accent; the district accent, event objective, medal target, and rules remain independently readable.
- **Distinct completion feedback:** The existing generic contract success cue now resolves to one of three restrained procedural stings from the shared SFX voice pool. Dustline, Wildbrush, and Sundown have distinct frequency contours, durations, and harmonics, while unknown legacy titles retain the generic fallback. The Effects setting, shared routing, and one-cue-per-contract guard remain authoritative.
- **Accessible and isolated:** The sponsor line is text-first rather than color-dependent, and Reduced Motion preserves the complete static briefing without a tween. Academy continues to expose no sponsor identity. The transition catalog probe now validates all 18 production activities, exact sponsor assignment, rank/progress/tagline composition, Academy isolation, and three distinct accents; it joins the recurring curated gate.
- **Visual iteration:** The first browser capture exposed a Web-only title clip after an enlarged transition band shifted during resize. The proven band geometry was restored and its six information lines were tightened. The repeated exact-pack capture at `artifacts/web-game-sponsor-identity-transition-final/shot-1.png` shows the complete centered hierarchy without overlap or clipping; `shot-2.png` reaches the matching live Dustline race HUD, and no console-error artifact was produced.
- **Release evidence:** The parser/editor scan, all 50 curated high-risk probes, and six clean-profile activity smokes pass in `482.9 seconds`. The focused sponsor, transition, audio, reduced-motion, and difficulty probes also pass directly. All 14 Web delivery/recovery tests pass.
- **Fresh Web candidate:** The exact local release is `index.fd2a5ab98df1.pck` (`5,096,548` bytes; SHA-256 `fd2a5ab98df19fc5fd5985e83004050fb81706f64a6a738e1150d52780d9b105`) beside the unchanged `index.9778df3b5d0e.wasm`; its manifest and file hash agree and the local-only marker scan remains clean.
- **Release status and next acceptance:** This identity pass remains local and has not been committed, pushed, or published without a new explicit publication request. Representative listening and long-session play should still judge whether the three stings are pleasant across speakers/headphones and whether the repeated taglines make each sponsor memorable without adding briefing fatigue.

## Quarry first-ride loading optimization - 2026-07-21

- **Measured bottleneck:** Opt-in production timing isolates Quarry's original `8.349 s` first activity preparation to a `7.232 s` track load. Terrain generation (`3.088 s`) and course dressing (`2.654 s`) account for roughly 79% of that work because both repeatedly search the authored terrain profiles.
- **Exact hot-path cache:** Each terrain profile now builds packed start, delta, and squared-length arrays once, removing repeated Dictionary lookups and temporary Vector construction from every coarse-segment query. An attempted outward-ring spatial index was rejected after it regressed track loading to `9.267 s`; Quarry's long route favors the compact linear arrays.
- **Repeat improvement:** Two stable production repeats load the complete track in `5.457 s` and `5.328 s`, about 25% below the original `7.232 s`; complete activity preparation is approximately `6.64 s` and `6.45 s`, down from `8.35 s`. Runtime profiling remains opt-in through `RIDING_DIRTY_PROFILE_ACTIVITY=1` and `RIDING_DIRTY_PROFILE_QUARRY=1`, so release play has no logging overhead.
- **Deterministic proof:** The new Quarry terrain-cache probe compares 2,400 full-terrain and near-route points against the original exhaustive segment scan. It reports zero cache-shape, segment, or full-context mismatches and approximately `5.0x` faster segment selection. Terrain ribbon clearance, geometry clipping, route authority, physical route traversability, and ground-cover clearance all pass unchanged.
- **Release evidence:** The parser/editor scan, all 51 curated high-risk probes, and six clean-profile activity smokes pass in `465.6 seconds`. The fresh release passes all 14 Web delivery/recovery tests. The exact candidate is `index.b1884076fd7a.pck` (`5,098,964` bytes; SHA-256 `b1884076fd7a1bf3e309035e403a1949ef910254876493ce31dba23c9432c181`) beside the unchanged `index.9778df3b5d0e.wasm`.
- **Browser proof:** The required browser client exercised the exact served candidate at 1280x720. `artifacts/web-game-quarry-load-final/shot-0.png` shows the fresh Garage on Quarry event 1, `shot-1.png` shows the complete sponsor transition, and `shot-2.png` reaches the live twelve-rider start with readable HUD; no console-error artifact was produced.
- **Release status and next acceptance:** This loading pass remains local and has not been committed, pushed, or published without a new explicit publication request. Representative players should still judge transition pacing on lower-end Web devices and the broader subjective fun, learning, audiovisual, and long-session balance goals.

## Sponsor and first-ride GitHub Pages publication - 2026-07-21

- **Source release:** Commit `de2ad828e50e054ee9392cb24e6ded630852872d` publishes the three sponsor relationship arcs, transition/audio identity, exact first-route presentation, and packed Quarry terrain-query optimization on `codex/racecraft-v27`.
- **Pages deployment:** GitHub Pages deployment `7cd56f01d97985e514b28841dd6860352c8618e6` built successfully from `gh-pages` with HTTPS enforcement at `https://willtran87.github.io/project-riding-dirty/`.
- **Public payload proof:** The public manifest and inner page reference `index.b1884076fd7a.pck`. An independent download is exactly `5,098,964` bytes with SHA-256 `b1884076fd7a1bf3e309035e403a1949ef910254876493ce31dba23c9432c181`, matching the committed release manifest.
- **Public browser proof:** The production wrapper loads without console errors, starts the real engine through its cross-origin-isolation service worker, opens on Quarry Trail event 1/18 with Dustline Prospect identity, and traverses the sponsor transition into a live twelve-rider Quarry race with the exact sponsor objective and reward line.
- **Release gates:** The exact published source passed the parser/editor scan, all 51 curated high-risk probes, six clean-profile activity smokes in `465.6 seconds`, and all 14 Web delivery/recovery tests. Representative players should still judge long-session fun, sponsor memorability, transition pacing on lower-end Web devices, audiovisual taste, and progression balance.

## Earned opponent Flow and tactical boosts - 2026-07-23

- **Opponents now build and spend Flow:** Every competitive rider owns a bounded `0-100` Flow reserve. Direction-gated drafting, clean/mastered skill lines, advantageous corner lines, and clean physical landings earn the resource; Academy remains boost-free. Spending matches the player's `35 Flow / 1.15 s` cadence instead of adding an invisible permanent pace scalar.
- **Tactical, readable use:** Riders boost only after the launch, while supported and in control, and when a pass, defense, strong draft, close attack, late-race push, or player chase justifies the spend. Unsafe committed jump approaches are excluded. The pack rider visibly tucks into an attack pose and throws a sharper sustained roost while its engine naturally rises with speed, reusing the existing two-emitter Web budget.
- **Fair difficulty progression:** Relaxed earns at `0.68x` and outputs `0.72x`; Standard uses the authored baseline; Expert earns at `1.18x` and outputs `1.10x`. Across Circuit, Pine, and Mesa, boost activations rise strictly by tier (`24/46/54`, `55/88/100`, and `29/53/66`) while every existing leader/median window and adjacent difficulty-separation contract remains valid.
- **Player pressure is materially stronger:** In the production-traffic Circuit samples, a sustained strong Flow pace finishes P9 and the elite `0.670x-gold` pace is challenged to P2 rather than an uncontested P1. Standard opponents peak at the bounded `2.30 m/s` boost output, earn the reserve through the shared racecraft rules, and produce deterministic full-field results.
- **Release validation:** The parser/editor scan, complete three-track opponent matrix, deterministic replay, starter/traffic/build-match/defense scenarios, all 51 curated high-risk probes, and six production activity smokes pass; the curated run completed in `430.9 seconds`. `git diff --check` remains clean.
- **Fresh Web candidate:** All 14 delivery/recovery tests pass. The exact local pack is `index.d53c48f2c046.pck` (`5,103,684` bytes; SHA-256 `d53c48f2c046d04bbfdbcfdb9d8a0dfd57188106339dbabcec18a65986413ffd`) beside the unchanged `index.9778df3b5d0e.wasm` runtime.
- **Browser proof:** The required browser client loads the exact candidate, opens Quarry event 1, and reaches the complete twelve-rider gate without a console-error artifact. Inspected 1280x720 frames at `artifacts/web-game-opponent-flow-final/shot-0.png` and `shot-4.png` show a clean Garage and live race HUD/field without clipping. The deterministic full-race probe remains the authoritative boost-activation proof because the screenshot harness advances only a few gameplay frames.
- **Release status and next acceptance:** This opponent pass is locally rebuilt and verified but has not been committed, pushed, or published without a new explicit release request. Representative full races should still judge whether the new attack frequency feels exciting on each difficulty and whether the visual tuck/roost is sufficiently legible amid dense traffic.

## Opponent Flow GitHub Pages publication - 2026-07-23

- **Source release:** Commit `aeeeb3619d9fa6d3c1673286d8e0daa0e2cc8a07` publishes earned opponent Flow, tactical boosts, difficulty scaling, pack-bike attack presentation, deterministic balance coverage, and the verified Web bundle on `codex/racecraft-v27`.
- **Pages deployment:** GitHub Pages deployment `73a8ad1cbf6b360e9fc204407f56f08fce662966` built successfully from `gh-pages` with HTTPS enforcement at `https://willtran87.github.io/project-riding-dirty/`.
- **Public payload proof:** The public manifest and inner page reference `index.d53c48f2c046.pck`. An independent public download is exactly `5,103,684` bytes with SHA-256 `d53c48f2c046d04bbfdbcfdb9d8a0dfd57188106339dbabcec18a65986413ffd`, matching the committed release manifest; the previous `index.b1884076fd7a.pck` returns 404.
- **Public browser proof:** The required browser client starts the production wrapper, opens on Quarry Trail event 1/18, and reaches the complete twelve-rider live gate without a console-error artifact. Inspected 1280x720 captures at `artifacts/github-pages-opponent-flow-live/shot-0.png` and `shot-2.png` show the clean Garage and unclipped live race HUD/field.
- **Release gates:** The published source passed the parser/editor scan, all 51 curated high-risk probes, six production activity smokes in `430.9 seconds`, the complete three-track opponent matrix, all 14 Web delivery/recovery tests, exact public payload verification, and production-browser startup. Representative full races should still judge the attack frequency and boost readability amid dense traffic.

## Physical automatic/manual transmission - 2026-07-26

- **Real drivetrain play:** The player bike now has one authoritative five-speed transmission shared by rear-wheel force, engine RPM/audio, and the HUD. Automatic remains the approachable default with load-aware upshifts, downshift hysteresis, restore/replay gear synchronization, and a brief physical torque cut; Manual preserves the complete RPM/torque curve, requires deliberate shifts, and loses drive when a gear is held beyond redline.
- **Complete input parity:** Shift Down and Shift Up are semantic, remappable Ride actions (`Q/E`, gamepad D-pad Down/Up). The settings inventory expands from 44 to 46 exact actions without same-context collisions. Manual touch play adds two accessible 112-authored-pixel shift targets; Automatic hides them to keep onboarding uncluttered.
- **Persistent, live configuration:** Settings schema V5 adds `AUTOMATIC` / `MANUAL` on the Ride page, sanitizes legacy or invalid data back to Automatic, saves atomically through the existing primary/backup store, and live-applies the mode to bike, HUD prompts, and touch controls. The speed cluster shows the authoritative mode and gear, briefly changes color on a shift, and teaches shift bindings only in Manual.
- **Responsive feedback and preserved handling:** Physics, audio, HUD, and haptics consume the same shift event. Automatic retains the authored baseline drive envelope between shifts, while discontinuous checkpoint/replay velocity restores select the matching gear without applying a duplicate torque cut. The complete bike dynamics suite passes with `0.983` jump-face speed retention, `32.00 m` jump distance, stable steering/slip, and unchanged air-control/recovery contracts.
- **Deterministic release proof:** The new transmission probe covers five automatic gears, hysteresis, anti-hunting shift cuts, manual requests, over-rev drive loss, suggested gear, signals, and authoritative engine-audio state. The parser/editor scan, all 52 curated high-risk probes, and six production activity smokes pass in `407.6 seconds`; all 14 Web delivery/recovery tests pass and `git diff --check` is clean.
- **Fresh Web candidate:** The exact local pack is `index.aa9c1ef573cc.pck` (`5,113,872` bytes; SHA-256 `aa9c1ef573cce9011f78d2736b52e023cfd4abae9c3c376c64d9d0b31930d9c9`) beside the unchanged `index.9778df3b5d0e.wasm` runtime. Port `8777` returns the matching V28 build header, exact content length, immutable asset caching, and the required same-origin COOP / require-corp COEP headers.
- **Browser proof:** The required browser client completed a clean rebuilt-package Garage run without a console-error artifact. A second traversal produced a real 1280x720 Quarry race capture at `artifacts/web-game-transmission-live/shot-1.png`; the inspected frame shows the readable `AUTO // G1` cluster, field, minimap, sponsor objective, and full race HUD without overlap. Two live captures completed without an error artifact before the outer third-iteration timeout.
- **Release status and next acceptance:** This drivetrain pass is rebuilt and locally verified but has not been committed, pushed, or published without a new explicit release request. Representative riders should still judge manual shift timing, automatic cadence, audio feel, touch placement, and long-race fatigue across classes and devices.

## Persistent multi-view riding cameras - 2026-07-26

- **Five useful riding views:** Chase, Close Chase, Helmet, First Person, and Handlebar are authored as distinct framing/response profiles over the existing predictive terrain-aware camera. Replay and Photo Mode continue to supply cinematic and free-camera presentation. Cycling is seamless during a ride and never interrupts physics, timing, opponents, or controls.
- **Player-owned comfort:** Settings schema V6 persists the selected view plus independently bounded camera distance (`75-135%`), height (`75-125%`), and stiffness (`60-150%`). FOV, impact/shake, Reduced Motion, and collision shortening remain compatible. The Garage and replay temporarily use safe Chase framing without overwriting the rider's preferred view, which returns on the next active ride.
- **Complete input and feedback parity:** `B`, gamepad Right Stick Click, and a 112-authored-pixel touch Camera target share one remappable Ride action, bringing the exact semantic inventory to 47 actions. Accepted changes save atomically, emit semantic interface audio, and present a clear `CAMERA // ...` HUD confirmation in every activity; a failed settings write restores the prior view rather than leaving memory and disk inconsistent.
- **Visual defect caught and fixed:** The first real First Person browser capture revealed that its optical point sat inside the opaque procedural rider mesh. Helmet, First Person, and Handlebar optical points now sit just ahead of their modeled bodywork at distinct heights. Repeated exact-package captures at `artifacts/web-game-camera-close-ready/shot-0.png`, `artifacts/web-game-camera-first-person-final/shot-0.png`, and `artifacts/web-game-camera-handlebar-final/shot-0.png` show unobstructed course horizons and the complete HUD with no console/page-error artifact.
- **Deterministic production proof:** The new camera probe verifies five distinct finite profiles, cycle wrap, personalization bounds, persistence/reload, Settings ordering/application, Garage override restoration, and a live active-race camera command. Production settings, touch, presentation, Reduced Motion, input-prompt, transmission, and complete bike-dynamics probes pass; default Chase retains its `78-92` degree envelope, stable pooled obstruction query, `0.983` jump-speed retention, and `32.00 m` jump distance.
- **Exact final release evidence:** The parser/editor scan, all 53 curated high-risk probes, and six production activity smokes pass against the final source in `412.5 seconds`. All 14 Web delivery/recovery tests pass. The exact local pack is `index.e0f86274f464.pck` (`5,121,024` bytes; SHA-256 `e0f86274f464876af95817609b550ef54504e7c4868c86b5015547f6ad430dd7`) beside the unchanged `index.9778df3b5d0e.wasm`; port `8777` retains the V28 and cross-origin-isolation headers.
- **Release status and next acceptance:** The transmission and camera milestones remain local, uncommitted, unpushed, and unpublished without a fresh explicit release request. Representative riders should still judge each view's comfort at speed and over jumps, tune the default cockpit heights on different displays, and assess motion fatigue with and without Reduced Motion.

## Individual physical riding assists - 2026-07-26

- **Five independent supports:** Steering, Braking, Landing, Traction, and Rider Balance are separately adjustable from `0-100%` in `5%` steps. Assisted, Sport, and Pro provide coherent quick presets, while any individual change becomes an exact Custom configuration. Each channel affects only its matching physical behavior: bounded yaw/stability support, slip-triggered brake-pressure release, touchdown alignment, excess rear-slip drive trimming, or upright/low-speed balance.
- **Persistent and recoverable:** Profile schema V7 stores the exact five-channel configuration, expands every legacy broad assist mode deterministically, and preserves hundredth-level values. Preset and individual changes use the existing save-first transaction with rollback on failure, so memory and disk cannot silently diverge.
- **Clear configuration and live identity:** Settings now has a dedicated six-row Assists page with one Quick Preset row and all five channels. Garage shows the selected preset or Custom plus the active-channel count; the live speed cluster shows the effective configuration. Mid-event edits are saved for the next activity instead of changing current physics invisibly, and equalized challenges explicitly disclose and apply their forced Sport or Pro configuration.
- **Competitive integrity:** Ordinary run signatures now include every exact assist value, while equalized challenge signatures retain their authored standard/limited rules. Racecraft signature version 4 separates these physics-affecting records from older laps.
- **Deterministic proof:** The new individual-assists probe covers all channels and presets, V7 legacy migration, exact Custom round-tripping, failed-save rollback, isolated physical modifiers, Settings inventory, HUD presentation, and signature separation. The parser/editor scan, all 54 curated high-risk probes, and six clean-profile production activity smokes pass against the final source in `417 seconds`; the complete bike-dynamics suite still passes its strict landing, handling, `0.983` jump-speed-retention, and `32.00 m` jump-distance contracts.
- **Fresh Web candidate:** All 14 delivery/recovery tests pass. The exact local pack is `index.8c81118cdd0f.pck` (`5,129,860` bytes; SHA-256 `8c81118cdd0f8e036eb0806228de923a44488e98355df24fd3dd20d3ce4f3ec5`) beside the unchanged `index.9778df3b5d0e.wasm` runtime. Port `8777` returns the matching `racecraft-v28` header, exact content length, immutable caching, and required cross-origin-isolation headers.
- **Browser proof:** A fresh required-client run opens on Quarry event 1 with `ASSIST SPORT (5/5)` at `artifacts/web-game-individual-assists-start/shot-0.png`. Direct production-browser inspection verifies the complete six-row Assists page and a live race using `ASSIST CUSTOM // 5 / 5` at `artifacts/web-game-individual-assists-settings/shot-0.png` and `artifacts/web-game-individual-assists-live/shot-0.png`; no runtime errors were reported.
- **Release status and next acceptance:** This assist milestone is rebuilt and locally verified but remains uncommitted, unpushed, and unpublished without a fresh explicit release request. Representative players should still judge the five defaults and progression across controllers, keyboards, touch devices, skill levels, and long sessions.

## Manual shifting Academy lesson - 2026-07-26

- **Progressive drivetrain onboarding:** Academy now contains a ninth optional lesson, Manual Shift Rhythm, directly after Gate Drop. It temporarily forces Manual without overwriting the rider's saved preference, teaches device-correct Shift Up and Shift Down prompts, grades three clean accepted shifts plus bounded over-rev time, and awards `$700 / 7 REP` on the first pass.
- **Authoritative and fair grading:** A modular tracker consumes accepted transmission snapshots rather than raw input, deduplicates shift revisions, judges the captured shift-time RPM instead of the post-shift value, and accumulates over-rev exposure from simulation delta. Automatic shifts cannot satisfy the lesson, and live coaching responds to gear, RPM, redline, and suggested direction.
- **Run and record integrity:** Every activity locks its effective transmission at composition. Settings changes made during a ride are saved for the next event, while the lesson stays Manual and restores the latest preference on exit. Competitive signature version 5 includes the actual transmission mode, separating Automatic and Manual boards, ghosts, and records.
- **Readable presentation:** The live Academy card exposes exact Clean Shifts and Overrev Seconds objectives and a contextual gear/RPM prompt on keyboard, gamepad, and touch. Its compact-height calculation now respects the real viewport, and all nine lessons fit at `175%` text scale without clipping.
- **Deterministic release proof:** The parser/editor scan, all 55 curated high-risk probes, and six clean-profile production activity smokes pass against the final source in `422.1 seconds`. Focused transmission, Academy, progression, settings, career, competitive, presentation, touch, assists, and bike-dynamics checks pass; `git diff --check` remains clean.
- **Fresh Web candidate:** All 14 delivery/recovery tests pass. The exact local pack is `index.8cc817305e84.pck` (`5,137,788` bytes; SHA-256 `8cc817305e843fd484e3e6c01c31d15d90b53f9042d27dfcf5db943d8029ea6`) beside the unchanged `index.9778df3b5d0e.wasm` runtime.
- **Browser proof:** The required fresh-profile browser client loaded the exact pack without a console-error artifact. The inspected 1280x720 frame at `artifacts/web-manual-academy-game/shot-0.png` confirms the clean Quarry-first Garage, `EVENT 01 / 18`, and `ACADEMY 0/9`; the complete lesson matrix and live manual coaching remain covered by deterministic presentation probes.
- **Release status and next acceptance:** The transmission, camera, individual-assist, and Academy milestones are ready for the explicitly requested commit, push, and GitHub Pages publication. Representative riders should still judge shift-window feel, lesson clarity, cockpit comfort, assist defaults, and long-session fatigue across input methods.

## Advanced riding controls GitHub Pages publication - 2026-07-26

- **Source release:** Commit `c7c32ebc069ae21725d64773241908379af724e9` publishes the physical automatic/manual drivetrain, five persistent riding cameras, five individual physical assists, Manual Shift Rhythm Academy lesson, record-integrity updates, deterministic probes, and content-addressed Web build on `codex/racecraft-v27`.
- **Pages deployment:** GitHub Pages deployment commit `2d0a636792279c6db60979665ff1830ba50b379f` and workflow run `30195273500` completed successfully from `gh-pages` with HTTPS enforcement at `https://willtran87.github.io/project-riding-dirty/`.
- **Public payload proof:** The public manifest and inner page reference `index.8cc817305e84.pck`. An independent public download is exactly `5,137,788` bytes with SHA-256 `8cc817305e843fd484e3e6c01c31d15d90b53f9042d27dfcf5db943d8029ea6c`, matching the committed release manifest; the former `index.d53c48f2c046.pck` returns 404.
- **Public browser proof:** The required browser client starts the production wrapper through the isolation service worker without a console-error artifact. The inspected 1280x720 frame at `artifacts/github-pages-advanced-controls-wrapper-live/shot-0.png` shows the clean Quarry-first Garage, event 1/18, Academy 0/9, and complete readable first-route hierarchy.
- **Release gates:** The published source passed the parser/editor scan, all 55 curated high-risk probes, six production activity smokes in `422.1 seconds`, all 14 Web delivery/recovery tests, exact public payload verification, and production-browser startup. Representative players should still judge manual-shift feel, camera comfort, assist tuning, Academy clarity, and long-session fun across devices.

## Independent control-response sensitivity - 2026-07-26

- **Four independent response controls:** Ride Settings now separates Steering Sensitivity, Rider Lean Sensitivity, and Air Control Sensitivity, while Camera adds Photo Look Sensitivity. Neutral `1.00` defaults preserve the authored handling exactly; lean, air, and camera values are bounded to `0.50-1.50`, with the existing broader steering range retained.
- **Physical separation, not cosmetic sliders:** Ground steering and rider lean use their own response paths, while airborne roll, yaw, pitch, weight shift, scrub, whip, and rider pose use Air Control sensitivity. Photo Mode yaw and pitch consume the camera value. A deterministic `60%` input sample proves isolated outputs of `0.30` steering, `0.90` lean, and `0.45` air response.
- **Reliable migration and fair run locking:** Settings schema V7 migrates older saves to neutral values and atomically round-trips all new preferences. Every activity freezes the exact seven-value physics response contract at composition; mid-event edits save successfully but clearly apply to the next event, preventing invisible handling changes during a signed run.
- **Competitive identity:** Racecraft signature version 6 includes steering, throttle, and brake deadzones plus steering sensitivity, rider-lean sensitivity, air-control sensitivity, and steering response curve. Records, boards, and ghosts therefore separate physics-affecting response configurations, while camera look remains an unsigned comfort preference.
- **Deterministic release proof:** The new control-response probe covers defaults/migration, bounds, verified disk round-trip, isolated response math, activity locking, next-event activation, competitive separation, and real Photo Mode rotation. The parser/editor scan, all 56 curated high-risk probes, and six production activity smokes pass in `428.6 seconds`; focused bike dynamics retain `0.983` jump-speed retention and `32.00 m` distance. `git diff --check` is clean.
- **Fresh Web candidate:** All 14 delivery/recovery tests pass. The exact local pack is `index.820fbb80e8b7.pck` (`5,142,604` bytes; SHA-256 `820fbb80e8b73d4edf76951ac30fe717946a49c30a57a5cfbd893b403ea4a279`) beside the unchanged `index.9778df3b5d0e.wasm` runtime.
- **Browser proof:** The required fresh-profile client loads the exact pack into Quarry event 1/18 at `artifacts/web-control-response-launch/shot-0.png`. Direct 1280x720 inspection at `artifacts/web-control-response-settings/ride-settings.png` and `camera-settings.png` confirms the new rows are readable and stylistically consistent; the browser reported no console or page errors.
- **Release status and next acceptance:** This sensitivity milestone is rebuilt and locally verified but remains uncommitted, unpushed, and unpublished without a fresh explicit release request. Representative riders should still judge each range and default across keyboard, controller, touch, different cameras, jump styles, and long sessions.

## Hold / Toggle preload accessibility and lap-seam reliability - 2026-07-26

- **Accessible sustained input:** Ride Settings schema V8 adds `PRELOAD INPUT: HOLD / TOGGLE`. Hold preserves the exact authored press-charge/release-hop behavior. Toggle arms on the first keyboard, gamepad, or touch press, remains charged after the physical release, and hops on the second press. Activity teardown, control lock, respawn, and behavior changes explicitly clear the latch so it cannot leak into another run.
- **Clear, device-correct feedback:** Toggle touch controls say `PRELOAD TAP` and remain visibly active while armed. The live HUD confirms `PRELOAD ARMED // TAP <binding> TO RELEASE` and `PRELOAD RELEASED` with semantic confirmation/cancel audio. Generic Ride prompts and the Preload & Landing Academy lesson teach the selected behavior on keyboard, gamepad, and touch without duplicating controls.
- **Fair records and persistence:** Legacy saves migrate to Hold, invalid values sanitize safely, and verified primary/backup persistence round-trips the exact selection. Every activity locks the full eight-value control-response contract at composition; a mid-run edit is saved for the next event. Competitive control identity now ends in `PMH` or `PMT`, so the different input semantics cannot share records or ghosts.
- **Closed-loop stability found by the release gate:** A timing-sensitive 10 Hz pack projection could briefly add a phantom lap when the finish trigger advanced before the prior approach sample entered the narrow seam window. The existing closed-loop alias reconciliation now covers the complete sampled start/finish approach. Three consecutive focused two-lap runs and the full gate preserve monotonic classification with no false penalty, respawn, or result corruption.
- **Deterministic release proof:** The parser/editor scan, all 56 curated high-risk probes, and all six production activity smokes pass against the final source in `428.9 seconds`. Focused response, production settings, touch, Academy, bike-dynamics, competitive, presentation, and repeated lap-continuity contracts pass; `git diff --check` is clean.
- **Fresh Web candidate:** All 14 delivery/recovery tests pass. The exact package is `index.46c826851ada.pck` (`5,148,816` bytes; SHA-256 `46c826851ada948cca69d42858c96c7fa6794ed4b0388b2d2368089dd99801d8`) beside the unchanged `index.9778df3b5d0e.wasm` runtime.
- **Browser proof:** The required client opens on Quarry event 1/18 at `artifacts/web-preload-toggle-launch/shot-0.png`. Direct 1280x720 production inspection confirms the readable Hold and Toggle setting at `artifacts/web-preload-toggle-settings/ride-hold.png` and `ride-toggle.png`, plus the live armed state at `artifacts/web-preload-toggle-live-final/preload-armed.png`; all captures report zero console or page errors.
- **Release status and next acceptance:** This milestone is ready for the explicitly requested source commit, branch push, and GitHub Pages republication. Representative riders should still judge whether Toggle reduces fatigue across keyboards, controllers, and touch devices without making jump timing less intentional.

## Advanced response and preload-toggle GitHub Pages publication - 2026-07-26

- **Source release:** Commit `331b11c` publishes independent steering, rider-lean, air-control, and Photo Mode sensitivities; Hold/Toggle preload accessibility; run-locked competitive identity; device-correct feedback and coaching; the closed-loop lap-seam correction; deterministic coverage; and the verified Web bundle on `codex/racecraft-v27`.
- **Pages deployment:** GitHub Pages commit `95e859bd5c52a42f586c8aeaae4e2452204d387c` and workflow run `30197459886` completed successfully from `gh-pages` with HTTPS enforcement at `https://willtran87.github.io/project-riding-dirty/`.
- **Public payload proof:** The public manifest and inner page reference `index.46c826851ada.pck`. An independent public download is exactly `5,148,816` bytes with SHA-256 `46c826851ada948cca69d42858c96c7fa6794ed4b0388b2d2368089dd99801d8`, matching the committed release manifest; the former `index.8cc817305e84.pck` returns 404.
- **Public browser proof:** The required client reaches the production wrapper at `artifacts/github-pages-preload-toggle-live/shot-0.png`. Clicking Start the Tour boots the isolated Godot game into the clean Quarry-first Garage at `artifacts/github-pages-preload-toggle-wrapper-game/game-live.png` with no console or page errors.
- **Release gates:** The published source passed the parser/editor scan, all 56 curated high-risk probes, six production activity smokes in `428.9 seconds`, three additional consecutive lap-continuity runs, all 14 Web delivery/recovery tests, exact public payload verification, and production-wrapper browser startup. Representative players should still judge response ranges and Toggle timing across devices and long sessions.

## Stock Workshop bike Test Rides - 2026-07-26

- **Try before buying:** The Workshop Bike tab now exposes a distinct `TEST RIDE STOCK // NO PURCHASE` action for every catalog bike, including reputation-locked and unaffordable machines. Keyboard uses the contextual `C` action, gamepad uses `Y`, and touch relabels the existing contextual button to `TEST RIDE` only while the Workshop is open.
- **Physical, representative previews:** A modular Test Ride policy builds a full-condition, stock version of the selected bike and launches a solo one-lap Quarry practice. The production browser proves the locked Rook 450 becomes the live physics bike, reports `activity/event/format=TEST_RIDE`, and uses a one-rider field; its stock runtime is deterministically distinct from the Tyke baseline.
- **Economically inert by construction:** Test Ride never purchases or selects the bike, spends cash, changes the rider's setup, applies wear, creates a trusted profile run, awards medals/PBs/records/championship points, or settles rewards. Sponsor contracts, relationship progress, feats, line rewards, and daily physics modifiers are disabled at the Ride Director policy boundary. Result payloads are explicitly stripped to zero rewards and `NO_AWARD`.
- **Clear lifecycle feedback:** Returning to the Garage restores the owned active bike, exact build, condition, cosmetics, and setup. The Garage displays `TEST RIDE COMPLETE // ROOK 450 // NO PURCHASE, WEAR, OR RECORDS`, and browser-readable state returns from `ROOK_450 / test_ride=true` to `TYKE_125 / test_ride=false`.
- **Deterministic proof:** The new Test Ride probe covers locked-bike selection, no profile mutation, full-condition stock construction, distinct physical output, solo practice configuration, stripped settlement, inert Ride Director policy, keyboard/gamepad context, and touch captions. The parser/editor scan, all 62 curated release probes, and six production activity smokes pass in `460.1 seconds`; after the final ghost/PB boundary guard, the parser plus Test Ride, ghost-eligibility, and race-feature contracts pass again against the exact final source. The affected settings, touch, HUD, Academy, presentation, and browser-state probes also pass directly.
- **Production Web proof:** All 17 delivery, recovery, accessibility, isolation, text-state, and packaging tests pass. After the final pre-settlement ghost/PB guard, the exact local release is `index.cdbadd673595.pck` (`5,204,272` bytes; SHA-256 `cdbadd673595568c259934ec4523f70d96e8b2622ace600c625bba237820f446`) beside `index.9778df3b5d0e.wasm`. The required browser client and a direct production flow traversed Garage → Workshop → locked Rook 450 → Test Ride → Garage with zero console/page errors and visually confirmed the clean practice HUD.
- **Release status and next acceptance:** This Test Ride milestone is rebuilt and locally verified but remains uncommitted, unpushed, and unpublished without a new explicit release request. Representative riders should still judge whether the stock Quarry loop communicates each bike's character clearly enough before purchase.

## Physical bike-condition feedback and crash consequences - 2026-07-26

- **Hidden wear made readable:** Bike condition already scaled the authoritative engine force, lateral grip, and maximum speed, but live riding exposed none of it. The bike now publishes one condition snapshot with exact `READY / LIGHT WEAR / WORN / DAMAGED / CRITICAL` semantics and matching physical factors. Full-condition bikes stay uncluttered; any wear adds a persistent textual `BIKE <STATUS> // <PERCENT>%` strip beneath the speed cluster.
- **Immediate crash receipt:** A damaging landing now produces `BIKE DAMAGE -N // N% // STATUS` in the live message layer while retaining the existing physical impact audio. Automatic tipped and world-fall recoveries now cause bounded 5% and 8% condition loss and use the established recovery audio; unknown automatic crash recovery uses 4%. Manual resets, Academy, release smokes, and economically inert Test Rides remain non-damaging.
- **Accessible browser observability:** The Web text projection exposes the same bounded condition percent and semantic status as the HUD, so essential damage state does not depend on color. The production default correctly reports `100 / READY`, while the rendered 1280x720 damaged-state audit at `output/bike-condition-visual-audit.png` shows the persistent 45% Damaged strip and the -8 damage receipt without overlapping speed, Flow, racecraft, or controls.
- **Deterministic release proof:** The new condition probe covers all five severity bands, exact physics-factor agreement, landing thresholds, automatic crash policy, manual-reset isolation, bike signal publication, persistent HUD text, warning polarity, and full-condition suppression. The parser/editor scan, all 63 curated probes, and six production activity smokes pass in `484.1 seconds`; physics retains `0.983` jump momentum and a `32.00 m` reference jump, and the opponent matrix retains 51 tactical Flow boosts in both strong and elite pressured samples.
- **Exact Web candidate:** All 17 delivery/recovery tests pass. The exact local pack is `index.8bfe13b85137.pck` (`5,209,820` bytes; SHA-256 `8bfe13b851374bde6595e63dcccca9007446170f478258a55ff2ea35aa00fac4`) beside the unchanged `index.9778df3b5d0e.wasm`. The required final-package client opens on Quarry Event 01/18 at `output/bike-condition-final-client/shot-0.png`; its browser state reports the starter Tyke at `bike_condition_percent=100`, `bike_condition_status=READY`, and no hidden damage clutter.
- **Release status and next acceptance:** This condition milestone and the earlier local Test Ride, landing, balance, and browser-observability milestones remain uncommitted, unpushed, and unpublished without a fresh explicit release request. Representative riders should still judge damage frequency, repair economics, and status-strip legibility during long races across display sizes.

## Local Duel pass-the-controller competition - 2026-07-26

- **Complete rotating-challenge duel:** Daily and Weekly Challenge cards now expose `LOCAL DUEL // 2 RIDERS // 2 RUNS EACH` on keyboard, gamepad, and touch. Starting one locks the exact rotating challenge, immediately launches Rider 1, then returns to the Garage between runs with the correct rider/attempt handoff. Completed duels retain final standings and offer a clear new-duel path.
- **Reliable identity and recovery:** The modular race service atomically persists the selected event, exact challenge ID, rider/attempt cursor, and best times through the existing save lifecycle. Invalid or stale payloads sanitize safely, interrupted sessions resume at the correct handoff, unrelated challenge results cannot advance the duel, and the complete state is projected into browser-readable JSON.
- **Readable competition presentation:** Live challenge headers identify the active rider, results show Local Duel standings and completed runs, the footer explicitly asks players to pass the controller, Garage cards expose the next turn, and Workshop close restores the contextual Duel caption. The final browser frame cleanly reads `DAILY CHALLENGE // RIDER 1` above the full 12-rider staging grid.
- **Fair challenge authority:** Production-browser testing found that challenge composition could be rejected when the rider's saved transmission or response settings differed from the schedule's assumed defaults. Daily and Weekly runs now explicitly force Automatic transmission plus the neutral standardized control response, and both values are part of the signed playable identity. The fixed browser flow reports `valid=true` with no console or page errors.
- **Deterministic release proof:** The parser/editor scan and all 64 curated high-risk probes pass, including the new Local Duel configure/persist/recover/identity/handoff/HUD/touch contract. Academy, Circuit, Pine Enduro, Mesa MX, Freestyle, and Discovery production activity smokes also pass. Opponent pressure remains challenging and legitimate: the strong and elite samples each execute 51 tactical Flow boosts, while bike dynamics retain `0.983` jump-speed retention and `32.00 m` distance. `git diff --check` is clean.
- **Exact Web candidate:** All 17 delivery/recovery tests and 22 subtests pass. The local `racecraft-v28` pack is `index.da8f4ada35a6.pck` (`5,220,396` bytes; SHA-256 `da8f4ada35a65029efe3b06f9e59becb8606824adfaec2357291ed9dbc31a7f2`) beside the unchanged `index.9778df3b5d0e.wasm`. The required client and direct production flow verify the Daily card, first-rider launch, structured Local Duel state, full field, and zero browser errors.
- **Release status and next acceptance:** This Local Duel milestone and the accumulated local riding, Test Ride, condition, landing, balance, and observability work remain uncommitted, unpushed, and unpublished without a fresh explicit release request. Representative players should still judge controller handoff clarity and tie/standings motivation through complete four-run duels on keyboard, gamepad, and touch.

## Persistent player-authored Custom Tours - 2026-07-26

- **Player-owned replayability:** The Garage now has a nineteenth event card, Custom Tour, where riders build a unique two-to-five round championship from unlocked Circuit, Enduro, Mesa, Elimination, Rival, Endurance, Hillclimb, Wet, and Rhythm events. The authored order persists, launches the exact next real race, and can be rebuilt after the finale without introducing a separate simplified gameplay path.
- **Meaningful series stakes:** Custom Tours use the production championship points table, full-field classifications, deterministic countback, standings, and champion resolution. Results identify the completed round, show the leading four riders and points, guide the player to the exact next round, and finish with an explicit champion rather than silently returning to unrelated progression.
- **Reliable lifecycle and input parity:** Draft, active calendar, round results, and completion state save through the atomic primary/backup lifecycle with rollback on write failure and recovery from a corrupt primary. Garage keyboard/gamepad actions, contextual touch labels, active-tour startup focus, return-to-Garage routing, and browser-readable state all consume the same authority. Wrong events, invalid/DNF results, duplicates, locked rounds, and over-capacity calendars cannot advance or corrupt the series.
- **Deterministic production proof:** The new Custom Tour probe covers minimum/maximum rules, exact sequencing, Garage construction and launch, atomic persistence/backup recovery, result identity, points/champion resolution, HUD presentation, and touch captions. The final parser/editor scan, all 65 curated production probes, and six clean activity smokes pass in approximately `499 seconds`; the strict bike suite retains `0.983` jump-speed retention and `32.00 m` distance, and the opponent pressure matrix retains 51 tactical Flow boosts in both strong and elite traffic samples. `git diff --check` is clean.
- **Exact Web candidate:** All 17 delivery, recovery, accessibility, isolation, text-state, and packaging tests pass. The local v28 pack is `index.f42552e1cba2.pck` (`5,255,912` bytes; SHA-256 `f42552e1cba22b1f1ece3e79a84afd293de67ac909ac6502c695675233234ec8`) beside the unchanged `index.9778df3b5d0e.wasm`.
- **Browser proof:** The required final-package client visibly reaches `CUSTOM TOUR // EVENT 19 / 19 // EMPTY` at `output/custom-tour-final-client/shot-0.png`; its structured state reports the same card, empty phase, and builder action with no console-error artifact. A direct production traversal visually verifies Saved Building feedback, candidate browsing, a readable Quarry/Pine two-round calendar, the enabled Start action, transition into the real Quarry event, and a live twelve-rider race with zero browser errors. Browser inspection caught and corrected the stale outer-wrapper “eighteen-event” copy before the final rebuild.
- **Release status and next acceptance:** This Custom Tour milestone is rebuilt and locally verified but remains uncommitted, unpushed, and unpublished because this continuation did not receive a new release instruction. Representative players should still judge calendar-building clarity, series motivation, event-mix strategy, and long-session pacing across input methods and display sizes.

## Complete bike / track / surface balance matrix - 2026-07-26

- **One production surface authority:** `DirtBikeController` now exposes ordered, read-only projections for all seven terrain profiles (`PACKED`, `DIRT`, `LOAM`, `LOOSE_DIRT`, `GRAVEL`, `MUD`, and `ROCK`) and all five session grip modifiers (`PACKED`, `WET`, `MUD`, `LOOSE`, and `RUTTED`). The live controller consumes those same canonicalization, terrain-profile, and session-grip functions, so deterministic balance checks cannot drift from shipped friction, drag, roughness, roost, or wet/loose penalties.
- **Explicit cross-product coverage:** The new bike/track balance matrix tests every catalog bike on Quarry, Pine, and Mesa; every physical terrain surface; every session surface modifier; Trail, Balanced, and Attack handling kits; and full-condition stock, maximum-upgrade, all-negative extreme tune, and all-positive extreme tune builds. All `3,780` combinations stay finite and inside bounded engine, grip, suspension, damping, brake-bias, preload, speed, braking, stability, air-control, drag-adjusted-speed, and surface-grip envelopes.
- **Meaningful build and tune contracts:** Every maximum build fills all five compatible upgrade slots and improves overall rating over stock. Both extreme tunes remain safe while preserving real tradeoffs: positive gearing increases acceleration but reduces top speed, and chassis extremes reach the actual spring stiffness, compression, rebound, front brake bias, and preload solver parameters. All seven terrain profiles are mechanically distinct.
- **Physical regression:** The allocation-free final source passes the parser/editor scan, all `93` focused production probes, and all six clean activity smokes in `727.2 seconds`. The full gate includes the new matrix; bike dynamics retain `0.983` jump-speed momentum and a `32.00 m` reference jump, while exact player-envelope traversal remains blocker-free across all three complete production routes. `git diff --check` reports no whitespace errors.
- **Exact Web candidate:** All 17 delivery, recovery, accessibility, isolation, text-state, and packaging tests pass. The local pack is `index.e73a35236b3f.pck` (`5,343,396` bytes; SHA-256 `e73a35236b3ff522054047525dbf2300b4111a077ffcbfd0c1981b65d5d65372`) beside the unchanged `index.9778df3b5d0e.wasm` runtime. Manifest, disk, inner-page reference, and HTTP payload agree exactly.
- **Required browser proof:** The prescribed `develop-web-game` Playwright client, using the shared `action_payloads.json`, opened the production wrapper and captured `output/bike-balance-matrix-optimized-final-required-client/shot-0.png`. The inspected 1280x720 frame is readable and stylistically intact, starts at Quarry Trail `EVENT 01 / 19`, and reports schema 4, a clean full-condition Tyke, and no console-error artifact.
- **Release status and next acceptance:** This matrix and the accumulated local milestones remain uncommitted, unpushed, and unpublished because this continuation received no new release instruction. The matrix proves deterministic compatibility and safe numeric envelopes; representative riders should still perform subjective handling and pace acceptance for stock, upgraded, and extreme builds on every terrain with keyboard, controller, and touch.

## Distinct sand and grass production surfaces - 2026-07-26

- **Real handling identities:** `DirtBikeController` now treats `SAND` and `GRASS` as first-class physical surfaces instead of aliasing sand to loose dirt and grass to packed ground. Sand has the lowest dry friction, second-highest drag, and strongest roost; grass has firmer grip, moderate rolling cost and roughness, and restrained roost. Both also have ordered, bounded session grip modifiers. The authoritative catalog is now nine physical surfaces and seven session modifiers.
- **Authored terrain, not enum-only coverage:** Quarry Freestyle's complete central pad uses its own warm procedural sand material and reports `SAND` through the real collision metadata consumed by wheel rays. Pine Ridge keeps the timed ribbon as loam while its green generated off-trail terrain and continuous catch floor report `GRASS`, making line departures mechanically and visually meaningful without changing the race route.
- **Complete feedback chain:** Permanent `engine_sand.res` and `engine_grass.res` loops provide unique SHA-identified waveforms on the independent Engine bus; the audio baker now has a clean scene-based `--engine-only` path that refreshes all eight engine/contact assets in `3.2 seconds` without rerendering music. Sand and grass also have unique roost colors and materially different equipment-wear behavior, with sand producing substantially more dry coating than grass under identical exposure.
- **Deterministic proof:** The new cross-system fidelity probe verifies all nine unique physics signatures, seven baked contact-loop identities and loop bounds, canonical aliases, session penalties, roost colors, equipment wear, texture color families, and the actual Quarry/Pine collision bodies. The expanded bike/track matrix passes all `6,804` combinations across three bikes, three tracks, nine surfaces, seven session modifiers, three setups, and four stock/max/extreme build states.
- **Production regression:** The parser/editor scan, all `94` focused production probes, and all six clean activity smokes pass in `753 seconds`. Focused audio, wear, strict bike dynamics, three-lane player-envelope route traversal, Pine Enduro, and Freestyle checks pass directly; bike dynamics retain `0.983` jump-speed momentum and `32.00 m` distance, while strong and elite opponent pressure still execute 51 tactical Flow boosts each. `git diff --check` remains clean.
- **Exact Web candidate:** All 17 delivery, recovery, accessibility, isolation, text-state, and packaging tests pass. The local pack is `index.76bbf0248cf6.pck` (`5,406,852` bytes; SHA-256 `76bbf0248cf6b4ce67c9485d3d3057c35595d2b9cb66151e36bfeef755eb953a`) beside the unchanged `index.9778df3b5d0e.wasm` runtime. Manifest, disk, inner-page reference, and the live `127.0.0.1:8777` immutable payload match exactly with the required cross-origin-isolation headers.
- **Browser proof:** The prescribed `develop-web-game` client using the shared `action_payloads.json` captured the clean Quarry-first Garage at `output/surface-feedback-final-required-client/shot-0.png`; it reports schema 4, Event 01/19, a full-condition Tyke, and no console-error artifact. A supplemental production traversal reaches live Quarry Freestyle at `output/surface-feedback-freestyle-sand-final-client/shot-0.png`; its structured state reports `activity=FREESTYLE`, `player.surface=SAND`, and the inspected 1280x720 frame visibly shows the granular warm sand pad.
- **Release status and next acceptance:** This surface milestone and the accumulated local work remain uncommitted, unpushed, and unpublished because this continuation received no new release instruction. Representative riders should still compare sand drag/roost and grass line-departure feel subjectively across keyboard, controller, and touch, especially in wet sessions and on lower-powered bikes.

## Coherent variable-weather endurance - 2026-07-26

- **One deterministic forecast authority:** Mesa Six-Lap now progresses through `CLEAR/PACKED`, `WINDY/LOOSE_DIRT`, `OVERCAST/PACKED`, `WET/WET`, `STORM/RUTTED`, and `OVERCAST/PACKED`. The complete dry/wind/cloud/rain/storm/drying sequence is readable on the pre-event card, and each lap exposes the next condition before it arrives.
- **Complete physical and presentation chain:** The effective lap condition drives player weather and session grip, opponent wet/rutted-surface skill, opponent roost and surface audio, rider/bike wetness, evolving-groove risk, gradual sky/fog/sun/precipitation transitions, live current-next HUD copy, and compact schema-7 browser state. Racecraft revision 7 separates records and ghosts created under the new pace model.
- **Opponent challenge retained:** The production field continues to earn Flow from drafting, clean lines, mastered sections, and landings, then tactically spends it on bounded boosts. The strong and elite pressured samples each execute 51 boosts while Relaxed/Standard/Expert ordering, build matching, deterministic replay identity, traffic stability, and fair starter/upgraded placement contracts remain green.
- **Deterministic release proof:** The new variable-weather probe verifies the exact six-lap forecast, static-event isolation, controller/pack agreement, condition and next-lap messages, and gradual storm onset/drying. The parser/editor scan, all `98` focused probes, and six production activity smokes pass in `719.9 seconds`; the post-gate Quarry/Pine terrain audit repair then passes its complete `18.99-second` full-ribbon check without the former missing test material.
- **Exact Web candidate:** All 17 delivery/recovery tests pass. The exact package is `index.6e651106bec7.pck` (`5,447,140` bytes; SHA-256 `6e651106bec7ca2853a98dfff9e6e9c6d1a8d6fb9f2e364a9bc708a73006902e`) beside the unchanged `index.9778df3b5d0e.wasm` runtime. Disk, manifest, inner-page reference, and the `127.0.0.1:8777` immutable payload agree with required isolation headers.
- **Browser proof:** The prescribed client and shared actions open the exact production package at `output/variable-weather-final-required-client/shot-0.png`; schema 7 reports the bounded conditions projection with no console-error artifact. A second inspected frame at `output/variable-weather-endurance-briefing-final-client-v2/shot-0.png` visibly shows Mesa Six-Lap Event 10/19 and its full forecast without clipping.
- **Release status and next acceptance:** The accumulated production candidate is ready for the explicitly requested source commit, branch push, and GitHub Pages republication. Representative riders should still judge the six-lap transition timing, wet-rut readability, and storm intensity across displays, performance settings, and input methods.

## Evolving-track and variable-weather GitHub Pages publication - 2026-07-26

- **Source release:** Commit `cd7526c7d421fbf5f9d729b2f46816c53ea96fb7` publishes event-correct sponsor landmarks, session-local evolving grooves, coherent six-lap variable weather, schema-7 observability, racecraft revision 7, opponent wet-condition behavior, deterministic probes, and the exact verified Web bundle on `codex/racecraft-v27`.
- **Pages deployment:** GitHub Pages commit `59af71b21e171dd3a63005f2c26d4eaa77cfeddc` completed successfully from `gh-pages` with HTTPS enforcement at `https://willtran87.github.io/project-riding-dirty/`.
- **Public payload proof:** The public manifest and inner page reference `index.6e651106bec7.pck`. An independent public download is exactly `5,447,140` bytes with SHA-256 `6e651106bec7ca2853a98dfff9e6e9c6d1a8d6fb9f2e364a9bc708a73006902e`, matching the committed release manifest; the former `index.260068df35da.pck` returns 404.
- **Public browser proof:** The prescribed shared-action client reaches the production wrapper and schema-7 Garage at `output/github-pages-variable-weather-final-client/shot-0.png`. The inspected 1280x720 frame opens on Quarry Trail Event 01/19 and reports no console/page error artifact.
- **Release gates:** The published source passed the parser/editor scan, all `98` focused probes, six production activity smokes in `719.9 seconds`, the repaired `18.99-second` terrain-ribbon audit, all 17 Web delivery/recovery tests, exact local/public payload verification, Mesa forecast inspection, and production-browser startup. Representative riders should still judge evolving-line strategy and variable-weather pacing across complete sessions.

## Player-selectable crash accessibility - 2026-07-26

- **Meaningful Standard/Assisted contract:** Settings now exposes `CRASH SUPPORT` on the Access page. Standard preserves the original experience. Assisted halves lasting landing and automatic-recovery condition damage and recognizes a stationary tipped bike after `0.6555` seconds instead of `1.15`, while manual reset remains damage-free.
- **Competitive honesty and session safety:** The preference uses verified settings schema 16 and is frozen when each activity is composed. Mid-activity changes save for the next event. Reset counts, integrity incidents, `NO_RESETS`, and configured time penalties are unchanged; racecraft revision 8 and the normalized competitive context separate Standard and Assisted records/ghost slots.
- **Clear production observability:** The bike and race session expose their exact support mode through compact schema-7 Web state, including the authoritative tipped-recovery delay. The settings receipt explains that Assisted means less damage and faster tip recovery, and existing semantic save feedback confirms persistence.
- **Deterministic release proof:** The new crash-support probe verifies defaults, Access presentation, run locking, physical bike configuration, damage scaling, penalty preservation, competitive separation, and verified reload. Directly affected condition, automatic recovery, settings, navigation, sensory accessibility, competitive services, and Web-state probes pass. The curated gate passes all `78` probes plus six production activity smokes in `527.1` seconds; full discovery passes all `99` focused probes in `596.7` seconds.
- **Exact Web candidate:** All 17 delivery/recovery tests pass. The local package is `index.d6186dba956c.pck` (`5,451,896` bytes; SHA-256 `d6186dba956cd41f42b731987894c00faa241254bbb4d9faec05062c08a6fb31`) beside `index.9778df3b5d0e.wasm`. Manifest, disk, inner-page reference, and the live `127.0.0.1:8777` response agree; the asset is served immutable with required cross-origin-isolation headers.
- **Packaged-browser proof:** The prescribed `develop-web-game` client and shared `action_payloads.json` open the exact Quarry-first package at `output/crash-support-release-client/shot-0.png` without a console-error artifact. Supplemental packaged Chromium interaction visibly confirms the readable 15-row Access page, switches Crash Support to Assisted with a saved receipt, and launches Quarry with both bike and race state reporting `ASSISTED`, a `0.6555`-second tipped recovery delay, and zero console/page errors.
- **Release status:** This milestone is included in the accumulated candidate authorized for source commit, branch push, and GitHub Pages republication. Representative riders should still judge whether the Assisted timing and reduced damage feel supportive without flattening the consequences of mistakes.
